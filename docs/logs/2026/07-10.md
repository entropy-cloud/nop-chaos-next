# Development Log — 2026-07-10

### 2026-07-10

- **解耦 prototype extension 与主项目默认构建路径** — 修复默认 `pnpm dev:main` 因 `import('@prototype-extension')` 无法解析而报错的问题（commit `8ab3d23` 引入的静态耦合缺陷）。根因：`config.ts:46` 的静态 `import('@prototype-extension')` 只在 prototype 模式注册了 Vite alias，默认模式无 alias → Vite resolver 失败；`declare module '@prototype-extension'` 只骗过 tsc，不解决 Vite 运行时解析。
- **改动**（彻底解耦，跨 `apps/main` + `packages/vite-plugin-prototype-server`）：
  - `packages/vite-plugin-prototype-server/src/index.ts`：`PrototypeServerOptions` 新增 `extensionEntry?`；新增 `transformIndexHtml`，当 `extensionEntry` 存在时把 `index.html` 的 `<!--NOP_EXTENSIONS_INJECT-->` 替换为注入 `window.__NOP_EXTENSIONS__ = [{ id, entry: '/@fs/<abs>' }]` 的 `<script>`。
  - `apps/main/vite.config.ts`：删除 `@prototype-extension` 条件 alias；`importPrototypePlugin` 透传 `prototypeExtensionResolved`。
  - `apps/main/src/extensions/config.ts`：删除 `getPrototypeExtensionSource()` 及其在 `getExtensionSources()` 的调用（prototype 现由优先级 1 `window.__NOP_EXTENSIONS__` 接管）。
  - `apps/main/src/types/demo-extension.d.ts`：删除 `declare module '@prototype-extension'`。
- **设计依据**：prototype 本就是独立启动入口（`dev:main:amis-prototype` / `flux-prototype`，dev-only），复用 `extension-system.md` 7.4.4 的最高优先级运行时注入路径；out-of-root 的 `examples/` 源码用 Vite `/@fs/` URL 在 `server.fs.allow` 内服务。
- **文档**：更新 `docs/design/amis-flux-json-prototyping-demo.md` §6（plugin 接口 + 解耦约束）、`docs/design/extension-system.md` §7.4.4（prototype dev 走优先级 1 的说明）。
- **验证**：`pnpm typecheck`（main + plugin）✅；`pnpm lint`（main + plugin）✅；plugin dist 重建 ✅；`extensions/index.test.ts` 11/11 ✅；`test:e2e:amis-prototype` 3/3 ✅；`test:e2e:flux-prototype` 3/3 ✅；`vite build`（默认模式）✅。

## Commits

nop-chaos-next commit included in this log entry.
