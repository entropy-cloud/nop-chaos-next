# Development Log — 2026-07-04

### 2026-07-04

- **Corrected stale Flux runtime status**: `docs/design/amis-flux-rendering-engine-integration.md` §1 status table claimed the Flux runtime was a placeholder ("当前是占位实现"). That was a stale snapshot. Actual state — `@nop-chaos/flux` core package is delivered via tarball (`libs/nop-chaos-flux-0.1.0.tgz`), declared in `apps/main/package.json`, resolved by pnpm, and `apps/main/src/flux/FluxRouteRenderer.tsx` performs real rendering via `createFluxSchemaRenderer()` with full schema fetching / loading / error handling (plus `adapter.ts`, `providers.ts`, `state.ts`, `testSchema.ts`). Updated the table row to reflect this.
- Corroboration: Flux prototype e2e has been green since 06-19 (`tests/e2e/flux-prototype.spec.ts`), confirming the integration is live, not placeholder.
- Scope note: only the §1 "现状" table row for Flux runtime was outdated. The rest of the doc (delivery options, recommended core-package scheme, host integration boundary) remains accurate — the recommended tarball delivery from §5 is exactly what landed.
