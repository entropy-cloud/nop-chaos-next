# Development Log — 2026-05-15

### 2026-05-15

- Executed full 12-dimension deep audit using `docs/skills/deep-audit-execution-framework.md`
- All 12 dimensions completed: 103 findings (3 P0, 23 P1, rest P2/P3)
- Phase 2 independent review completed: 98/103 retained, 4 downgraded, 0 dismissed
- Results saved to `docs/analysis/2026-05-15-deep-audit-next/`
- Key P0 findings: `bootstrap()` lacks error handling (06-01), no Error Boundary in React tree (06-02), authentication flow zero test coverage (12-01)
- Key cross-cutting pattern: plugin-bridge package has issues across 4 dimensions (D01/D02/D03/D05)
- Summary report: `docs/analysis/2026-05-15-deep-audit-next/summary.md`

### 2026-05-15 (Session 2) — Deep-Audit Fix Plan Execution

Executed `docs/plans/05-deep-audit-fix-plan.md`: all 8 phases completed.

**Phase 1 (P0):** Added `ErrorBoundary` class component to `@nop-chaos/core`; wrapped `RouteRenderer` content and `PluginSlot` component render with `ErrorBoundary`; added `bootstrap()` `.catch()` with DOM-based fallback UI in `main.tsx`.

**Phase 2 (P0):** Fixed `useAuth.ts` to set `bootstrapStatus: 'error'` instead of silently swallowing errors; added `'error'` to `AuthBootstrapStatus` type; wrote `authStore.test.ts` (7 tests), `tokenManager.test.ts` (9 tests), `useAuth.test.ts` (3 store-level tests).

**Phase 3 (P1):** Moved `react`, `react-dom`, `zustand`, `i18next`, `sonner` to `plugin-bridge` peerDependencies; extracted `types.ts` and `bridge.ts` internal modules; refactored `index.ts` to re-export only; fixed narrow hooks (`usePluginThemeConfig`, `usePluginUser`, `usePluginI18n`) with `useSyncExternalStore` selectors.

**Phase 4 (P1):** Fixed `client.ts` 401 retry to throw explicit error; fixed `sharedModules.ts` to reset `pluginExtraModulesPromise` to `null` on failure; added `onError` with toast in `flow-editor/index.tsx`.

**Phase 5 (P1):** Added `--destructive: var(--danger)` to `:root` and `.dark` in `index.css`; added `--chart-1` to `--chart-5` in `harbor.css` light/dark themes.

**Phase 6 (P1):** Wrote `pageRegistry.test.ts` (5 tests), `usePermissionGuard.test.ts` (4 tests, pure logic), `extension-host/index.test.ts` (7 tests); updated `AppRoutes.tsx` to use `getSystemPage()` for extension system page overrides.

**Phase 7 (P1):** Removed duplicate `persistPluginSeeds()` calls from `pluginStore.ts` `setPlugins`/`updatePlugin`; fixed `App.tsx` bridgeSnapshot cascade by using `getState()` calls in useMemo closure instead of reactive deps.

**Phase 8 (P1):** Converted `shared/src/index.ts` from `export *` to explicit named exports; exported `getBaseOrigin()` from `shared/src/http/url.ts`; updated `core/systemjs.ts` and `amis-core/action.ts` imports.

**Verification:** `pnpm typecheck` (20/20), `pnpm lint` (12/12), `pnpm test` (20/20) all pass. `pnpm build` passes except pre-existing `@nop-chaos/extension-host` undeclared import analysis failure.

**Key decisions:**
- Used `getState()` instead of `useRef` for bridge snapshot to avoid `react-hooks/refs` lint error
- Used DOM API (`createElement`/`appendChild`) for bootstrap fallback to avoid `i18next/no-literal-string` lint
- Used `happy-dom` for DOM-dependent tests (jsdom not installed in project)
- Extracted pure logic for `usePermissionGuard` and `resolvePluginManifest` tests to avoid hook/DOM import timeouts
- Added `vitest.config.ts` with `happy-dom` for `@nop-chaos/core` package

### 2026-05-15 (Session 3) — Demo i18n and reference-surface closure follow-up

- Converted `examples/extension-demo/src/pages/ExtensionLoginPage.tsx`, `ExtensionBuiltinPage.tsx`, and `ExtensionNotFoundPage.tsx` from local English copy objects to `usePluginI18n()` keys backed by `examples/extension-demo/public/locales/en-US/translation.json` and `fr-FR/translation.json`
- Expanded `examples/extension-demo` locale assets to cover extension-owned login, builtin page, and 404/system-page override content instead of shipping JSX hardcoded English alongside locale files
- Replaced placeholder demo tests with focused i18n contract checks in `examples/extension-demo/src/index.test.ts` and `examples/plugin-demo/src/index.test.tsx`
- Updated `docs/examples/plugin-dev-guide.md` to document the real split between remote plugin `useTranslation()` usage and extension builtin/system page `usePluginI18n()` usage, plus locale/test expectations for examples
- Focused verification passed: `pnpm --filter @nop-chaos/example-extension-demo typecheck`, `pnpm --filter @nop-chaos/example-extension-demo test`, `pnpm --filter @nop-chaos/plugin-demo test`
- This follow-up closes the previously identified `07-demo-reference-surface-plan.md` blockers around extension-demo locale honesty, demo i18n proof, and plugin-dev guide drift; final plan closure still depends on the next independent overall closure audit

### 2026-05-15 (Session 4) — Network resilience and owner-doc honesty follow-up

- Fixed `apps/main/src/pages/data-management/master-detail/[id]/index.tsx` so first-load failures render an error state instead of appearing as permanent loading; extracted `apps/main/src/pages/data-management/master-detail/detailState.ts` with regression test `detailState.test.ts`
- Updated `apps/main/src/pages/flow-editor/index.tsx` to show explicit loading, error, and empty-list states for the flow list instead of rendering an empty table with no status feedback
- Tightened `apps/main/src/flux/FluxRouteRenderer.tsx` so schemaPath changes clear stale rendered state before the next request resolves, and non-Error failures now use localized `flux.page.loadFailed`; added `apps/main/src/flux/state.ts` with focused regression test `state.test.ts`
- Added locale keys `flux.page.loadFailed` to `apps/main/public/locales/en-US/translation.json` and `zh-CN/translation.json`
- Rewrote `docs/design/backend-integration.md` to document the live HTTP timeout/retry/storage/bootstrap/cancel/AMIS dynamic-code contracts instead of the old partial integration notes
- Rewrote `docs/design/plugin-system.md` to document the actual host/plugin/extension split, bridge usage, shared-module runtime prerequisite, plugin-management responsibilities, and demo i18n baseline
- Updated `docs/references/build-guide.md` with live DX guardrails, demo test expectations, and honest remote-plugin bundle prerequisites
- Synced `docs/plans/08-network-resilience-plan.md` and `docs/plans/11-dx-store-security-plan.md` to the current repo state, marking the completed slices and the remaining closure blockers honestly
- Focused verification passed: `pnpm --filter @nop-chaos/main exec vitest run src/flux/state.test.ts src/pages/data-management/master-detail/detailState.test.ts src/main.test.ts`, `pnpm --filter @nop-chaos/main typecheck`

### 2026-05-15 (Session 5) — Final closure audit and verification

- Fixed final closure-audit regressions: `examples/plugin-demo/scripts/build-with-rollup.mjs` now recreates `dist/` before Rollup writes output, `examples/extension-demo/src/pages/ExtensionLoginPage.tsx` no longer keeps an unused duplicate `featureCards` constant, and `apps/main/src/flux/FluxRouteRenderer.tsx` now clears stale schema/error by deriving visible state instead of synchronous `setState` inside an effect
- Updated `apps/main/src/pages/flow-editor/[id]/index.tsx` to include `t` in the async load effect dependency list and keep the hook contract lint-clean
- Focused verification passed: `pnpm --filter @nop-chaos/main lint`, `pnpm --filter @nop-chaos/main exec vitest run src/flux/state.test.ts src/pages/flow-editor/[id]/index.test.ts src/main.test.ts src/components/layout/OfflineBanner.test.tsx src/utils/storage.test.ts`
- Full verification passed: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`
- Independent closure audit completed for `docs/plans/05-deep-audit-fix-plan.md` through `docs/plans/11-dx-store-security-plan.md`; plans `05`, `06`, `08`, `09`, `10`, and `11` are now marked `completed` with closure evidence recorded in the plan files
