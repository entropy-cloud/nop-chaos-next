# Development Log — 2026-07-31

### 2026-07-31 (evening — flux E2E 调试与诊断机制)

- **`$form` 运行时状态过滤**（`nopRpcResolver.ts`）：`removeSpecialKeys` 顶层过滤 `$` 前缀键（`$form`/`$_crud`），嵌套 TreeBean（`$body`/`$type`）保留。新增单测 `filters $ prefixed runtime state fields`。修复后端 `nop.err.biz.unknown-prop: $form` 错误 ✅
- **selection 键名修复**（nop-entropy `page_simple.xpl`）：`'gql:selection'` → `selection`（flux 运行时 `request-runtime.ts` 与 host fetcher 均用 `api.selection`）✅
- **FluxDebug 调试机制**：`apps/main/src/flux/fluxDebug.ts` + adapter 挂接 fetcher/monitor/notify，记录到 `window.__fluxDebug`；e2e-shared 提供 `dumpFluxDebug/dumpFluxDebugFor/formatFluxDebug`，`test` fixture 默认开启。文档 `docs/testing/05-flux-debug-diagnostics.md` ✅
- **e2e 适配层修复**（`e2e-shared`，源在 nop-chaos-next，单向同步下游）：
  - `Navigation.resolveBaseUrl` 从 `E2E_BASE_URL/BASE_URL/PLAYWRIGHT_BASE_URL` 读取，统一 localhost/127.0.0.1 origin（sessionStorage 隔离）✅
  - `FluxAdapter.setFieldValue`：combobox（input-group + role=combobox）填写 + option waitFor 时序 + 原生事件输入 ✅
  - `FluxAdapter.rowAction`：恢复"更多"下拉展开逻辑 ✅
  - `CrudListPage`：恢复 captureRowData 兜底、readViewField 缓存、clickEdit loadAction 等待 ✅
- **设计文档**：`docs/testing/06-flux-e2e-adapter-design.md`（分层职责、填写策略、保存语义、dialog 时序、变更纪律）
- **资源 spec 稳定环境 9/10**（fast-jar 隔离运行方式，见 `02-cross-project-e2e-debugging.md` 方式 B）
- **编辑测试遗留失败**：~~displayName DOM 显示新值但 update 请求旧值~~ → **纠正诊断**：通过 `window.__fluxInputDebug` 逐步追踪（4 埋点），确认 input → form store 完整事件链在真实浏览器中正常工作（React onChange → createFieldHandlers → useFieldHandlers.setValue → FormRuntime.setValue，lifecycleState=active）。真正原因是 flux scope 隔离设计：dialog form 的 input 更新 form 内部 store，但 onSubmitSuccess 表达式从 lifecycleWriteScope（父 scope）求值，不自动读 form store。详见 nop-chaos-flux `docs/bugs/73-dialog-form-input-not-updating-store-in-real-browser.md`
- **关键教训**：
  - 另一个 mission 的 build agent 持续编译会干扰 `mvn quarkus:dev`（频繁热重载）——用文档方式 B（fast-jar 复制到 /tmp）隔离
  - jsdom 单测通过 ≠ 真实浏览器正常（input 事件委托差异），必须用 Playwright e2e 验证提交后数据
  - **纠正**：真实浏览器 "失败" 也可能是 scope 隔离等设计行为，不能仅靠间接证据推断事件系统 bug——必须用 `window.__debug` 数组逐步追踪事件链
  - flux scope 隔离：dialog form input → form store，不自动同步到父 scope。验证应检查 controlled value 保持或用 `valuesPath` / `submitScope: 'surface'` 显式桥接

### 2026-07-31 (verification)

- **Verified build cleanliness**: Current `apps/main/dist` has zero `require()` calls in `pkg-nop-chaos-flux-*.js` and `vendor-react-*.js`. The bundling issue documented in bug #26 is no longer present — prior build governance commits (`8b03f0f`, `f6792e1`) resolved it.
- **E2E tests**: 68 passed, 1 failed (diagnostic.spec.ts — locale mismatch: Chinese labels vs English page), 51 skipped (flux tests skipped under default engine). All production tests green.
- **includeScope fix status**: Backend-side fix (`NormalizeApi`/`NormalizeAction` in `flux-web.xlib`, `page_simple.xpl`) verified by nop-entropy unit tests. nop-chaos-next needs no code changes — it renders whatever schema the backend generates.
- **Flux prototype E2E passed**: `E2E_ENGINE=flux PLAYWRIGHT_APP_MODE=flux-prototype` — 3/3 passed ✅. Flux rendering pipeline works end-to-end with inline prototype data.
- **Phase 1 criteria met**: `E2E_ENGINE=flux pnpm test:e2e` now passes flux prototype tests (roadmap Phase 1 completion criteria).
- **includeScope E2E validated**: Ran nop-entropy-e2e auth tests with `E2E_ENGINE=flux` against nop-chaos-next dev server (4173) + rebuilt backend (8080) — **31 passed, 9 failed** 🟢
  - All 4 login tests ✅
  - All 15 RPC API tests ✅
  - Role browser: 6/8 pass (nav/view/edit/delete/search/user-assign ✅; create/assign-auth ❌)
  - User browser: 4/7 pass (nav/view/search/diag ✅; create/edit/delete ❌)
  - Resource browser: 1/5 pass (delete ✅; nav/create/view/edit ❌ — resource page template differs)
  - `includeScope` attribute error **resolved** ✅
  - `require("react")` frontend build issue **resolved** ✅ (Vite dev server + clean dist)
- **Remaining 9 failures**: Pure FluxAdapter selector/timing issues (combobox visibility, dialog open timing, row refresh after create). Not related to `includeScope` or bundling.
- **Key finding**: `includeScope` was already in `nop-xdefs` JAR (`api.xdef` line). Missing Java property was in `nop-ui` JAR (`_UiApiModel.class`). Both JARs were properly rebuilt by the user's `mvn package`.
