# Development Log — 2026-06-01

### 2026-06-01

- Diagnosed AMIS CRUD row-action wrapping as a host-side styling gap rather than a schema-generation issue: upstream `.cxd-ButtonGroup` defaults to `flex-wrap: wrap`, so `View + More` can wrap inside operation cells unless the host forces nowrap. Confirmed that the AMIS preview confirm popup also came from the host `ConfirmDialogHost`, not AMIS native dialog rendering. Code paths: `apps/main/src/styles/amis-fix.css`, `apps/main/src/amis/adapter.ts`, `apps/main/src/components/common/ConfirmDialogHost.tsx`, `apps/main/src/services/confirm.ts`.
- Added a mock AMIS table scenario and Playwright regression coverage for `dev:mock`, covering single-line row actions and AMIS-specific confirm dialog styling. Files: `apps/main/src/amis/testSchema.ts`, `tests/e2e/amis-preview-crud.spec.ts`, `docs/bugs/42-amis-operation-wrap-and-confirm-style-drift.md`.
- Verification: `pnpm test:e2e -- tests/e2e/amis-preview-crud.spec.ts`, `pnpm --filter @nop-chaos/main typecheck`, `pnpm --filter @nop-chaos/main lint`, `pnpm --filter @nop-chaos/main build`, `pnpm build`.

### 2026-06-01

- Removed the temporary root `sync:flux` alias so the live Flux source-baseline refresh command is explicitly `pnpm sync:flux:src`, and documented that `libs/*.tgz` is committed repository state rather than a disposable local cache. Paths: `package.json`, `libs/README.md`, `README.md`, `README.en.md`, `docs/references/build-guide.md`.

### 2026-06-01

- Switched AMIS and `@nop-chaos/flux` file-tarball consumption to repo-local `libs/*.tgz` so ordinary `pnpm install` / `pnpm build` no longer depend on sibling repos being present. Code paths: `apps/main/package.json`, `packages/amis-react/package.json`, `scripts/ensure-amis-file-deps-built.mjs`, `scripts/clean-dist-packages.mjs`.
- Added explicit import/refresh scripts for upstream artifacts: `scripts/import-amis-to-libs.sh`, `scripts/import-flux-to-libs.sh`, and `scripts/refresh-libs-deps.sh`. `repack-upstreams-and-refresh.sh` now imports into `libs/`, refreshes lockfile/install from `libs/`, and still allows optional Flux source sync into `flux-lib`.
- Updated build/docs workflow so `site` now means local `pnpm build` plus `sync-site`, while Flux source syncing remains optional/manual via `pnpm sync:flux:src`. Docs: `docs/references/build-guide.md`, `docs/references/flux-sync-spec.md`, `docs/design/main-bundle-dependency-spec.md`.

### 2026-06-01

- Restored locale-aware AMIS page schema caching in `apps/main/src/services/pageApi.ts` so repeated menu opens reuse the previously fetched `getPage` result instead of re-requesting the same schema on every route re-entry.
- Added regression coverage in `apps/main/src/services/pageApi.test.ts` for repeated same-path loads, locale-separated cache buckets, failed-request eviction, `/p/*` direct page fetches, and static `.json` schema assets.
- Key decision: keep the fix minimal at the page data layer instead of changing AMIS MobX store cleanup; the regression was specifically that `getPage` result caching from the legacy app was missing in the new host.
- Next step: validate with `pnpm --filter @nop-chaos/main exec vitest run src/services/pageApi.test.ts`, then run `pnpm typecheck`, `pnpm build`, and `pnpm lint`.
### 2026-06-01

- Extended the shell runtime contract so extension `shell.sidebarWidthRem` and `shell.sidebarCollapsedWidthRem` can provide host sidebar default widths, with precedence defined as user layout setting > extension default > built-in default. Code paths: `packages/shared/src/types/extension.ts`, `packages/extension-host/src/runtime.ts`, `apps/main/src/store/layoutStore.ts`, `apps/main/src/router/AppShell.tsx`.
- Reduced the built-in expanded sidebar default width from `18rem` to `16rem` and updated the example extension to demonstrate extension-owned sidebar width defaults. Code paths: `apps/main/src/config/layout.ts`, `examples/extension-demo/src/index.ts`.
- Updated design docs for layout settings and extension shell config; added regression coverage for runtime config merge and layout width precedence. Docs: `docs/design/layout-settings.md`, `docs/design/extension-system.md`. Tests: `packages/extension-host/src/index.test.ts`, `apps/main/src/store/layoutStore.test.ts`, `apps/main/src/hooks/useShellConfig.test.ts`.
