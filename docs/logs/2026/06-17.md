# Development Log — 2026-06-17

### 2026-06-17 (implementation)

- **Executed Plan 31**: AMIS / Flux JSON Prototyping Demo implementation
  - `packages/delta-merge/`: Pure JSON Delta merge engine created. Exports `mergeNode`, `mergeProperty`, `mergeArray`, `resolvePrototypes`, `cleanupXProps`. Supports `x:extends` chain inheritance, `x:override` (5 ops), `x:prototype` cloning, array merge by `id`, cycle detection. 17 unit tests passing.
  - `packages/vite-plugin-prototype-server/`: Vite plugin serving `/api/prototype/menu.json`, `/api/prototype/pages/*.json` (with x:extends via `@nop-chaos/delta-merge`), `/api/prototype/assets/*`, mock middleware auto-loading from `mock/` directory, HMR via `server.ws.send('prototype:change')`.
  - `examples/amis-prototype-demo/`: ShellExtension using async `getExtension` to fetch menu from prototype server, `pageType: 'amis'`.
  - `examples/flux-prototype-demo/`: Symmetric ShellExtension, `pageType: 'flux'`.
  - Host integration: `apps/main/vite.config.ts` conditional plugin (via `VITE_PROTOTYPE_DIR`), `apps/main/src/extensions/config.ts` supports `VITE_PROTOTYPE_EXTENSION_ENTRY`, `.env.amis-prototype`, `.env.flux-prototype`, `.env.prototype`, root `package.json` scripts `dev:main:amis-prototype` and `dev:main:flux-prototype`.
  - Typecheck passes for all 5 new/altered packages.
- Key decision: prototype extensions use `getExtension` named export (async) since `loadExtensions.ts` supports this async lifecycle for dynamic `userMenuItems`.
- Phase 6 e2e manual verification deferred.

### 2026-06-17 (later)

- 新增设计文档：`docs/design/amis-flux-json-prototyping-demo.md`
- 设计了两个 demo extension：
  - `examples/amis-prototype-demo/`：根据配置目录下的 `menu.json` 生成 AMIS 页面，用于 HTML 原型 → AMIS JSON 的快速验证
  - `examples/flux-prototype-demo/`：对称设计，使用 Flux 渲染引擎
- 共享的 Vite plugin `packages/vite-plugin-prototype-server/` 提供 mock server 能力：读取 `VITE_PROTOTYPE_DIR` 目录、serving 页面 JSON、HMR 热更新
- 新增核心包 `packages/delta-merge/`（`@nop-chaos/delta-merge`）：Nop 平台标准 Delta 差量合并算法的 JSON 实现，递归解析 `x:extends`、`x:override`、`x:prototype`，支持 `merge/replace/remove/bounded-merge/merge-replace` 算子
- 关键决策：mock server 使用 Vite plugin 而非独立 Express 服务；共享 plugin 避免两个 demo 重复；extension 通过 `userMenuItems` 注入菜单；`x:extends` 在服务端解析而非浏览器端；delta-merge 独立包而非内联
- Next step: 实现 `packages/delta-merge/` → `packages/vite-plugin-prototype-server/` → `examples/amis-prototype-demo/` → `examples/flux-prototype-demo/`

---

- 修复 `.gitignore` 误伤源码目录的 bug：原第 7 行 `extensions/`（无前导斜杠）会匹配任何层级的 `extensions/` 目录，实测 `apps/main/src/extensions/` 下的新增文件会被静默忽略（现有文件仅因先于规则入库才被追踪）。该规则在 commit `b13babb`（contribution→extension 改名）中与源码目录改名同时引入，属于误配。
- 改为 `/extensions/*` + `!/extensions/.gitkeep`：锚定根目录，只忽略根目录 `extensions/` 的内容，保留目录结构（新增 `extensions/.gitkeep` 占位说明）。
- 验证：`git check-ignore` 确认 `apps/main/src/extensions/` 下文件不再被忽略；`git add --dry-run extensions/.gitkeep` 成功；根目录 `extensions/` 子项仍被忽略。
- 更新 `AGENTS.md` Workspace Layout 与 Extension Project Boundary，明确根目录 `extensions/` 的定位与命名约束，防止规则再次被误改。

---

- 将外部 AMIS 框架开发参考 `amis-guide/` 从 `C:\can\sources\amis-guide` 迁入仓库根目录，作为 `docs/` 的同级目录。
- 内容包括：`README.md`（架构与类型索引）、`01-quickstart.md`（常用代码段）、`02-reference.md`（表达式/API/事件/数据流核心机制）、`amis-types/*.d.ts`（组件 TypeScript 接口，字段知识源）、`design-patterns/`（业务场景 cookbook）。
- 说明：`docs/amis-types` 在仓库中并不存在，无需删除（已与用户确认跳过）。
- 完善各类索引：
  - `AGENTS.md`：Scope 与 Workspace Layout 新增 `amis-guide/` 条目。
  - `docs/index.md`：新增 Sibling References 段落、Read This First 行、Reading Guide 条目。
  - `docs/source-of-truth-and-precedence.md`：新增“AMIS 框架知识参考”路由条目，区分上游框架知识与本仓库 AMIS 集成设计。
  - 根 `README.md`：项目结构补 `amis-guide/`。
  - `amis-guide/README.md`：扩充文件索引，新增 `design-patterns/` 清单与 `amis-types/` 入口说明。
- 关键决策：amis-guide 定位为外部框架知识（type 字段、表达式、事件等），与本仓库“如何集成 AMIS”的设计文档（`docs/design/amis-theme-bridge.md`、`amis-flux-rendering-engine-integration.md`）职责分离。

---

### 2026-06-17 (Phase 6 verification - Plan 31 closure)

- **Completed Plan 31 Phase 6**: e2e API verification of AMIS/Flux JSON Prototyping Demo
- **Resolved runtime blocker**: Built packages/delta-merge and packages/vite-plugin-prototype-server (created tsconfig.build.json for both, fixed .js extension imports for Node.js ESM, fixed Windows pathToFileURL for mock middleware import)
- **Fixed config bugs**: delta-merge exports to dist/, VITE_PROTOTYPE_DIR path (../../prototypes/...), optimizeDeps.exclude flux, async mock loading
- **Phase 6 API verification**: menu (8 items), pages (8/8), mock (7 endpoints), 404 handling - all passed
- **Pre-existing blocker**: @nop-chaos/flux dependency optimization error (not in Plan 31 scope)
- **x:extends e2e verified**: added `prototypes/amis-demo/pages/xextends-test.json` extending `_base/page-base.json`; merge-replace override verified via live API
- **Bugs recorded**: `docs/bugs/43-` (configureServer async fire-and-forget), `docs/bugs/44-` (TypeScript ESM `.js` extension)
- **Experience doc**: `docs/references/workspace-package-build-for-node-esm.md`
- **Independent closure audit**: fresh opencode session (task `ses_12a7c8ce0fferGgLZ92PcKVrw0`) returned PASS-WITH-NOTES; 4 issues identified and fixed (unchecked gates, mock-loader text mismatch, log wording, pending closure section)
- **Plan 31 closed**: `Plan Status: completed` after audit compliance
