# Development Log — 2026-05-17

### 2026-05-17

- 继续收口动态加载边界：`extension-host`、`core/systemjs`、`AMIS xui:import` 现统一只接受相对路径或根路径输入，并通过 shared URL helper 解析为同源地址；带协议 URL（含 `https:` / `data:`）会被拒绝，而不是直接放行：`packages/shared/src/http/url.ts`、`packages/extension-host/src/loadExtensions.ts`、`packages/core/src/utils/systemjs.ts`、`packages/amis-core/src/page/action.ts`。
- 同步修正 extension / plugin 相关测试，使测试夹具与新契约一致：入口配置测试改为断言相对/根路径，remote loader 回归测试新增协议拒绝覆盖：`apps/main/src/extensions/index.test.ts`、`packages/extension-host/src/index.test.ts`、`packages/core/src/utils/systemjs.test.ts`、`packages/amis-core/src/page/action.test.ts`、`packages/shared/src/http/url.test.ts`。

- 继续收口对抗性审查遗留问题：`useShellConfig()` 与 `usePluginNotifications()` 现已改为基于订阅的响应式 hook，避免 runtime config / bridge 注入变化后 UI 不更新：`packages/extension-host/src/runtime.ts`、`apps/main/src/hooks/useShellConfig.ts`、`packages/plugin-bridge/src/index.ts`。
- 修复 auth 双存储与跨层 refresh 去重：`authStore.login/logout` 现在同步 managed token storage，shared HTTP client 与 tokenManager 共用同一个 refresh promise，避免主动刷新与 401 重试并发时重复刷新：`apps/main/src/store/authStore.ts`、`packages/shared/src/auth/tokenManager.ts`、`packages/shared/src/http/client.ts`。
- 给 extension bootstrap 增加并发去重，避免启动期多次调用造成非原子重复初始化：`apps/main/src/extensions/bootstrap.ts`。
- 为 AMIS schema 递归处理补充循环引用与深度保护，并新增回归测试，避免异常 schema 触发无限递归或栈溢出：`packages/amis-core/src/page/processor.ts`、`packages/amis-core/src/page/processor.test.ts`。
- 记录跨包 auth/refresh 契约问题的 bug note：`docs/bugs/35-auth-managed-token-sync-and-shared-refresh-lock.md`。
- 收口 Plan 20：应用层 `--radius` 覆盖已移除，`themeCss.ts` 现在提供 `subscribeToSystemDisplayMode()` 统一 system-mode listener，`useSystemDisplayMode()` 复用该 helper，并新增 hook/helper 生命周期测试：`apps/main/src/styles/index.css`、`apps/main/src/utils/themeCss.ts`、`apps/main/src/hooks/useSystemDisplayMode.ts`、`apps/main/src/hooks/useSystemDisplayMode.test.tsx`。
- 对 Plan 19 / 20 / 21 做了独立 closure audit 并回填计划文档：三者现均满足 live baseline 与全仓 `pnpm typecheck`、`pnpm build`、`pnpm lint`、`pnpm test` 的关闭条件：`docs/plans/19-icon-system-governance-plan.md`、`docs/plans/20-theme-system-gaps-plan.md`、`docs/plans/21-state-architecture-test-coverage-plan.md`。

- 执行了一轮新的开放式对抗性审查，结果落在 `docs/analysis/2026-05-17-adversarial-review/`。
- 移除了 AMIS `@fn:` / `compileFunction` 动态代码执行路径，改为显式报错并要求使用 `@action:` 绑定预注册动作：`packages/amis-core/src/page/action.ts`、`packages/amis-core/src/types.ts`、`apps/main/src/amis/adapter.ts`、`apps/main/src/amis/testSchema.ts`。
- 新增应用内统一确认服务与宿主对话框，AMIS / Flux / AppShell / 多个主应用页面不再依赖 `window.confirm`：`apps/main/src/services/confirm.ts`、`apps/main/src/components/common/ConfirmDialogHost.tsx`。
- 修复了多处运行时问题：AMIS init 失败后不可重试、fullscreen 状态在失败时不同步、flow editor 保存失败静默、tabStore 关闭不存在标签页时错误跳转、AMIS logout 未清理 managed tokens。
- 为 `ErrorBoundary` 增加了手动重试入口，避免一次性渲染错误永久锁死当前边界：`packages/core/src/components/ErrorBoundary.tsx`。
- 修复了 plugin bridge `BoundStore` 的 selector 契约断裂：host 侧现在通过 `createBoundStore()` 真正支持 `bridge.stores.authStore(selector)` 形式，并补了回归测试：`apps/main/src/plugins/boundStore.ts`、`apps/main/src/plugins/boundStore.test.ts`、`packages/plugin-bridge/src/index.test.ts`。
- 更新设计说明，明确当前 baseline 已移除 AMIS 动态代码执行能力：`docs/design/backend-integration.md`。
- 收口 Plan 07：`extension-demo` 登录示例改为显式 demo-only mock session 生成，不再依赖硬编码密码或固定 token；新增 focused test 覆盖 session contract，并同步更新 extension locale 文案与 `plugin-dev-guide` 的 demo 基线说明：`examples/extension-demo/src/pages/ExtensionLoginPage.tsx`、`examples/extension-demo/src/pages/demoSession.ts`、`examples/extension-demo/src/pages/ExtensionLoginPage.test.ts`、`examples/extension-demo/src/index.test.ts`、`examples/extension-demo/public/locales/en-US/translation.json`、`examples/extension-demo/public/locales/fr-FR/translation.json`、`docs/examples/plugin-dev-guide.md`、`docs/plans/07-demo-reference-surface-plan.md`。
- 对 Plan 07 完成总体验证与 closure 回填：focused demo tests、全仓 `pnpm typecheck`、`pnpm build`、`pnpm lint`、`pnpm test` 全部通过，demo/reference surface 现达到当前仓库内的最低可信基线。
- 推进 Plan 22 的第一批 retained finding：host `App` 不再因 `location.pathname` 变化重建 plugin bridge，而是通过 runtime getter 暴露最新 path；`plugin-bridge` 的 React 订阅现统一走单一 fan-out 路径，并补了 bridge 替换、通知引用更新、route change 不重建的 focused tests：`apps/main/src/App.tsx`、`apps/main/src/App.test.tsx`、`packages/plugin-bridge/src/bridge.ts`、`packages/plugin-bridge/src/index.ts`、`packages/plugin-bridge/src/index.test.ts`。
- 同步加固 remote plugin 运行时故障隔离：`loadRemoteComponent()` 现在拒绝非法 `default export`，`PluginSlot` 增加受控加载超时和明确错误态，并补了 success / invalid export / timeout focused tests；相关插件设计文档与构建指南已同步更新：`packages/core/src/utils/systemjs.ts`、`packages/core/src/utils/systemjs.test.ts`、`packages/core/src/components/PluginSlot.tsx`、`packages/core/src/components/PluginSlot.test.tsx`、`docs/design/plugin-system.md`、`docs/references/build-guide.md`、`docs/plans/22-current-deep-audit-remediation-plan.md`。
- 继续推进 Plan 22 Workstream 1：`RouteRenderer` 的 iframe 路径已补齐固定 `sandbox="allow-scripts allow-same-origin allow-forms allow-popups"` allowlist，并新增 focused route test 证明该 contract 落地；同轮还将 `App.tsx` 的 bridge path / navigate ref 同步移动到 `useLayoutEffect`，消除当前 React lint 对 render 期 ref 写入的违规：`apps/main/src/router/RouteRenderer.tsx`、`apps/main/src/router/RouteRenderer.test.tsx`、`apps/main/src/App.tsx`。
- 继续推进 Plan 22 Workstream 1：shared HTTP client 现在会在 refresh 成功后的 second-401、refresh 失败、以及无 refresh token 三种场景统一走 unauthorized 终止路径；`amis-core` 的 shared client fallback 也改为复用同一套 401 / refresh / unauthorized 契约，不再保留 amis-local 的“401 直接 logout”分支，并补了 focused regression tests：`packages/shared/src/http/client.ts`、`packages/shared/src/http/client.test.ts`、`packages/amis-core/src/core/ajax.ts`、`packages/amis-core/src/core/ajax.test.ts`、`packages/amis-core/src/types.ts`、`docs/design/backend-integration.md`、`docs/plans/22-current-deep-audit-remediation-plan.md`。
- 为通过当前 workspace lint，同时收口同一 Workstream 的 runtime hygiene：`PluginSlot` 的 cache-busting request URL 生成移入 effect，避免 render 期调用 `Date.now()` 触发 React purity 规则；相关 focused test 与全仓 `pnpm typecheck`、`pnpm build`、`pnpm lint`、`pnpm test` 已重新通过：`packages/core/src/components/PluginSlot.tsx`、`packages/core/src/components/PluginSlot.test.tsx`。
- 继续推进 Plan 22 Workstream 3/4：`homePath` 现以 `apps/main/src/config/homePath.ts` 为唯一 runtime canonical source，由 `apps/main/src/config/systemMenus.ts` 在菜单 merge 后写入，`tabStore`、`closeAllTabs()` 与 `403/404/500` 返回首页按钮统一读取该值；同时 `AppRoutes` 保留“菜单过滤 + 路由 guard”双层权限模型，并补了 focused route proof 与权限 E2E：`apps/main/src/config/homePath.ts`、`apps/main/src/config/systemMenus.ts`、`apps/main/src/store/tabStore.ts`、`apps/main/src/store/tabStore.test.ts`、`apps/main/src/hooks/useTabManagement.ts`、`apps/main/src/router/AppRoutes.tsx`、`apps/main/src/router/AppRoutes.test.tsx`、`apps/main/src/pages/errors/403.tsx`、`apps/main/src/pages/errors/404.tsx`、`apps/main/src/pages/errors/500.tsx`、`tests/e2e/permission.spec.ts`、`docs/design/extension-system.md`。
- 同轮修复并记录了 `useShellConfig()` 登录页白屏的真实根因：`packages/extension-host/src/runtime.ts` 的 `getShellRuntimeConfig()` 改为返回稳定 snapshot，满足 `useSyncExternalStore` 契约，不再因每次读取都 clone 新对象触发 React `#185`；`tests/e2e/login.spec.ts` 随后恢复通过。
- Plan 22 的 retained UI consistency P1 也已收口：`settings/theme` 的主题卡片与 `plugins/management` 的 select chips 已统一切到 `Toggle` / `Button` 体系，并通过 focused UI tests 复验：`apps/main/src/pages/settings/theme/index.tsx`、`apps/main/src/pages/settings/theme/index.test.tsx`、`apps/main/src/pages/plugins/management/index.tsx`、`apps/main/src/pages/plugins/management/index.test.tsx`。
- 追加 closure 证据：`pnpm --filter @nop-chaos/plugin-bridge test`、`pnpm --filter @nop-chaos/plugin-demo build`、`pnpm --filter @nop-chaos/main exec vitest run src/store/tabStore.test.ts src/pages/settings/theme/index.test.tsx src/pages/plugins/management/index.test.tsx src/router/AppRoutes.test.tsx`、`pnpm test:e2e -- tests/e2e/permission.spec.ts` 均已通过；Plan 22 当前仅待独立 closure audit 结论回填。
- Plan 22 的独立 closure audit 首轮先抓到一个真实 contract gap：`examples/plugin-demo/package.json` 之前遗漏了 `react` / `react-dom` peerDependencies，和 host `apps/main/src/plugins/sharedModules.ts` 的 shared-runtime 前提不一致；修复该 gap 并同步 `docs/examples/plugin-dev-guide.md` 后，复审 task `ses_1c9646590ffeN3qmgHfJcBWX9J` 判定 Plan 22 可正式关闭。
