# Development Log — 2026-07-13

### 2026-07-13 (evening)

- 将 inline TypeScript test schemas 转换为 `public/data/` JSON 文件：`apps/main/public/data/amis-preview.json`、`apps/main/public/data/flux-demo.json`
- 移除 `apps/main/src/amis/testSchema.ts` 和 `apps/main/src/flux/testSchema.ts` 及它们的 dynamic import 加载路径
- `apps/main/src/amis/providers.ts` 和 `apps/main/src/flux/providers.ts` 不再拦截 `mock://preview` / `mock://flux-demo`，统一走 `fetchAmisPage` / `fetchFluxPage` → `loadSchemaAsset` 的 HTTP 路径
- `menu-config.json` 的 schemaPath 更新为 `/data/amis-preview.json` 和 `/data/flux-demo.json`
- 更新所有 e2e 测试 mock 数据引用（`auth.ts`、`lazy-loading.spec.ts`、`i18n-persistence.spec.ts`）以匹配新的 JSON 路径
- 设计权衡：JSON 文件无法携带 TypeScript 类型（如 `as const` / `satisfies` / `FluxSchema`），但运行时类型断言在 `fetchFluxPage` 的 `return value as FluxSchema` 处保持安全；AMIS schema 是无类型的普通对象，无影响

### 2026-07-13 (afternoon)

- Flux 页面类型检测从 `meta.pageType: 'flux'` 改为 `component: 'FLUX'`，与 AMIS (`component: 'AMIS'`) 保持一致：`apps/main/src/services/menuMapper.ts:69` `toPageType()`
- 同时将 AMIS/IFRAME 检测统一改用 `normalizeComponentKey()`（`componentKey`），消除此前部分用 `resource.component` 原始值、部分用 `componentKey` 的不一致
- 更新 `menuMapper.test.ts` 中两条 flux 测试用例：用 `component: 'FLUX'` 替代 `meta: { pageType: 'flux' }`
- 更新 e2e 测试 mock 数据（`lazy-loading.spec.ts`、`i18n-persistence.spec.ts`）：将 `fluxEnabledSiteMapResponse` 中的 `component: 'flux-demo'` + `meta.pageType: 'flux'` 改为 `component: 'FLUX'` + `meta.schemaPath`
- `pnpm dev:mock` 的 `menu-config.json` 已包含 AMIS（`/amis/preview`）和 Flux（`/flux-demo`）页面，通过 inline testSchema 提供测试数据
- 验证：`pnpm typecheck` 27/27、`pnpm build` 14/14、`pnpm test` 346/346

### 2026-07-13

- 兼容旧版 nop-chaos 的 amis 配置 JSON：支持 `type: "vue-form-item"` + `vueComponent: "icon-picker"`。旧版是 Vue 宿主 + veaury 桥接，新版纯 React 宿主无 Vue，改为通用「name→React 组件」注册表 + amis `FormItem` 渲染器。
- 新增通用桥接（`@nop-chaos/amis-react`）：`packages/amis-react/src/vue-form-item/`（`registry.ts` 注册表、`VueFormItemRenderer.tsx` 渲染器、`register.ts` 调 `FormItem({type:'vue-form-item',autoVar:false})` 注册）。渲染器移植旧 `VueControl` 语义：读 `props.vueComponent` 查表、props 变量解析（`${...}` 走 `resolveVariableAndFilter`）、`value`/`onValueChange`、`change` 事件可阻止。
- 新增 React `IconPicker`（`apps/main/src/amis/components/IconPicker.tsx`）：用 `@nop-chaos/ui` 的 Popover/Input/ScrollArea + `@nop-chaos/core` 的 `<LowCodeIcon>`（已有的按名渲染控件，lucide+FA）。候选图标取 `lucide-react` 全表 kebab 名，带搜索 + 分批显示。接线在 `apps/main/src/amis/vueFormItemRegister.ts`，由 `ensureAmisRuntime()` 触发。
- 关键决策：图标集改用 lucide（旧 `ant-design:*` 值无法渲染，按名走 `<LowCodeIcon>` 回退）；不另造 `<Icon>` 组件，直接复用 `@nop-chaos/core` 的 `LowCodeIcon`（避免与 `iconMap.tsx` 重复）。曾在 `flux-lib/ui` 建过 `<Icon>`，发现重复 + `rebuild:flux` 会清除，已删除还原。
- ant-design→lucide 映射：在 `packages/core/src/utils/iconMap.tsx` 加内存 map `ANT_DESIGN_LUCIDE_MAP`（约 200 条，已用项目内 lucide-react 逐一校验目标存在）+ `resolveLucideName`。解析链：检测到 `ant-design:` 前缀时剥前缀 + 去 `-outlined/-filled/-twotone` 后缀，查 map，再走 lucide；配合 97 个直接同名命中，覆盖约 300/455（其余多为品牌图标，回退 FontAwesome）。只对带命名空间前缀的名字生效，不影响普通图标名既有行为。
- 构建分层修复：`vueFormItemRegister.ts`/`IconPicker.tsx` 需归入 `host-amis-bootstrap`（bridge 层），否则落到默认 `host-runtime`（host 层）会触发 host→bridge 层级违规。更新 `scripts/main-bundle-utils.mjs:572` 的 `includesAny` 列表。
- `type: "vue-renderer"` + `vueComponent: "icon"` 兼容（表格单元图标渲染）：新增 `@nop-chaos/amis-react` 中 `packages/amis-react/src/vue-renderer/`（`registry.ts` 注册表、`VueRenderer.tsx` 非表单 amis `Renderer` 组件，支持 `$xxx`/`${xxx}` 变量解析、`register.ts` 调 `Renderer({type:'vue-renderer',autoVar:false})` 注册）。宿主新增 `apps/main/src/amis/components/IconCellRenderer.tsx`（用 `<LowCodeIcon>` 渲染 `props.icon`），接线在 `apps/main/src/amis/vueRendererRegister.ts`，由 `ensureAmisRuntime()` 触发。同样纳入 `host-amis-bootstrap` 分层白名单。
- 验证：`pnpm typecheck`/`build`/`lint`/`test` 全绿（apps/main 343 + amis-react 28 测试通过，build 层级违规 0）。
- Bug fix：后端 `SiteMapApi__getSiteMap` 对无值字段返回 `null`（`icon: null`, `displayName: null`, `hidden: null`），mapper `toIcon()`/`title`/`hideInMenu` 原样透传给 `assertOptionalString`/`assertOptionalBoolean`，后者只接受 `undefined`，抛异常 `"Invalid menu config: 'items[0].icon' must be a string"`。React Query 静默吃掉异常，导致菜单为空且无错误提示。
  - 修复：三处 `?? undefined` 归一化（`apps/main/src/services/menuMapper.ts`）。
  - 回归测试：`menuMapper.test.ts` 加 3 条 null→undefined 用例（icon/displayName/hidden）。
  - Bug note：`docs/bugs/45-menu-mapper-null-silently-kills-validation.md`。
- `@tanstack/react-query` v5+ 默认不再对 `queryFn` 错误输出 console.error（v3/v4 曾有 `defaultLogger`），所有 query 异常被静默吞掉。`fetchMenuConfig` 加了 `try/catch` + `console.error` 确保菜单查询异常有迹可循。其余 queryFn 若也对静默敏感应同样包裹。
