# Development Log — 2026-05-14

### 2026-05-14

- Fixed the login form icon overlap by updating `apps/main/src/pages/auth/login/index.tsx` to keep explicit left icon spacing together with right padding on the shared `Input` component.
- Fixed mock-mode English menu labels falling back to translation keys by awaiting `initializeI18n()` in `apps/main/src/main.tsx` before the app renders; added initialization coverage in `apps/main/src/config/i18n/index.test.ts`.
- Fixed the deeper runtime i18n regression where shell interactions could make menu and login labels fall back to translation keys: `packages/core/package.json` now declares `i18next` as a peer so `TabsBar`/`PluginSlot` share the same i18n singleton as `apps/main`; added `tests/e2e/i18n-persistence.spec.ts` to cover English persistence through sidebar toggle and logout.
- Audited workspace i18n singleton boundaries after the fix. Current `react-i18next` consumers are limited to `apps/main`, `packages/core`, and `examples/plugin-demo`; no additional packages were missing `i18next` after the `packages/core` peer fix.
- Added `docs/testing/` to mirror the manual issue tracking style used in `nop-chaos-flux`, including `docs/testing/index.md` and `docs/testing/00-testing-issue-writing-guide.md`.
- Recorded two manually diagnosed issues in `docs/testing/2026/05-14.md`: login input icon/text overlap and mock-mode English menu labels rendering as `menuKey`.
- Fixed the remaining Flux-triggered i18n regression by wrapping the host root with `I18nextProvider` in `apps/main/src/main.tsx`, so entering `Flux Demo` no longer lets Flux overwrite the host app's active translation instance.
- Updated `tests/e2e/support/auth.ts` to preserve the stored language across login test setup resets and extended `tests/e2e/i18n-persistence.spec.ts` with a dedicated `Flux Demo -> host route -> logout` regression path.
- Recorded the Flux-specific diagnosis in `docs/testing/2026/05-14.md` and `docs/bugs/29-flux-react-i18next-default-instance-pollution.md`.
- Fixed the AMIS visual drift that appeared after opening `Amis Preview`: `apps/main/src/amis/init.ts` now loads `amis/lib/themes/cxd.css` before `apps/main/src/styles/amis-theme-bridge.css`, and the bridge stylesheet reasserts the host `html/body` baseline so AMIS global reset rules do not visibly shift the shell after first load.
- Added AMIS style isolation coverage in `tests/e2e/lazy-loading.spec.ts` by comparing computed document baseline styles before and after opening `Amis Preview`.
- Recorded the diagnosis in `docs/bugs/30-amis-global-css-baseline-drift.md`.

### 2026-05-14

- Integrated the packaged `@nop-chaos/flux` tarball into `apps/main` and switched the mock Flux demo route to real schema-driven rendering via `apps/main/src/flux/FluxRouteRenderer.tsx`, `apps/main/src/flux/providers.ts`, and `apps/main/src/flux/testSchema.ts`.
- Updated main bundle runtime policy so `i18next` and `react-i18next` are validated as `bundler-override` dependencies instead of exact same-path singletons; current policy and build gate live in `scripts/main-bundle-utils.mjs`, `scripts/check-main-external-runtime-deps.mjs`, and `docs/design/main-bundle-dependency-strategy.md`.
- Narrowed public bridge/runtime i18n typing away from concrete `i18next` exports in `packages/plugin-bridge/src/index.ts` and `packages/amis-core/src/types.ts`, then patched host consumers (`apps/main/src/App.tsx`, `examples/extension-demo/src/pages/ExtensionLoginPage.tsx`) to tolerate optional bridge capabilities.
- Restored Flux/UI compatibility required by the packaged Flux bundle by exporting `setI18nGetter` from `flux-lib/ui/src/index.ts` and teaching `flux-lib/ui/src/lib/i18n.ts` to use a host-provided translator when present.
- Repacked and reinstalled `@nop-chaos/flux` so the consumed tarball now matches the current bundle output: `i18next`, `react-i18next`, and `recharts` stay external in the published dist and the host no longer needs a `require('react')` runtime shim. `apps/main/src/flux/init.ts` now only guarantees Flux styles are loaded before `apps/main/src/flux/FluxRouteRenderer.tsx` is imported.
- Updated `tests/e2e/lazy-loading.spec.ts` so the Flux assertion checks real schema-rendered content instead of assuming the sidebar menu label is duplicated inside the rendered page body.
- Validation: `pnpm typecheck`, `pnpm lint`, and `pnpm build` now pass in `nop-chaos-next` after the Flux tarball update and host compatibility fixes.
- Validation: Flux demo now renders successfully in browser and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4193 pnpm test:e2e -- tests/e2e/lazy-loading.spec.ts` passes end-to-end.

### 2026-05-14 (afternoon)

- Extracted the AMIS reset+normalize CSS from upstream `amis-react19/packages/amis-ui/scss/base/{_reset,_normalize}.scss` into `apps/main/src/styles/amis-reset.css` (pure CSS, replacing SCSS variables with CSS custom properties).
- Added `@import './amis-reset.css'` to `apps/main/src/styles/index.css` so the global baseline is established at app startup instead of on first AMIS page visit.
- Removed `!important` baseline overrides from `apps/main/src/styles/amis-theme-bridge.css` since the reset is now statically loaded before `cxd.css`.
- `cxd.css` component styles remain lazy-loaded via `apps/main/src/amis/init.ts` — only the ~400-line reset portion is pre-loaded.
- Updated `docs/bugs/30-amis-global-css-baseline-drift.md` with the Phase 2 fix details.
- Validation: `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm test` all pass.

### 2026-05-14 (evening)

- Discovered `_typography.scss` in `amis-react19/packages/amis-ui/scss/base/` also applies global rules (`body { font-family, font-size, line-height, color, background }`, `html { font-size: 16px }`, `html,body { width/height: 100% }`, etc.) that were NOT covered by the reset/normalize extraction.
- Appended the typography global rules to `apps/main/src/styles/amis-reset.css` so AMIS body font-family (`var(--fontFamilyBase)`) is established at startup instead of on first AMIS page visit.
- Created `tests/e2e/amis-css-isolation.spec.ts` with two dedicated tests: sidebar style stability (151 elements) and global document style stability, both verifying zero non-route-active changes after AMIS CSS loads.
- Updated `docs/bugs/30-amis-global-css-baseline-drift.md` with Phase 3 fix details.
- Validation: all e2e tests, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm test` pass.
