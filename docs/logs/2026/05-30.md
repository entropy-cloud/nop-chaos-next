# Development Log — 2026-05-30

### 2026-05-30

- Fixed auth session restore so refresh-time bootstrap passes the persisted access token explicitly to `fetchCurrentUser`, matching the backend `LoginApi__getLoginUserInfo` contract that reads `request.accessToken`: `apps/main/src/hooks/useAuth.ts`.
- Added a regression test to verify restore-time current-user fetch uses the managed access token instead of omitting it: `apps/main/src/hooks/useAuth.test.ts`.
- Verification status: `pnpm --filter @nop-chaos/main exec vitest run src/hooks/useAuth.test.ts`, `pnpm typecheck`, `pnpm build`, and `pnpm lint` all passed.
