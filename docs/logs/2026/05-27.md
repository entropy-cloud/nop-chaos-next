# 2026-05-27

### 2026-05-27

- Added a host app favicon to stop the browser's default `/favicon.ico` request from returning 404:
  - updated `apps/main/index.html` to declare `rel="icon"` for `/favicon.svg`
  - added `apps/main/public/favicon.svg` as a lightweight static asset served by Vite
- Verification for the favicon change:
  - `pnpm typecheck` — pending
  - `pnpm build` — pending
  - `pnpm lint` — pending

- Fixed the duplicate `flow-editor` route registration warning in `apps/main/src/router/AppRoutes.tsx`:
  - changed `dedupeRoutesByPath()` to keep the more specific route when parent and child menu items share the same normalized path
  - this preserves leaf route registrations such as `flow-editor-list` instead of always keeping the first flattened parent container
  - added a regression test in `apps/main/src/router/AppRoutes.test.tsx` covering the shared-path parent/child menu shape
- Reduced Recharts first-render sizing noise on the dashboard:
  - added a shared `chartInitialDimension` in `apps/main/src/pages/dashboard/components/chartUtils.ts`
  - passed that initial size into the dashboard `ResponsiveContainer` instances so charts do not mount from an undefined `-1` size during early layout
- Verification after the routing/chart fixes:
  - `pnpm --filter @nop-chaos/main exec vitest run src/router/AppRoutes.test.tsx` — passed
  - `pnpm typecheck` — passed
  - `pnpm build` — passed
  - `pnpm lint` — passed (same pre-existing warnings in `packages/extension-host/src/index.test.ts`)
  - focused Playwright login repro against an already-running `http://127.0.0.1:4175` server did not complete because that external server was not serving the expected default login form for `tests/e2e/support/auth.ts`

- Reduced the React `key`-prop spread warning from Base UI item render chains:
  - updated `flux-lib/ui/src/components/ui/select.tsx` so `SelectItem` uses an explicit `render` callback that pulls `key` out of Base UI's generated props before rendering the backing `<div>`
  - updated `flux-lib/ui/src/components/ui/combobox.tsx` with the same `key` extraction pattern for `ComboboxItem`
  - focused browser reproduction on the login language selector no longer showed the prior `key`-spread warning after this wrapper-side fix
- Re-verified after the wrapper changes:
  - `pnpm typecheck` — passed
  - `pnpm build` — passed
  - `pnpm lint` — passed (same pre-existing warnings in `packages/extension-host/src/index.test.ts`)
  - `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 PLAYWRIGHT_LIVE_MENU_URL=http://127.0.0.1:4173 pnpm exec playwright test tests/e2e/tmp-react-306.spec.ts --project chromium` — passed
- Continued tracing the `outdated JSX transform` warning:
  - it was not reproduced in the latest focused login/dashboard console captures after the wrapper fix
  - current evidence still points away from repo-authored TSX compilation and toward a dependency-side runtime warning when it does appear

- Verified the temporary React 306 live-menu repro against non-mock / Java-backend mode:
  - updated `tests/e2e/tmp-react-306.spec.ts` to use real non-mock credentials defaults (`admin` / `123456`) with env overrides via `PLAYWRIGHT_LIVE_USERNAME` and `PLAYWRIGHT_LIVE_PASSWORD`
  - replaced the stale hard-coded `用户` menu click with stable shell assertions that match the current host app after Java-backed login
  - verified with `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 PLAYWRIGHT_LIVE_MENU_URL=http://127.0.0.1:4173 pnpm exec playwright test tests/e2e/tmp-react-306.spec.ts --project chromium`
- Investigated the two React console warnings reported during non-mock runs:
  - the `key`-prop spread warning traces into installed `@base-ui/react` internals (`utils/useRenderElement.js`, `select/item/SelectItem.js`) rather than the app's ordinary business JSX
  - the outdated JSX transform warning was not reproduced from repo source files during this pass and appears more likely to come from an upstream dependency bundle than from current workspace TS/TSX compilation settings

- Prevented the default Playwright suite from depending on an external live Java menu instance:
  - updated `tests/e2e/tmp-react-306.spec.ts` to run only when `PLAYWRIGHT_LIVE_MENU_URL` is explicitly provided
  - replaced the hard-coded `http://127.0.0.1:4174` target with the environment-provided live menu base URL
- Verification after the E2E guard change:
  - `pnpm test` — passed (`303/303`)
  - `pnpm test:e2e` — passed (`60 passed`, `2 skipped`)

- Compared the repo docs governance shape against `C:\can\nop\attractor-guided-engineering-template` and pulled in the most useful lightweight pieces without copying the full template structure:
  - added `docs/source-of-truth-and-precedence.md` to define which artifact answers which question
  - added `docs/skills/README.md` to explain the role of reusable methods and tool-usage docs
  - upgraded `docs/index.md` from a directory list into a top-level docs router with task-oriented entry points and explicit directory roles
  - imported a docs-routing audit skill as `docs/skills/index-routing-audit-prompt.md` so index/readme routing quality can be checked explicitly in future sessions
  - renamed `docs/design/package-exports-strategy.md` to `docs/design/package-exports-spec.md` to keep current-rule docs under `design/` on consistent `spec` naming
  - ran a docs-routing audit and fixed missing top-level routes for `design/icon-system.md`, `design/submit-concurrency-guard.md`, and bug notes `30`, `31`, `35`
  - clarified `docs/skills/README.md` as the complete sub-index for skill files in `docs/skills/` except `README.md` itself
- Fixed post-split test regressions caused by the new route wrapper entrypoints:
  - changed `apps/main/src/router/AmisRouteEntry.tsx` and `FluxRouteEntry.tsx` to export ordinary wrapper components that render inner lazy route components, instead of exporting lazy objects directly
  - this restored AMIS preview rendering and Flux route rendering in Playwright after the chunk-splitting refactor
- Updated lazy-loading E2E expectations to match the stricter current behavior:
  - removed the stale assumption that dashboard bootstrap must eagerly load AMIS assets
  - relaxed the aggregate chunk-family smoke assertion so route chunk presence is validated by the dedicated AMIS/Flux lazy-loading tests instead of the dashboard baseline page
- Removed an obsolete eslint suppression in `flux-lib/ui/src/components/ui/label.tsx` so `pnpm lint` is green again
- Full verification after the fixes:
  - `pnpm typecheck` — passed
  - `pnpm build` — passed
  - `pnpm lint` — passed
  - `pnpm test` — passed (`303/303`)
  - `pnpm test:e2e` — passed (`60 passed`, `1 skipped`)
- Clarified the `docs/design/` contract further:
  - `docs/design/index.md` now serves as the design-directory owner guide
  - dependency owner doc renamed to `docs/design/main-bundle-dependency-spec.md` to reflect that design docs here are current specs, not plans
- Cleaned `docs/index.md` bug listing duplicates for entries `39` and `40`

- Reorganized dependency/bundle documentation by responsibility instead of keeping overlapping narrative copies in multiple places:
  - `docs/skills/main-bundle-dependency-governance.md` now holds the generic analysis method only
  - `docs/design/main-bundle-dependency-spec.md` now holds the current `apps/main` project-specific dependency spec only
  - measurement/tool usage guidance now lives directly in `docs/skills/main-bundle-dependency-governance.md`
- Updated `docs/index.md` and `docs/references/build-guide.md` to reflect the new split:
  - `skills/` = reusable method
  - `design/` = current repo target state and current active spec
  - core analysis tooling guidance is documented alongside the reusable method instead of a separate strategy reference
- Added `docs/design/index.md` to define the responsibility of `design/` explicitly:
  - design docs describe the latest target/current active state, not execution plans or historical change logs
  - when multiple technical options existed, the chosen option and rationale must be recorded
- Updated dependency-related bug notes to clarify that they remain issue records, not the primary source of dependency governance rules:
  - `docs/bugs/24-main-bundle-chunk-boundary-regression.md`
  - `docs/bugs/25-amis-file-dependency-runtime-split.md`

- Split route-level Flux and AMIS lazy loaders into dedicated wrapper entries:
  - `apps/main/src/router/FluxRouteEntry.tsx`
  - `apps/main/src/router/AmisRouteEntry.tsx`
- Updated `apps/main/vite.config.ts` chunk grouping so:
  - external `amis-core` now emits as `vendor-amis-core`
  - Flux route wrapper emits its own entry chunk instead of sharing the RouteRenderer import site directly
- Verified the build graph improved materially after the split:
  - `vendor-amis-core-P0mHQKOX.js` now exists as a separate chunk
  - `vendor-misc` no longer directly imports `vendor-amis-formula`
  - temporary graph check in `C:\Users\a758371\AppData\Local\Temp\opencode\analyze-flux-amis-graph-result.json` now reports `fluxToAmis: null`
  - `sharedImporters` is now empty for the Flux runtime set vs AMIS chunk set
- Residual coupling still exists through host preloads:
  - `amisToFlux` still resolves through `host-runtime -> FluxRouteEntry`
  - `FluxRouteRenderer` still imports `host-runtime` because Flux host helpers currently live under `apps/main/src/flux/*` and still depend on shared host services
- Verified after the second chunk-splitting pass:
  - `pnpm --filter @nop-chaos/main build` — passed
  - `pnpm typecheck` — 21/21 tasks passed
  - `pnpm lint` — passed, with the same pre-existing warnings in `packages/extension-host/src/index.test.ts`
- Finalized host-side isolation by splitting shared app state/runtime inputs out of `host-runtime`:
  - `apps/main/vite.config.ts` now emits `src/config/*`, `src/services/*`, and `src/store/*` as `host-app-state`
  - `apps/main/src/flux/*` now emits as `host-flux-runtime`
- Verified the build graph now fully isolates Flux from AMIS in both directions:
  - temporary graph check in `C:\Users\a758371\AppData\Local\Temp\opencode\analyze-flux-amis-graph-result.json` reports:
    - `fluxToAmis: null`
    - `amisToFlux: null`
    - `sharedImporters: []`
- Verified after the final host-side split:
  - `pnpm --filter @nop-chaos/main build` — passed
  - `pnpm typecheck` — 21/21 tasks passed
  - `pnpm lint` — passed, with the same pre-existing warnings in `packages/extension-host/src/index.test.ts`
- Consolidated reusable main-bundle chunking rules into shared tooling instead of leaving them only in `apps/main/vite.config.ts`:
  - added `getMainChunkGroupName()` to `scripts/main-bundle-utils.mjs`
  - added matching declaration coverage in `scripts/main-bundle-utils.d.mts`
  - simplified `apps/main/vite.config.ts` to delegate main chunk naming to the shared helper
- Extended `docs/design/main-bundle-dependency-strategy.md` with:
  - which `apps/main` chunking heuristics are now considered stable shared rules
  - when optimization经验 should be recorded in docs vs encoded in tool functions
- Updated `docs/index.md` reading guide to surface bundle dependency optimization docs explicitly
- Re-verified after the tooling/documentation consolidation:
  - `pnpm --filter @nop-chaos/main build` — passed
  - `pnpm typecheck` — 21/21 tasks passed
  - `pnpm lint` — passed, with the same pre-existing warnings in `packages/extension-host/src/index.test.ts`

- Tightened `apps/main/vite.config.ts` AMIS manual chunk matching so targeted renderer splits also match installed package paths (`amis/esm`, `amis/lib`) instead of only source workspace paths.
- Resulting build now emits stable dedicated chunks for targeted AMIS renderer families:
  - `vendor-amis-chart`
  - `vendor-amis-office-viewer`
  - `vendor-amis-pdf-viewer`
  - `vendor-amis-rich-text`
  - `vendor-amis-json`
  - `vendor-amis-renderer`
- Verified the host bootstrap dependency surface shrank:
  - `host-amis-bootstrap` now points to `vendor-amis-bridge` instead of directly carrying broad AMIS vendor dependencies.
- Verified output after the chunking change:
  - `pnpm typecheck` — 21/21 tasks passed
  - `pnpm build` — 12/12 tasks passed
  - `pnpm lint` — passed, with the same pre-existing warnings in `packages/extension-host/src/index.test.ts`

## extension manifest + multi-extension discovery

### Changes
- Added `ExtensionManifest` type to `packages/shared/src/types/extension.ts`
  - Fields: `id`, `name`, `version?`, `description?`, `entry`, `styleAssets?`
- Added `extensionManifestPlugin` Vite plugin in `examples/extension-demo/vite.config.ts`
  - Scans build output for entry chunk + CSS assets
  - Writes `extension.json` to dist after build
- Updated `apps/main/src/extensions/config.ts`
  - Added logging when runtime `window.__NOP_EXTENSIONS__` sources are found (shows count and IDs)
- Exported `ExtensionManifest` from `packages/shared/src/index.ts`
- Updated `docs/design/extension-system.md`
  - Section 7.4: Extension Manifest (`extension.json`) — format, deployment structure, server-side integration contract, host discovery priority, manifest generation

### Generated manifest example
```json
{
  "id": "example-extension-demo",
  "name": "Harbor Operations Suite",
  "version": "0.0.1",
  "entry": "./assets/index-COw24fxy.js",
  "styleAssets": ["./assets/index-3Z9nCm1K.css"]
}
```

### Verification
- `pnpm typecheck` — 21/21 tasks passed
- `pnpm --filter @nop-chaos/main build` — green, extension synced
- `pnpm test` — 303/303 tests passed
- `pnpm lint` — clean (only pre-existing warnings in extension-host test file)
- Updated `scripts/sync-site.sh` to use deterministic gzip output via `gzip -n -k -9`
- Verified sync idempotence for unchanged `apps/main/dist`: running `sync-site.sh` twice produced identical file hashes in `nop-web-site/src/main/resources/META-INF/resources`
- Added `scripts/repack-upstreams-and-refresh.sh` to repack `amis-react19` and `nop-chaos-flux`, sync Flux sources, and force-refresh `file:` dependencies in `nop-chaos-next`
- Split site workflows:
  - `pnpm site` = repack upstreams + refresh deps + build current repo + sync site
  - `pnpm site:local` = build current repo + sync site only
- Confirmed the synced site output returned exactly to the recorded baseline after reverting the AMIS experiment (`site-current-full-site.txt` matched `site-baseline-full-site.txt` with an empty diff)
- Narrowed `scripts/repack-upstreams-and-refresh.sh` to:
  - refresh all affected AMIS/Flux `file:` tarball lock entries explicitly
  - run importer-level `pnpm add ... --lockfile-only` so the pre-refresh phase only updates lock state
  - restore `package.json` dependency specs back to repo-standard `file:../../../...` format after lock refresh
- Re-verified the refreshed workflow after script tightening:
  - `pnpm refresh:upstreams` — passed
  - `pnpm typecheck` — 21/21 tasks passed
  - `pnpm build` — 12/12 tasks passed
  - `pnpm lint` — passed, with the same pre-existing warnings in `packages/extension-host/src/index.test.ts`

### Previous commit
- `484ed11` fix(build): use relative paths and remove Google Fonts
