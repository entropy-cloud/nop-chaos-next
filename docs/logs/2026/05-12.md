# Development Log — 2026-05-12

### 2026-05-12

- Executed Plan 04: Workspace Tooling and Docs Alignment
- **Phase 1 - Turborepo**: Installed `turbo@2.9.6`, created `turbo.json` with build/typecheck/test/lint task topology, updated root scripts to use `turbo run`. Verified FULL TURBO cache hit on second build. Fixed amis package relative paths in `apps/main/package.json` (`file:../../amis-react19` → `file:../../../amis-react19`).
- **Phase 2 - ESLint**: Installed `eslint-plugin-react`, `eslint-plugin-unicorn`, `eslint-plugin-i18next`. Added react plugin full rule set, max-lines, ban-ts-comment, no-unused-vars, reportUnusedDisableDirectives, unicorn/filename-case, i18next/no-literal-string. Renamed ~70+ files from PascalCase/camelCase to kebab-case across all packages. Fixed all lint errors — `pnpm lint` passes with 0 errors across 11 packages.
- **Phase 3 - Prettier + lint-staged**: Created `.prettierrc`, `.prettierignore`, `.editorconfig`. Added `lint-staged` config for eslint --fix + prettier --write. Updated `.husky/pre-commit` to `pnpm exec lint-staged`. Formatted entire repo.
- **Phase 4 - knip + jscpd**: Installed `knip` and `jscpd`. Created `knip.json` and `.jscpd.json`. Added `audit:knip` and `check:duplicates` scripts. Baseline report: unused exports in amis-core/core, unused root devDependencies (hoisted for subpackages).
- **Phase 5 - Vitest workspace**: Created `vitest.workspace.ts` and `vitest.shared.ts` for workspace-level test configuration.
- **Phase 6 - Check scripts**: Ported `check-oversized-code-files.mjs`, `verify-no-src-artifacts.mjs`, `clean-src-artifacts.mjs` from flux. Updated `lint` script to include src-artifacts check. Updated `.gitignore` to exclude src artifacts.
- **Phase 7 - Docs restructuring**: Created `docs/design/`, `docs/references/`, `docs/examples/` subdirectories. Moved all flat-numbered docs into categorized locations. Created `docs/index.md` navigation page with complete file routing table.
- **Phase 8 - AGENTS.md**: Updated pageRegistry path reference to `page-registry.tsx`. AGENTS.md already contained all required sections (docs maintenance, verification checklist, bug fix test coverage rule, tooling, formatting).
- **Known issue**: `@nop-chaos/amis-react` build/typecheck fails because the external `amis-react19` library is not locally built (`lib/` directory missing). This is a pre-existing issue outside the scope of this tooling plan.
