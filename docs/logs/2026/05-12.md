# Development Log — 2026-05-12

### 2026-05-12

- Removed the `App.tsx` startup prewarm that imported `./amis/init` whenever the fetched menu tree contained any `amis` page. `AMIS` runtime initialization is now left to the lazy route entry in `apps/main/src/router/RouteRenderer.tsx`, so `Flux`-only sessions no longer pull `AMIS` during app startup.
- Updated `docs/design/main-bundle-dependency-strategy.md` to record the rule that menu capability detection must not eagerly import page-specific runtimes.

### 2026-05-12

- Removed `vite-plugin-compression` from `apps/main/vite.config.ts`, so production builds now only emit the original `.js` assets and no longer write companion `.js.gz` files.
- Re-verified the main app build and repo checks after disabling gzip output.

### 2026-05-12

- Updated `docs/skills/main-bundle-dependency-governance.md` and `docs/design/main-bundle-dependency-strategy.md` to reflect the verified execution state: the chunk model now includes a dedicated `bridge` layer for `host-amis-*`, records `src/components/plugin/*` and `src/lib/*` as proven host-shared boundaries, and adds scenario-based bundle reduction guidance for `sharedModules.ts`, `iconMap.tsx`, flow designer, code editor, dashboard charts, and AMIS renderer capabilities.

### 2026-05-12

- Disabled source map emission for `@nop-chaos/main` production builds by setting `build.sourcemap` to `false` in `apps/main/vite.config.ts`, so the main app no longer writes `.map` assets during build output.
- Re-verified the main app build and repo checks after the sourcemap change.

### 2026-05-12

- Finished the `apps/main` bundle dependency governance loop for the external `file:` AMIS setup: `scripts/ensure-amis-file-deps-built.mjs` now verifies/builds the required external `amis-react19` outputs before the main app build, and local TS resolution was fixed with `scripts/main-bundle-utils.d.mts` plus a root `amis` type path mapping.
- Tightened `apps/main/vite.config.ts` chunk boundaries so app-local shared modules under `src/components/plugin` and `src/lib` are lifted into host runtime chunks instead of leaking through page chunks; this removed the remaining `page -> page` shared-runtime violations.
- Updated `scripts/analyze-main-chunk-graph.mjs` to classify `host-amis-*` bridge/runtime chunks separately from ordinary host chunks so the chunk-layer check matches the actual bridge architecture.
- Verification now passes: `pnpm typecheck`, `pnpm --filter @nop-chaos/main build`, `node scripts/analyze-main-chunk-graph.mjs --check`, and `pnpm lint`.

### 2026-05-12

- Added `docs/design/main-bundle-dependency-strategy.md` and `docs/skills/main-bundle-dependency-governance.md` to formalize `apps/main` chunk layering, package graph analysis, and post-build chunk graph verification.
- Added `scripts/main-bundle-utils.mjs`, `scripts/analyze-main-package-graph.mjs`, `scripts/analyze-main-chunk-graph.mjs`, and `scripts/ensure-amis-file-deps-built.mjs` to turn bundle dependency governance into build-time checks.
- Updated `apps/main/vite.config.ts` to classify workspace packages across `packages/` and `flux-lib/`, route shared package code into stable chunks, and move `src/components/common` into the host shared runtime layer instead of letting page chunks absorb it.
- Added bug note `docs/bugs/24-main-bundle-chunk-boundary-regression.md` for the cross-layer chunk boundary regression involving `flux-lib/ui`, page-shared runtime leakage, and unreliable old chunk analysis.
- Current blocker: `apps/main` still depends on external `file:` AMIS packages under `C:/can/nop/amis-react19`, and that external workspace currently cannot build on this machine because its own toolchain is incomplete (`rimraf` missing in the external repo build environment).

### 2026-05-12

- Added `docs/design/amis-flux-rendering-engine-integration.md` for AMIS/Flux dual-renderer integration.
- Key decision: keep `nop-chaos-flux` independently built; do not add its packages to this repo workspace. Consume Flux through a tarball/private-registry facade package (`@nop-chaos/flux`) rather than syncing Flux internals into `flux-lib/*`.
- Verified current Vite 8 docs: use `build.rolldownOptions`; `build.rollupOptions` is deprecated alias. Documented facade packaging requirements for external/peer deps, CSS exports, declaration bundling, `workspace:*` removal, and tarball-first validation.
- Clarified tarball dependency policy: package archives should not contain `node_modules`; third-party code only enters the archive if bundled into `dist`, and heavy optional dependencies such as CodeMirror should stay out of the core Flux facade.
- Updated `docs/index.md` to include the new rendering engine integration design.

### 2026-05-12

- Executed Plan 04: Workspace Tooling and Docs Alignment
- **Phase 1 - Turborepo**: Installed `turbo@2.9.6`, created `turbo.json` with build/typecheck/test/lint task topology, updated root scripts to use `turbo run`. Verified FULL TURBO cache hit on second build. Fixed amis package relative paths in `apps/main/package.json` (`file:../../amis-react19` → `file:../../../amis-react19`).
- **Phase 2 - ESLint**: Installed `eslint-plugin-react`, `eslint-plugin-unicorn`, `eslint-plugin-i18next`. Added react plugin full rule set, max-lines, ban-ts-comment, no-unused-vars, reportUnusedDisableDirectives, unicorn/filename-case, i18next/no-literal-string. Renamed ~70+ files from PascalCase/camelCase to kebab-case across all packages. Fixed all lint errors — `pnpm lint` passes with 0 errors across 11 packages.
- **Phase 3 - Prettier + lint-staged**: Created `.prettierrc`, `.prettierignore`, `.editorconfig`. Added `lint-staged` config for eslint --fix + prettier --write. Updated `.husky/pre-commit` to `pnpm exec lint-staged`. Formatted entire repo.
- **Phase 4 - knip + jscpd**: Installed `knip` and `jscpd`. Created `knip.json` and `.jscpd.json`. Added `audit:knip` and `check:duplicates` scripts. Baseline report: unused exports in amis-core/core, unused root devDependencies (hoisted for subpackages).
- **Phase 5 - Vitest workspace**: Created `vitest.workspace.ts` and `vitest.shared.ts` for workspace-level test configuration.
- **Phase 6 - Check scripts**: Ported `check-oversized-code-files.mjs`, `verify-no-src-artifacts.mjs`, `clean-src-artifacts.mjs` from flux. Updated `lint` script to include src-artifacts check. Updated `.gitignore` to exclude src artifacts.
- **Phase 7 - Docs restructuring**: Created `docs/design/`, `docs/references/`, `docs/examples/` subdirectories. Moved all flat-numbered docs into categorized locations. Created `docs/index.md` navigation page with complete file routing table.
- **Phase 8 - AGENTS.md**: Updated pageRegistry path reference to `page-registry.tsx`. AGENTS.md already contained all required sections (docs maintenance, verification checklist, bug fix test coverage rule, tooling, formatting).
- **Known issue**: `@nop-chaos/amis-react` build/typecheck fails because the external `amis-react19` library is not locally built (`lib/` directory missing). This is a pre-existing issue outside the scope of this tooling plan.
- **Naming convention correction**: Reverted the incorrect kebab-case file naming introduced in Phase 2. The project uses a split convention:
  - `flux-lib/`: kebab-case (flux framework convention, enforced by ESLint)
  - `apps/` + `packages/` + `examples/`: PascalCase for React components (.tsx), camelCase for utilities/stores/hooks (.ts), enforced by ESLint
  - Renamed ~80 files back to their original names, plus renamed ~35 new files created during Plan 04 to match the application convention
  - Updated `unicorn/filename-case` rule: separate config blocks for `flux-lib/` (kebab-case only) vs. everything else (camelCase + pascalCase)
  - `pnpm lint --force` passes with 0 errors across 11 packages
- **Skills docs migration**: Adapted 11 audit/review/refactoring prompt documents from `nop-chaos-flux/docs/skills/` into `nop-chaos-next/docs/skills/`. Files adapted with varying degrees of modification:
  - **Minimally adapted** (project name + docs path changes only): `open-ended-adversarial-review-prompt.md`, `code-quality-audit-prompt.md`, `ai-tone-and-filler-review.md`, `branch-merge.md`, `deprecated-feature-cleanup.md`, `doc-evaluation.md`, `next-gen-lowcode-attractor-discovery-prompt.md`
  - **Moderately adapted** (docs references + contract types adjusted): `unit-test-logic-and-contract-coverage-audit-prompt.md`, `code-refactor-prompt.md`
  - **Significantly adapted** (flux-specific concepts replaced with next architecture): `react19-best-practices-review.md`, `code-refactor-discovery-prompt.md`
  - **Not ported** (too flux-specific to adapt meaningfully): `deep-audit-prompts.md` (20-dimension manual, most dimensions are renderer/BEM/styling-system specific), `exploratory-contract-testing-prompt.md` (relies on renderer contracts, debugger API, scope-debug), `exploratory-e2e-testing-prompt.md` (relies on playground pages, component lab, debugger API)
- **Deep audit framework extraction**: Created `docs/skills/deep-audit-execution-framework.md` — extracted the project-agnostic execution methodology from `nop-chaos-flux/docs/skills/deep-audit-prompts.md` and designed 12 nop-chaos-next-specific audit dimensions:
  - **Methodology extracted**: Iterative deep-digging model (初审→追加深挖→价值收敛→上限保护), independent review model (维度复核 + 子项分层复核), finding completeness rules (反压缩规则、最低行数、证据片段), parallel scheduling strategy, shared prompt prefix pattern, archival conventions
  - **Dimensions designed for nop-chaos-next**:
    - A. Architecture: 01 Dependency graph & package boundaries, 02 Module responsibility & file boundaries, 03 API surface & contract consistency
    - B. Runtime & state: 04 State ownership (Zustand stores, persistence, React Query cache), 05 Reactive subscription precision, 06 Async patterns & cancellation safety
    - C. Routing & plugins: 07 Routing, permissions & page registry, 08 Plugin bridge & shared modules (SystemJS import maps)
    - D. UI & styling: 09 Theme & CSS custom properties, 10 Component usage & consistency
    - E. Engineering quality: 11 Type safety & contract quality, 12 Test coverage & quality
- **Submit concurrency guard fixes**: Fixed double-submit vulnerabilities across demo pages:
  - `master-detail/[id]/index.tsx`: Save button now uses `saveMutation.mutate()` instead of `mutateAsync()`, added `disabled={saveMutation.isPending}` and pending text
  - `master-detail/index.tsx`: Batch delete and row delete extracted into `deleteMutation` (useMutation), buttons disabled during pending
  - `flow-editor/index.tsx`: All row actions (duplicate/toggle/delete) consolidated into `actionMutation` (useMutation), buttons disabled during pending
  - Added i18n keys: `common.saving` ("保存中..." / "Saving...")
  - Created `docs/design/submit-concurrency-guard.md` with recommended patterns (useMutation + isPending), anti-patterns, and HTTP-layer protection documentation
