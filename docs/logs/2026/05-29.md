# Development Log — 2026-05-29

### 2026-05-29

- Completed verification for backend-owned navigation and user-menu delta customization: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`, and `pnpm test:e2e -- tests/e2e/sidebar-user-menu.spec.ts` passed.
- Ran the sidebar user-menu E2E against a temporary mock preview on port `4185` because an unrelated existing Vite `node.exe` process was already listening on `4175`.
- Isolated `apps/main/src/extensions/bootstrap.test.ts` from the real plugin store module so the bootstrap contract test no longer times out under the full workspace unit test run.

### 2026-05-29

- Changed host navigation semantics so backend menu responses remain the source of truth for the left sidebar; extension `menus` / `overrideMenus` are now deprecated compatibility inputs and no longer merge into navigation.
- Kept settings/help pages routable through route-only menu entries while keeping them out of the sidebar navigation unless the backend provides them.
- Added extension-controlled left-bottom user menu delta customization via `userMenuItems` with `override: 'merge' | 'replace' | 'remove'`; documented the contract in `docs/design/extension-system.md`.
