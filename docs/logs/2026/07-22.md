# Development Log — 2026-07-22

### 2026-07-22

- **AMIS Tabs 表单不渲染 Bug 定位与修复** — 通过系统诊断发现 nop-chaos-next 前端下 NopAuthUser 新增/编辑对话框的 Tab 内容不渲染，但 nop-web-site（生产构建）正常。
  - 使用 `dumpPageStructure` + React fiber 树分析确认 TabComponent `hasEntered=true`、`mountOnEnter=false`、`isActive=true` 但 `childrenCount=0`
  - 排除了 `transformPageJson`、Vite dev 模式、`applyPageTransformers` 等原因
  - 通过 git 回溯 `libs/` tgz 文件，发现 commit `28ec07a`（Plan 30）替换了 AMIS tgz
  - diff 新旧 `Root.js`，确认真实变动仅有 `renderChild` 中 `createElement(Comp, props)` → `jsx(Comp, props)`
  - 根因：React 19 的 `jsx()` 和 `React.createElement()` 在无 children 元素的命令式创建场景下有行为差异，`renderChild` 是 AMIS 递归渲染子 schema 的核心入口
  - 修复：创建 Vite 插件 `fixAmisRenderChild`（`packages/amis-react/src/vite-fix-amis-render-child.ts`），在 `normalizeEsmJsxRuntime` 后处理 `Root.js` 后，将 `renderChild` 中的 `jsx(Comp,` 替换为兼容包装
  - 写了 bug 记录 `docs/bugs/46-amis-tabs-content-not-rendering-under-react19-jsx-runtime.md`
  - Source: `packages/amis-react/src/vite-fix-amis-render-child.ts`、`docs/bugs/46-amis-tabs-content-not-rendering-under-react19-jsx-runtime.md`

- **nop-entropy-e2e 全量浏览器测试适配完成（38/38 全绿）** — 修复了最后一个失败测试（删除用户），并同步 e2e-shared 代码和文档。
  - **根因 1：`BASE_URL` 未设置** — Playwright `page.goto('#/NopAuthUser-main')` 解析到 `baseURL`（默认后端 8080），不是当前页面 URL。`login()` 内部用绝对 URL 所以登录能过，但后续 hash 导航指向后端。修复：必须设 `BASE_URL=http://localhost:4173`
  - **根因 2：删除确认对话框 click 静默失败** — nop-chaos-next 自定义 alert-dialog 使用 `position: fixed`，Playwright locator click 可能不触发事件。修复：`CrudListPage.deleteRow()` 改用 `page.evaluate()` 原生 DOM `element.click()`
  - **e2e-shared 两份同步** — `nop-chaos-next/packages/e2e-shared/src/` 和 `nop-entropy-e2e/packages/e2e-shared/src/` 完全对齐（AmisAdapter.ts、CrudListPage.ts 差异消除）
  - **清理** — 删除 17 个临时诊断测试文件
  - **文档更新** — 更新 4 份文档：`docs/testing/01-e2e-developer-guide.md`（新增 §3.5 AMIS DOM 结构速查）、`docs/testing/02-cross-project-e2e-debugging.md`（新增错误 6-8、反模式 6-7）、`docs/design/e2e-shared-infrastructure.md`（新增实战经验小节）、`docs/design/e2e-frontend-mode.md`（新增 BASE_URL 解析关系警告）
  - Source: `packages/e2e-shared/src/CrudListPage.ts`、`packages/e2e-shared/src/AmisAdapter.ts`、`docs/testing/01-e2e-developer-guide.md`、`docs/testing/02-cross-project-e2e-debugging.md`、`docs/design/e2e-shared-infrastructure.md`、`docs/design/e2e-frontend-mode.md`

- **e2e-shared 架构改进：消除 AMIS 选择器泄漏（P0+P1）** — 参考 nop-entropy 的分析报告，补全 EngineAdapter 接口，抽取通用 PO 方法，消除 PageObject 层的 AMIS 特定选择器。
  - **新增 EngineAdapter 接口方法**（`types.ts`）：`searchField`、`searchButton`、`refreshButton`、`staticFieldValue`、`confirmDialogAction`
  - **AmisAdapter 实现新方法**：搜索表单用 `.cxd-Table-searchableForm`、filter 用 `input[name^="filter_"]`、刷新用 `[class*="fa-sync"]`、只读字段用 `[data-amis-name]` + `.cxd-Form-static`、确认对话框用原生 DOM click
  - **FluxAdapter 实现新方法**：搜索用 `[data-slot="filter-input"]`、`[data-testid="btn-search"]`、`[data-testid="btn-refresh"]`、只读字段用 `[data-field]`
  - **CrudListPage 新增通用方法**：`search(fieldName, value)`、`clickView`、`clickEdit`、`clickDelete`、`clickSave`、`readViewField`、`assertEntityExists`、`assertEntityNotExists`、`getTableRowCount`
  - **FormDialog.getField** 简化：移除 AMIS 硬编码 fallback，改用 `engine.staticFieldValue()`
  - **3 个 PO 精简**：user.po.ts、role.po.ts、resource.po.ts 移除重复的 AMIS 选择器代码，改为继承 CrudListPage 通用方法或使用 `this.search()` 引擎抽象
  - **AMIS 选择器文档独立**：从 `01-e2e-developer-guide.md` 中拆出 §3.5，创建独立文档 `docs/testing/03-amis-dom-selector-reference.md`（17 节，覆盖所有 AMIS 组件 DOM 结构、选择器优先级、三种下拉区别、删除确认对话框、DOM 诊断方法）
  - **测试验证**：38/38 全绿（nop-auth-e2e 全量）
  - Source: `packages/e2e-shared/src/types.ts`、`packages/e2e-shared/src/AmisAdapter.ts`、`packages/e2e-shared/src/FluxAdapter.ts`、`packages/e2e-shared/src/CrudListPage.ts`、`packages/e2e-shared/src/FormDialog.ts`、`docs/testing/03-amis-dom-selector-reference.md`、`docs/testing/01-e2e-developer-guide.md`
