# Development Log — 2026-05-16

### 2026-05-16 (Session 1) — Plan 17: Network & Async Safety Extension

- Converted `createHttpClient` in `packages/amis-core/src/core/ajax.ts` from per-call instantiation to lazy-init singleton via `_client` / `getClient()`, ensuring concurrent requests share the same `refreshPromise` and only one token refresh fires on parallel 401s
- Added concurrent refresh deduplication test in `packages/shared/src/http/client.test.ts`: mock `refreshAccessToken` with a slow promise, trigger 3 concurrent 401 requests, verify refresh called exactly once
- Wrapped `navigator.clipboard.writeText()` in try-catch in `apps/main/src/pages/ai-workbench/index.tsx` with toast error fallback for non-HTTPS environments
- All verification passed: `pnpm --filter @nop-chaos/amis-core build`, `pnpm typecheck`, `pnpm build`, unit tests
- Ref: `docs/plans/17-network-async-safety-extension-plan.md`

### 2026-05-16 (Session 2) — Plan 13: Package Dependency & Exports Hygiene

- **Phase 1**: Added `"lucide-react": "^1.7.0"` to `peerDependencies` in `packages/amis-react/package.json` (was a phantom dep via monorepo hoisting)
- **Phase 2**: Moved `"react": "^19.0.0"` and `"lucide-react": "^1.7.0"` from `dependencies` to `peerDependencies` in `packages/core/package.json` (aligned with ui package strategy)
- **Phase 3a**: Removed phantom dep `"@base-ui/react": "^1.3.0"` from `apps/main/package.json` (confirmed zero imports in apps/main/src — only used in flux-lib/ui)
- **Phase 3b**: Replaced empty `export {}` in `packages/theme-tokens/src/index.ts` with CSS-only package doc comment + `TOKEN_NAMES` const object + `TokenName` type for JS consumers
- **Phase 4a**: Added JSDoc above `ShellRuntimeConfig` in `packages/extension-host/src/runtime.ts` explaining its relationship to `@nop-chaos/shared` input types and why it's intentionally separate
- **Phase 4b**: Created `docs/design/package-exports-strategy.md` documenting src-style vs dist-style exports rationale and peerDependency strategy
- **Phase 4c**: Updated audit rule f in `docs/skills/deep-audit-execution-framework.md` to allow `amis-react` → `@nop-chaos/ui` dependency
- Verification: `pnpm install`, `pnpm typecheck`, `pnpm lint` all pass. `pnpm build` has a pre-existing layer violation (`host-runtime -> index`) from other uncommitted code changes (verified by confirming the violation persists with all plan 13 changes reverted)
- Ref: `docs/plans/13-package-dependency-exports-hygiene-plan.md`

### 2026-05-16 (Session 3) — Plan 16: Plugin Build & Bridge Runtime

- **Phase 1**: Added `'react/jsx-dev-runtime'` to Rollup `external` array in `examples/plugin-demo/scripts/build-with-rollup.mjs`; verified plugin-demo build output doesn't bundle jsx-dev-runtime inline
- **Phase 2**: Changed `setPluginBridge` call from `useEffect` to `useLayoutEffect` in `apps/main/src/App.tsx` so bridge is available before first paint; added `useLayoutEffect` to React import
- **Phase 3**: Added 14 hook tests to `packages/plugin-bridge/src/index.test.ts` covering all 7 hooks (`usePluginBridge`, `usePluginBridgeSnapshot`, `usePluginThemeConfig`, `usePluginUser`, `usePluginManifest`, `usePluginI18n`, `usePluginNotifications`) via mocked `useSyncExternalStore`; total 21 tests all passing
- Verification: `pnpm --filter @nop-chaos/plugin-demo build` passes, `pnpm typecheck` passes (scope packages), `pnpm lint` passes, `pnpm --filter @nop-chaos/plugin-bridge test` 21/21 green
- Pre-existing `@nop-chaos/amis-react` build error (RootRenderProps) is unrelated to this plan
- Ref: `docs/plans/16-plugin-build-bridge-runtime-plan.md`

### 2026-05-16 (Session 4) — Plan 19: Icon System Governance

- **Phase 1**: Replaced `import * as LucideIcons from 'lucide-react'` with `import { icons, type LucideIcon } from 'lucide-react'` in `packages/core/src/utils/iconMap.tsx`; updated `getLucideIcon` to use `(icons as Record<string, LucideIcon>)[pascalName]` instead of `(LucideIcons as Record<string, unknown>)[pascalName]`
- **Phase 2**: Merged dual maps (`fontAwesomeAliasMap` + `iconRegistry`) into single `faNameMap: Record<string, string>`; removed `normalizeAppIconName` import (no longer needed); simplified `resolveFontAwesomeName` to single-map lookup; removed exported `iconRegistry` (was not re-exported from package index)
- **Phase 3**: Created `docs/design/icon-system.md` documenting static vs dynamic icon conventions and name resolution chain; updated `docs/references/icon-naming-and-rendering.md` to reference `faNameMap`
- Verification: `pnpm --filter @nop-chaos/core build` passes, `pnpm typecheck` 21/21 tasks pass, core tests 13/13 pass
- Ref: `docs/plans/19-icon-system-governance-plan.md`

### 2026-05-16 (Session 5) — Plan 21: State Architecture & Test Coverage

- **Phase 1 – Plugin List Single Source**: Removed React Query (`useQuery`) and `fetchPluginList` import from `apps/main/src/pages/plugins/management/index.tsx`. The Zustand store (`usePluginStore`) is retained as the single data source — it persists to localStorage, provides `updatePlugin` for mutations, and is consumed by App.tsx, RouteRenderer.tsx, and PluginMountPanel.tsx. Removed the sync `useEffect`, `setPlugins` selector, loading/error states, and the `items` fallback variable. The page now reads directly from `plugins` via the store.
- **Phase 2 – amis-react Basic Tests**: Created `packages/amis-react/src/env.test.ts` (11 tests) covering `createAmisEnv`: session/_page binding, fetcher function presence, `jumpTo`/`updateLocation` delegation to `adapter.navigate`, `isCurrentUrl` delegation, `notify` message normalization (string/Error/unknown), `alert`/`confirm` delegation. Created `packages/amis-react/src/components/AmisSchemaPage.test.tsx` (3 tests) covering loading/error/success render paths using `act()` from React with happy-dom environment, mocking `amis` and `@nop-chaos/amis-core` modules.
- **Phase 3 – Test Mock Deduplication**: Extracted `createMockAdapter(overrides?: Partial<AmisRuntimeAdapter>)` factory to `packages/amis-core/src/test-helpers/mockAdapter.ts`. Updated `transform.test.ts` (removed 20-line local factory, now uses `{ hasRole: ... }` override). Updated `action.test.ts` (replaced 7 × 17-line inline mock definitions with `createMockAdapter({ resolveAction/compileFunction })` calls — net reduction ~104 lines). Total amis-core tests remain 20/20 green.
- Verification: `pnpm --filter @nop-chaos/main exec tsc --noEmit` passes, `pnpm --filter @nop-chaos/amis-core exec tsc --noEmit` passes, `pnpm --filter @nop-chaos/amis-react exec tsc --noEmit` passes, `pnpm --filter @nop-chaos/amis-core exec vitest run` 20/20, `pnpm --filter @nop-chaos/amis-react exec vitest run` 14/14
- Ref: `docs/plans/21-state-architecture-test-coverage-plan.md`
