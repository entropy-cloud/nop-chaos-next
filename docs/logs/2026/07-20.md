# Development Log — 2026-07-20

- **Plan 11 executed** — Replace AMIS-only PageObject in nop-entropy-e2e (`docs/plans/2026-07-20-2000-3-replace-amis-pageobject-nop-entropy.md`):
  - Phase 1: Audit — documented all imports from `@nop-entropy/e2e-shared` across 3 e2e packages; identified gaps (old helpers not re-exported, incompatible RpcClient types); decided to keep `@nop-entropy/e2e-shared` as single import path with backward-compat re-exports + deprecation markers
  - Phase 2: Fix tsconfig (add DOM/node types, exclude test files); update `index.ts` with backward-compat exports; rewrite `AmisCrudPage` to use `EngineAdapter` (via `createEngine()`) + `FormDialog` from shared lib instead of raw AMIS selectors
  - Phase 3: Export old helpers (form-helper, modal-helper, table-helper, button-helper, amis-selectors, rpc-helper, login-page) from index.ts as `@deprecated` backward-compat; old rpc-helper retained as default `loginRpc`/`rpc` (Playwright APIRequestContext API); shared `RpcClient` available as opt-in
  - Phase 4: `pnpm typecheck` ✅ (all 4 packages), `pnpm build` ✅ (Vite bundle 23 modules). Playwright e2e tests have pre-existing Quarkus backend `ClassNotFoundException: ISearchEngine` — unrelated to TS changes.
  - 计划状态更新为 `completed`；roadmap item 2.2 更新为 ✅；Phase 2 tracking: 2/7 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `../nop-entropy/nop-entropy-e2e/packages/e2e-shared/`

- **Plan 10 executed** — Sync shared lib to nop-entropy-e2e (`docs/plans/2026-07-20-2000-2-sync-e2e-shared-to-nop-entropy.md`):
  - Phase 1: Verified `scripts/sync-e2e-shared.sh` exists and is executable; confirmed target path `../nop-entropy/nop-entropy-e2e/packages/e2e-shared/` exists and is writable
  - Phase 2: Ran sync — 26 files in target (13 source files + 3 test files synced, 10 project-specific files preserved); version marker `0.0.1` matches source; `package.json` valid with `file:` dependency; idempotent second run confirmed
  - Full verification: `pnpm typecheck` (28/28), `pnpm build` (15/15), `pnpm lint` (28/28, 0 errors), `pnpm test` (28/28, all green)
  - Plan status updated to `completed`; roadmap item 2.1 updated to ✅; Phase 2 tracking: 1/7 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `scripts/sync-e2e-shared.sh`, `../nop-entropy/nop-entropy-e2e/packages/e2e-shared/`

- **Plan 9 executed** — PageObject migration: lazy loading and prototype specs (`docs/plans/2026-07-20-2000-1-pageobject-migration-lazy-loading-prototype-specs.md`):
  - Phase 1: Added `mockMenuRoutes?: boolean` to `LoginOptions` in `MockAuthAdapter.ts` — guards sitemap/menu route registration behind `mockMenuRoutes !== false`
  - Phase 2: Migrated `lazy-loading.spec.ts` imports from `./support/auth` to `@nop-chaos/e2e-shared` — 8/8 passed
  - Phase 3: Migrated `amis-prototype.spec.ts` — removed local `login()`, uses shared `login(page, { username: 'proto', mockMenuRoutes: false })` — pre-existing failures unchanged
  - Phase 4: Migrated `flux-prototype.spec.ts` — removed local `login()`, uses shared `login(page, { username: 'proto', mockMenuRoutes: false })` — pre-existing failures unchanged
  - Phase 5: Full suite — 63/74 passed (9 pre-existing prototype/live failures, no regression). `pnpm test` 368/368, `pnpm typecheck && pnpm build && pnpm lint` all green
  - 计划状态更新为 `completed`；roadmap item 1.7 更新为 ✅；Phase 1 tracking: 8/8 全部完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `packages/e2e-shared/src/MockAuthAdapter.ts`, `tests/e2e/lazy-loading.spec.ts`, `tests/e2e/amis-prototype.spec.ts`, `tests/e2e/flux-prototype.spec.ts`

- **Plan 8 executed** — PageObject migration: CRUD and AI Workbench specs (`docs/plans/2026-07-20-1945-8-pageobject-migration-crud-ai-workbench-specs.md`):
  - Phase 1: Migrated `master-detail-buttons.spec.ts` imports — 27/27 passed
  - Phase 2: Migrated `master-detail-dialogs.spec.ts` imports — 6/6 passed
  - Phase 3: Migrated `ai-workbench-styles.spec.ts` imports — serial mode preserved, 4/4 passed
  - Phase 4: Full suite — 63/74 passed (9 pre-existing prototype/live failures, no regression)
  - 计划状态更新为 `completed`；roadmap item 1.6 更新为 ✅；Phase 1 tracking: 7/8 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `tests/e2e/master-detail-buttons.spec.ts`, `tests/e2e/master-detail-dialogs.spec.ts`, `tests/e2e/ai-workbench-styles.spec.ts`

### 2026-07-20

- **Plan 7 executed** — PageObject migration: AMIS-related specs (`docs/plans/2026-07-20-1945-7-pageobject-migration-amis-specs.md`):
  - Phase 1: Migrated `amis-demo.spec.ts` and `amis-preview-crud.spec.ts` imports from `./support/auth` to `@nop-chaos/e2e-shared` — both 1/1 passed
  - Phase 2: Migrated `amis-css-isolation.spec.ts` imports — 2/2 passed
  - Phase 3: Migrated `amis-react19-transition.spec.ts` imports — 2/2 passed
  - Phase 4: Full suite — 63/74 passed (9 pre-existing prototype/live failures, no regression)
  - 计划状态更新为 `completed`；roadmap item 1.5 更新为 ✅；Phase 1 tracking: 6/8 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `tests/e2e/amis-demo.spec.ts`, `tests/e2e/amis-preview-crud.spec.ts`, `tests/e2e/amis-css-isolation.spec.ts`, `tests/e2e/amis-react19-transition.spec.ts`

### 2026-07-20

- **Plan 6 executed** — PageObject migration: flow editor and plugin specs (`docs/plans/2026-07-20-1930-6-pageobject-migration-flow-editor-plugin-specs.md`):
  - Phase 1: Extracted shared `demoRoutesMenuResponse` and `useSeededDemoMenu` to `tests/e2e/support/demoRoutes.ts` — eliminated duplication across `flow-editor.spec.ts` and `plugin-demo.spec.ts`
  - Phase 2: Migrated `flow-editor.spec.ts` and `plugin-demo.spec.ts` imports from `./support/auth` to `@nop-chaos/e2e-shared` — both 1/1 passed
  - Phase 3: Migrated `extension-demo.spec.ts` imports — skip mode works; extension-demo mode has pre-existing failure (confirmed by revert, not caused by migration)
  - Phase 4: Full suite — 63/74 passed (9 pre-existing prototype/live failures, no regression). `E2E_ENGINE=flux`: 63/74 passed, no crash
  - 计划状态更新为 `completed`；roadmap item 1.4 更新为 ✅；Phase 1 tracking: 5/8 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `tests/e2e/flow-editor.spec.ts`, `tests/e2e/plugin-demo.spec.ts`, `tests/e2e/extension-demo.spec.ts`, `tests/e2e/support/demoRoutes.ts`

- **Plan 5 executed** — PageObject migration: login + dashboard + permission + i18n specs (`docs/plans/2026-07-20-1930-5-pageobject-migration-login-dashboard-permission-i18n.md`):
  - Phase 1: Migrated `login.spec.ts` and `sidebar-user-menu.spec.ts` imports from `./support/auth` to `@nop-chaos/e2e-shared` — 1/4 tests passed
  - Phase 2: Migrated `permission.spec.ts` — custom `setup` callback works with shared `login()` — 1/1 passed
  - Phase 3: Migrated `i18n-persistence.spec.ts` — `addInitScript` + `setup` callback works with shared `login()` — 2/2 passed
  - Phase 4: Full suite — 63/74 passed (9 pre-existing failures in prototype/live tests, no regression). `E2E_ENGINE=flux`: 8/8 passed, no crash
  - 计划状态更新为 `completed`；roadmap item 1.3 更新为 ✅；Phase 1 tracking: 4/8 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `tests/e2e/login.spec.ts`, `tests/e2e/sidebar-user-menu.spec.ts`, `tests/e2e/permission.spec.ts`, `tests/e2e/i18n-persistence.spec.ts`

- **Plan 4 executed** — MockAuthAdapter and Shared Lib Unit Tests (`docs/plans/2026-07-20-1930-4-mock-auth-adapter-and-unit-tests.md`):
  - Phase 1: Verified `MockAuthAdapter.ts` exists with `login()` API drop-in compatible with `support/auth.ts`; exports all types (`LoginVariant`, `LoginOptions`, `buildMockLoginResponse`, `defaultSiteMapResponse`, `defaultMenuResponse`, `MockAuthAdapter`)
  - Phase 2: Created `engine.test.ts` (12 tests), `GraphQlClient.test.ts` (12 tests), `RpcClient.test.ts` (9 tests) — total 33 new unit tests covering all exported functions, error paths, and edge cases
  - 全量验证通过：`pnpm typecheck` (28/28) + `pnpm build` (15/15) + `pnpm lint` (28/28, 5 pre-existing warnings) + `pnpm test` (28/28, all 33 new tests green)
  - 计划状态更新为 `completed`；roadmap item 1.8 更新为 ✅
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `packages/e2e-shared/src/MockAuthAdapter.ts`, `packages/e2e-shared/src/engine.test.ts`, `packages/e2e-shared/src/GraphQlClient.test.ts`, `packages/e2e-shared/src/RpcClient.test.ts`

### 2026-07-20

- **Plan 3 executed** — E2E Engine and Base URL Config Support (`docs/plans/2026-07-20-1928-3-e2e-engine-and-base-url-support.md`):
  - Phase 1: Added `BASE_URL` env var alias support in `playwright.config.ts` (falls back to `PLAYWRIGHT_BASE_URL`, default `http://127.0.0.1:4175`)
  - Phase 2: Added `@nop-chaos/e2e-shared` as root devDependency for e2e test context; updated `playwright.config.ts` to read `E2E_ENGINE` env var and forward to webServer environment
  - 全量验证通过：`pnpm typecheck` (28/28) + `pnpm build` (15/15) + `pnpm test` (55 files, 368 tests) + `E2E_ENGINE=flux pnpm test:e2e --list` (74 tests, no crash)
  - 计划状态更新为 `completed`；roadmap items 1.1, 1.2 更新为 ✅
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `docs/design/e2e-shared-infrastructure.md`, `docs/design/e2e-frontend-mode.md`

### 2026-07-20

- **Plan 2 executed** — Create e2e-shared Sync Script (`docs/plans/2026-07-20-1928-2-create-e2e-shared-sync-script.md`):
  - Phase 1: Created `scripts/sync-e2e-shared.sh` — rsync/cp-based sync script that copies `packages/e2e-shared/src/` to target, generates/updates `package.json` with `file:` dependency, writes `e2e-shared-version.txt`, preserves project-specific files
  - Verified: no-args exits 1, sync to `/tmp/test-e2e-sync` copies 12 files, version marker (0.0.1), idempotent second run
  - 全量验证通过：`pnpm test` (28/28) + `pnpm typecheck` (28/28) + `pnpm build` (15/15)
  - 计划状态更新为 `completed`；roadmap item 0.2 更新为 ✅
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `docs/design/e2e-shared-infrastructure.md`

### 2026-07-20

- **Plan 1 executed** — Create e2e-shared Workspace Package (`docs/plans/2026-07-20-1928-1-create-e2e-shared-package.md`):
  - Phase 1: Created `packages/e2e-shared/` scaffolding (package.json, tsconfig.json, tsconfig.base.json alias, pnpm install)
  - Phase 2: Implemented core types (`EngineAdapter`, `CrudPageConfig`, `EngineType`) and engine factory (`getEngineType`, `createEngine`, `getEngine`)
  - Phase 3: Implemented `AmisAdapter` (`.cxd-*` selectors) and `FluxAdapter` (`data-slot`/`data-testid` selectors)
  - Phase 4: Implemented PageObject hierarchy (`BasePage`, `CrudListPage`, `FormDialog`)
  - Phase 5: Implemented API clients (`GraphQLClient` full CRUD, `RpcClient`, `Navigation`)
  - Phase 6: Implemented fixtures (`test` with engine/page customization), `index.ts` public API exports, `README.md`
  - 全量验证通过：`pnpm typecheck` (28/28) + `pnpm build` (15/15) + `pnpm test` (55 files, 368 tests)
  - 计划状态更新为 `completed`；roadmap item 0.1 更新为 ✅
  - Source: `docs/design/e2e-shared-infrastructure.md`, `docs/backlog/e2e-upgrade-roadmap.md`

### 2026-07-20

- **Plan 42 executed** — Page JSON Extension Point (`docs/plans/42-page-json-extension-point.md`):
  - Phase 1: 类型定义 — 新增 `packages/shared/src/types/pageTransformer.ts`（`PageTransformerContext`、`PageTransformFn<T>`、`PageTransformerRegistration<T>`），修改 `ShellExtension` 新增 `pageTransformers` 字段
  - Phase 2: Registry + 执行引擎 — 新增 `packages/extension-host/src/pageTransformers.ts`（`registerPageTransformer`、`unregisterPageTransformer`、`getPageTransformers`、`applyPageTransformers`），从 `@nop-chaos/extension-host` 导出
  - Phase 3: Bootstrap 注册 — `apps/main/src/extensions/bootstrap.ts` 在 `applyExtensionDefinitions` 中注册 extension 声明的 `pageTransformers`
  - Phase 4: AMIS 集成 — `apps/main/src/amis/providers.ts` 中 `mainAmisPageProvider.getPage` 包装 `applyPageTransformers`
  - Phase 5: Flux 集成 — `FluxRouteRenderer.tsx` 初始加载 + `adapter.ts` 内部导航 `loadPage` 均经过 transformer
  - Phase 6: 单元测试 — 13 个测试覆盖 registry 排序、串联执行、异常处理、快照隔离、泛型推断
  - 全量验证通过：`pnpm typecheck` (27/27) + `pnpm build` (14/14) + `pnpm lint` (0 errors) + `pnpm test` (55 files, 368 tests)
  - 计划状态更新为 `completed`；所有 closure gates 勾选
- **Plan 30 executed** — AMIS React 19 JSX Transform Remediation plan (`docs/plans/30-amis-react19-jsx-transform-remediation-plan.md`):
  - Phase 1: Confirmed trigger surface already frozen. Updated docs/logs.
  - Phase 2: SchemaRenderer.tsx `React.createElement` → JSX (confirmed done in earlier commit). Added `schemaRenderer-jsx-runtime.test.tsx` with 3 focused tests covering dynamic components, ref forwarding, and invisible branch. amis-ui calendar `React.createElement` calls adjudicated as non-trigger residual.
  - Phase 3: Hardened build pipeline — all 3 rollup configs (amis-core, amis-ui, amis) patched with explicit `jsx: 'react-jsx'` in compilerOptions; added `normalizeEsmJsxRuntime()` post-processing plugin for ESM to strip spurious `createElement` from `@rollup/plugin-typescript` output. `transpileReactCreateElement()` already limited to CJS. `npm run pack:nop-chaos` rebuilt all 5 tgz packages. Verified ESM output: SchemaRenderer.js, Root.js, index.js all have 0 `createElement` calls/imports.
  - Phase 4: Fresh tgz consumed in `nop-chaos-next/libs/`. Host verification: `pnpm typecheck` (27/27), `pnpm build` (14/14), `pnpm lint` (27/27), `pnpm test` (55 files, 368 tests) all green.
  - Plan status updated to `completed`. Documentation: `docs/references/build-guide.md` updated with React 19 JSX runtime build notes.
  - Residual: amis-ui calendar `React.createElement` and amis-core `uncontrollable.tsx` `React.createElement` moved to `Deferred But Adjudicated`. Full closure audit (Playwright console-capture) deferred to independent sub-agent.
- **Plan 15 executed** — Route & Permission Robustness plan (`docs/plans/15-route-permission-robustness-plan.md`):
  - Phase 1/2: closure audit confirmed `dedupeRoutesByPath` specificity-based dedup + console.warn、两层权限模型注释、`filterMenusByRoles` 空父级处理均已 landing
  - Phase 3: 补齐 focused verification — `apps/main/src/router/AppShell.test.tsx` 新增 4 条测试，验证 `tabStore.activePath` 在 MemoryRouter initial mount、forward/back history navigation、multi-step traverse 下与 `location.pathname` 同步正确
  - 全量验证通过：`pnpm typecheck` (27/27) + `pnpm build` (14/14) + `pnpm lint` (27/27, 5 pre-existing warnings) + `pnpm test` (52/52 test files, 350/350 tests)
  - 计划状态更新为 `completed`
- 引入 Mission Driver 基础设施：
  - 创建 `missions/e2e-upgrade.json` — e2e 升级 mission 配置
  - 创建 `docs/backlog/e2e-upgrade-roadmap.md` — 6 个 Phase、~30 个工作项的完整跨项目路线图
  - 创建 `tools/mission-driver.sh` — launcher 脚本，指向 AGE 模板引擎
  - 创建 `docs/skills/multi-dimensional-audit-prompt.md`、`docs/skills/open-ended-audit-prompt.md` — DEEP_AUDIT 步骤所需的审计提示
  - 创建 `docs/audits/`、`docs/context/` 目录（功能预留）
  - 更新 `AGENTS.md` — 增加 Mission Driver 工作流说明
- **Plan 18 executed** — Component Decomposition Plan (`docs/plans/18-component-decomposition-plan.md`):
  - 所有三个 Phase 已完成（dashboard 171 行、flow-editor 222 行、graphql 175 行）
  - Dashboard 6 个图表组件已提取到 `dashboard/components/`
  - Flow editor 7 个 hooks + types/constants/utils/context 已提取
  - GraphQL module 已拆分为 `graphqlArgs.ts` + `graphqlFilter.ts`（含独立测试）
  - 全量验证通过：`pnpm typecheck` (27/27) + `pnpm build` (14/14) + `pnpm lint` (27/27) + `pnpm test` (55 files, 368 tests)
  - 计划状态更新为 `completed`；所有 closure gates 勾选
  - 路径约定：所有项目假设为兄弟目录（如 `nop-chaos-next`、`nop-entropy`、`nop-app-erp`、`attractor-guided-engineering-template` 在同一父级目录下平级存放）；嵌套 worktree 通过软链接适配
- 设计文档 `docs/design/e2e-shared-infrastructure.md`：基于对 nop-chaos-next、nop-entropy-e2e、nop-app-erp 三套 e2e 的全面分析，设计了统一共享基础设施：
  - EngineAdapter 接口（AmisAdapter / FluxAdapter 双引擎）
  - PageObject 层次（BasePage → CrudListPage + FormDialog）
  - API 客户端（GraphQLClient + RpcClient）
  - 统一 env var 体系（E2E_ENGINE、E2E_AUTH_MODE、FRONTEND_DEV_MODE 等）
  - 代码源放在 nop-chaos-next `packages/e2e-shared`，通过 sync 脚本分发到其他项目
- 设计文档 `docs/design/e2e-frontend-mode.md`：定义 nop-entropy-e2e 测试如何通过 `FRONTEND_DEV_MODE` 环境变量切换到 nop-chaos-next 的 Vite dev server 作为前端来源，无需修改测试代码；CI 默认行为不变（使用 `nop-web-site` JAR）。
- **Plan 14 executed** — Type Safety & API Surface verification plan (`docs/plans/14-type-safety-api-surface-plan.md`):
  - 验证所有 8 个 deep audit 发现（03-02, 03-03, 03-06, 11-01, 11-02, 11-03, 11-06, 11-07）已在 live repo 中修复
  - Phase 1: 确认 `payload.ts` 有 `@remarks` JSDoc、`AmisSchemaPage.tsx` 无 `as never`、`providers.ts` 有返回类型和校验
  - Phase 2: 确认 `apps/main/src/types/` 无 `systemjs.d.ts`、无裸 `System` 全局引用
  - Phase 3: 确认 `iconRegistry` 不导出、`AppIconProps` 已导出、`AmisFetcherResult.headers` 为必选
  - Phase 4: 确认 `ShellExtension`、`PluginBridge`、`AmisRuntimeAdapter` 均有 JSDoc
  - 全量验证通过：`pnpm typecheck` (27/27) + `pnpm build` (14/14) + `pnpm lint` (27/27) + `pnpm test` (27/27, 346 tests)
  - 计划状态更新为 `completed`
- **Plan 25 executed** — API Surface Hygiene Follow-Up (`docs/plans/25-api-surface-hygiene-follow-up-plan.md`):
  - Phase 1: Removed dead `isAbsoluteUrl` duplicate from `packages/amis-core/src/core/url.ts` (zero consumers). Canonical version remains in `packages/shared/src/http/url.ts`.
  - Phase 2: Audited all 22+ low-level exports from `packages/shared/src/index.ts`. Removed 14 zero-consumer exports from root entry (URL helpers: `appendQueryParams`, `normalizeRequestUrl`, `resolveRequestUrl`, `hasProtocolPath`, `isProtocolRelativePath`; JWT internals: `decodeJwtPayload`, `getTokenExpiry`, `isTokenExpiringSoon`, `isTokenExpired`, `refreshAccessToken`, `getRefreshPromise`, `setRefreshPromise`, `createTokenStorage`, `getTokenStorage`). All retained exports have confirmed external consumers.
  - 全量验证通过：`pnpm typecheck` (27/27) + `pnpm build` (14/14) + `pnpm test` (55 files, 368 tests, 0 failures)
  - 计划状态更新为 `completed`
- **Plan 26 executed** — Plugin ESM Dual-Format Follow-Up (`docs/plans/26-plugin-esm-dual-format-follow-up-plan.md`):
  - Phase 1 (Contract Decision): 裁定构建合同为 `SystemJS`-only；更新 `docs/examples/plugin-dev-guide.md`（移除/收紧 ESM 误导性表述）、`docs/design/plugin-system.md`（添加 contract 明确声明）
  - Phase 2 (Reference Implementation): 验证 plugin-demo 构建仅输出 SystemJS 格式，与合同一致；`pnpm --filter @nop-chaos/plugin-demo build` 通过
  - 全量验证通过：`pnpm typecheck` + `pnpm build` + `pnpm test`
  - 计划状态更新为 `completed`
- 设计文档 `docs/design/page-json-extension-point.md`：为 extension 系统新增页面 JSON 后处理扩展点。
  - 新增 `PageTransformerRegistration` 类型 + registry（`packages/extension-host/src/pageTransformers.ts`）
  - `ShellExtension` 新增 `pageTransformers` 字段
  - AMIS 流水线：`fetchAmisPage` → `applyPageTransformers` → `AmisPageRoute` setSchema → AmisSchemaPage
  - Flux 流水线：`fetchFluxPage` → `applyPageTransformers` → `FluxRouteRenderer` setSchema → FluxSchemaRenderer
  - 与现有 `transformPageJson` / `registerXuiComponent` 互补，先于 AMIS 内部转换执行
  - 经独立子 agent 审计后修正：AMIS 侧应用点从 `AmisPageRoute.tsx` 改为 `providers.ts`（避免包边界违规）；Flux 侧覆盖初始加载 + 内部导航 `loadPage` 两条路径；类型签名改为泛型 `PageTransformFn<T>`
  - 开发计划 `docs/plans/42-page-json-extension-point.md`：6 个 Phase（类型定义 → Registry → Bootstrap → AMIS → Flux → 单元测试），含 closure gates
- **Plan 23 executed** — Flow Editor Reactive Cost Containment (`docs/plans/23-flow-editor-reactive-cost-plan.md`):
  - Phase 1: Memo Dependency Narrowing — `editorActions` useMemo deps narrowed from full `state` to individual values; `state.setHoveredNodeId/setHoveredEdgeId` destructured instead of accessed via `state.*`
  - Phase 2: Hover Fan-Out Containment — hover state (`hoveredNodeId`, `hoveredEdgeId`, `setHoveredNode`, `setHoveredEdge`) split from `FlowEditorActions` into separate `FlowEditorHoverContext`; `editorActions` no longer depends on hover values
  - Phase 3: Dirty Detection Cost Reduction — `dirty` computation wrapped in `useMemo` with `[initialized, nodes, edges, savedSnapshot]` deps instead of bare render-time `JSON.stringify`
  - New test files: `useFlowEditorActions.test.ts` (3 identity-focused tests), `useFlowEditorState.test.ts` (6 dirty semantic tests)
  - 全量验证通过：`pnpm typecheck` (27/27) + `pnpm build` (14/14) + `pnpm lint` (0 errors) + `pnpm test` (54 files, 358 tests)
  - 计划状态更新为 `completed`
- **Plan 24 executed** — Coverage And Hygiene Follow-Up Plan (`docs/plans/24-coverage-and-hygiene-follow-up-plan.md`):
  - Phase 1: Store And Shared Utility Proof — Added focused tests for `layoutStore.ts` (toggleMenuGroup, clamp, persistence merge), `themeStore.ts` (themeId normalization, persist restore), `url.ts` (isAbsoluteUrl, appendQueryParams, normalizeRequestUrl, resolveRequestUrl), `payload.ts` (isApiPayload, unwrapApiPayload success/error/fallback). 55 test files, 368 tests.
  - Phase 2: Test Harness Readability And GraphQL Error Paths — Moved React mock in `plugin-bridge/src/index.test.ts` from mid-file to top (eliminates hoisted-mock readability trap). Added 6 error-branch focused tests for `normalizeGraphQLResponse` (errors array, nop-status, missing message, primitive error, non-object passthrough, operation name extraction). amis-core: 151 tests.
  - Phase 3: Extension Demo E2E Entry And Package Hygiene — Removed unused `clsx` and `react-router-dom` from `packages/core/package.json`. Created `test:e2e:extension-demo` npm script (`PLAYWRIGHT_APP_MODE=extension-demo playwright test tests/e2e/extension-demo.spec.ts`). Documented entry in `docs/testing/index.md`.
  - 全量验证通过：`pnpm typecheck` (27/27) + `pnpm build` (14/14) + `pnpm lint` (27/27, 0 errors) + `pnpm test` (55 files, 368 tests)
  - 计划状态更新为 `completed`
