# Development Log — 2026-06-19

### 2026-06-19

- **Fixed AMIS Prototype schemaPath bug**: `prototypes/amis-demo/menu.json` used full URLs (`/api/prototype/pages/...`) for `schemaPath`, but the AMIS extension mapper's `toSchemaUrl()` prepends `/api/prototype/pages/` again, causing double-prefixed broken URLs. Changed to relative paths (`./system/users.json` etc.).
- **Fixed Flux Prototype extension**: `examples/flux-prototype-demo/src/index.ts` flattened all menu items to `pageType: 'flux'` without handling nested children. Rewrote with recursive `mapMenuItem()` (matching AMIS pattern): parent groups → `pageType: 'builtin'`, leaf items → `pageType: 'flux'` with resolved `schemaPath`.
- **Created prototypes/flux-demo/**: Full Flux prototype data — `menu.json` (two-level menu), `pages/_partials/crud-page.json` (x:extends base), `pages/system/users.json`, `pages/system/roles.json`, `pages/content/articles.json` (all with inline `source` data + x:extends), `mock/index.mjs` (CRUD mock middleware).
- **Added Flux prototype e2e test**: `tests/e2e/flux-prototype.spec.ts` — 3 tests covering menu loading, user page data rendering, roles page, and articles page.
- **Added Playwright config**: `playwright.flux-prototype.config.ts` (port 4177, `vite dev --mode flux-prototype`).
- **Added root script**: `test:e2e:flux-prototype` in `package.json`.
- **Typecheck passes**: All 5 affected packages (@nop-chaos/main, @nop-chaos/vite-plugin-prototype-server, @nop-chaos/delta-merge, @nop-chaos/example-amis-prototype, @nop-chaos/example-flux-prototype).
- **Build note**: `pnpm build` fails on `flux-lib/ui/dist` bundling (pre-existing; dev mode unaffected).
- **Status**: Both prototype demos have complete code, data, and e2e tests. Full e2e verification requires Playwright browser (manual).
