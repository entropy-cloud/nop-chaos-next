# Development Log — 2026-05-19

### 2026-05-19

- 调整多处设置/编辑表单的字段垂直节奏，统一把 `label` 与输入控件之间偏紧的 `space-y-2` / `gap-2` 提升为更清晰的 `gap-2.5`，涉及 `apps/main/src/pages/settings/layout/index.tsx`、`apps/main/src/pages/auth/login/index.tsx`、`apps/main/src/pages/plugins/management/index.tsx`、`apps/main/src/pages/flow-editor/[id]/components/NodeInspector.tsx`、`apps/main/src/pages/flow-editor/[id]/components/EdgeInspector.tsx`、`apps/main/src/pages/data-management/master-detail/[id]/components/AddressDialog.tsx`、`apps/main/src/pages/data-management/master-detail/[id]/components/LogisticsDrawer.tsx`。
- `Layout settings` 页额外从原生 `<label>` 包裹写法切到显式 `Label + Input` 字段结构，避免字段内部节奏继续依赖 `space-y` 和浏览器默认块级表现。
- 本轮为界面密度微调，不涉及状态逻辑或设计边界变更；验证待跑：`pnpm typecheck`、`pnpm lint`、`pnpm build`。

### 2026-05-19

- 用 Playwright 诊断 e2e 打印了 AI Workbench "重新生成"按钮的实际 computed style 和 CSS 变量值，确认 Tailwind 类已生效、AMIS 未污染、`--host-*` 保护正常。
- 真正根因：`--secondary` token 值为 `255 92% 76%`（高饱和紫色），`--secondary-foreground` 为 `217 89% 35%`（深蓝色），两者配在一起就是用户报告的"深色底配深色文字"。
- 将所有 4 个主题变体的 `--secondary` 改为浅灰色（light：`210 40% 96%`，dark：`217 33% 17%`），`--secondary-foreground` 改为与 `--foreground` 一致，符合 shadcn 风格的浅灰背景+深色文字。
- 验证通过：诊断 e2e 确认新值为 `background: rgb(241, 245, 249)` + `color: rgb(2, 8, 23)`；整份 `tests/e2e/ai-workbench-styles.spec.ts` 4/4 通过；`pnpm lint` 通过；unit tests 9/9 通过。

### 2026-05-19

- 复核 AI Workbench “重新生成”按钮语义色问题时，确认真正未生效的不是 `apps/main/tailwind.config.ts` 本身，而是 `apps/main/src/styles/tailwind.css` 仍把 `@config` 指向仓库根 `tailwind.config.ts`，导致宿主 app 的 Tailwind content 配置没有进入实际样式生成链路。
- 将 `apps/main/src/styles/tailwind.css` 改为 `@config '../../tailwind.config.ts'`，使宿主 CSS 入口实际使用 `apps/main/tailwind.config.ts`，从而恢复 `@nop-chaos/ui` secondary / primary 语义按钮样式。
- 在 `apps/main/src/styles/themeContract.test.ts` 补充 CSS 入口断言，并新增 `tests/e2e/ai-workbench-styles.spec.ts`，直接锁定 `/#/ai-workbench` “重新生成”按钮计算样式必须匹配 secondary 语义 token。
- 更新 `docs/testing/2026/05-19.md`、`docs/bugs/40-main-tailwind-ui-content-scan-drift.md`、`docs/design/styling-system-specification.md`，把根因描述从“content 目录漂移”修正为“Tailwind CSS 入口 config 漂移”。
- 本轮验证通过：`pnpm --filter @nop-chaos/main exec vitest run src/styles/themeContract.test.ts`、`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 pnpm exec playwright test tests/e2e/ai-workbench-styles.spec.ts --project=chromium`、`pnpm --filter @nop-chaos/main exec vite build --mode mock`。

### 2026-05-19

- 修复 `apps/main/src/pages/data-management/master-detail/[id]/index.tsx` 及其 `ItemsSection.tsx`、`AddressesSection.tsx`、`LogisticsSection.tsx`、`AddressDialog.tsx`、`LogisticsDrawer.tsx` 中表单内按钮类型未显式声明的问题；非提交动作统一改为 `type="button"`，页面主保存动作改为 `type="submit"`，避免删除物流时误触发表单提交和 `beforeunload` 确认循环。
- 新增 `tests/e2e/master-detail-dialogs.spec.ts` 回归用例，覆盖物流删除确认的取消/确认链路，约束 confirm 只打开一次且确认后只删除目标记录。
- 在 `docs/testing/2026/05-19.md` 追加本次手工复现问题，并新增 `docs/bugs/39-master-detail-detail-form-button-submit-loop.md` 记录“应用内 confirm 与浏览器原生离开确认叠加”这一跨层根因。
- 验证结果：`pnpm lint` 通过；`pnpm typecheck` 与 `pnpm build` 被仓库现有 `apps/main/src/flux/adapter.ts` 隐式 `any` 报错阻塞，与本次修复无关；Playwright 默认起服路径被现有 `4175` 端口占用阻塞，改用 `http://127.0.0.1:4173` 现成服务后已打到真实删除确认链路，但后续复跑时该本地 dev server 已不可达，未完成最终全绿复验。

### 2026-05-19

- 使用 `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 pnpm exec playwright test tests/e2e/master-detail-dialogs.spec.ts -g "deleting logistics confirms once and closes cleanly" --project chromium` 成功把 master-detail 删除物流回归稳定锁定为 UI 层问题：取消 confirm 后，关闭态 `alert-dialog-overlay` 仍拦截 pointer events，导致第二次删除点击超时。
- 在 `flux-lib/ui/src/components/ui/alert-dialog.tsx` 为 overlay 增加 `data-closed:pointer-events-none`，消除关闭动画阶段的交互遮挡；保留前面已做的详情页表单按钮 `type` 修正。
- 重新验证通过：focused 删除物流 e2e 通过；`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 pnpm exec playwright test tests/e2e/master-detail-dialogs.spec.ts --project chromium` 全量 5/5 通过；`pnpm lint` 通过。

### 2026-05-19

- 将原先单个“删除物流确认链路”回归拆成两个更窄的 Playwright 用例：`canceling logistics deletion allows reopening confirm` 专门锁定“取消后可以再次打开确认框”；`confirming logistics deletion removes the record without leftover overlay` 专门锁定“确认后记录被删除且页面后续交互不会被残留遮罩挡住”。
- 使用 `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 pnpm exec playwright test tests/e2e/master-detail-dialogs.spec.ts -g "canceling logistics deletion allows reopening confirm|confirming logistics deletion removes the record without leftover overlay" --project chromium` 复验，拆分后的两个 focused 用例均通过。

### 2026-05-19

- 根据用户追加反馈，把删除物流回归进一步改成会显式监听 Playwright `dialog` 事件：只要删除物流链路触发浏览器原生空 confirm / beforeunload dialog，测试就会直接失败。
- 排查后补齐了 `apps/main/src/pages/data-management/master-detail/[id]/components/FilterToolbar.tsx` 中遗漏的 `Clear filters` 按钮 `type="button"`，清掉详情页表单内最后一个默认 submit 风险点。
- 重新验证通过：`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 pnpm exec playwright test tests/e2e/master-detail-dialogs.spec.ts -g "canceling logistics deletion allows reopening confirm|confirming logistics deletion removes the record without leftover overlay" --project chromium` 以及整份 `tests/e2e/master-detail-dialogs.spec.ts` 均通过，且未捕获任何浏览器原生 `dialog`；`pnpm lint` 通过。

### 2026-05-19

- 纠正验证基线：不再复用来源不明的 `4173` 现成服务，而是显式启动一份受控的 mock dev server：`pnpm --filter @nop-chaos/main exec vite --mode mock --host 127.0.0.1 --port 4317 --strictPort`。
- 基于这份明确的 `dev:mock` 服务重跑删除物流 focused e2e：`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4317 pnpm exec playwright test tests/e2e/master-detail-dialogs.spec.ts -g "canceling logistics deletion allows reopening confirm|confirming logistics deletion removes the record without leftover overlay" --project chromium`，结果 2/2 通过。
- 继续基于同一份 `dev:mock` 服务重跑整份 `tests/e2e/master-detail-dialogs.spec.ts`，结果 6/6 通过；`pnpm lint` 通过。当前在受控 mock 基线上仍未复现“点击 confirm 后再弹浏览器空 confirm”。

### 2026-05-19

- 根据用户在 `4317` 上的最新反馈，继续追到 `apps/main/src/components/common/ConfirmDialogHost.tsx`：根因不是浏览器原生 confirm，而是 `request` 清空后 host 仍渲染了一个空的 `AlertDialog` 外壳，导致标题回退成 `Confirm`、正文为空、按钮点击无业务效果。
- 将 `ConfirmDialogHost` 改为 `request` 为空时直接 `return null`，并把删除物流 e2e 补成确认后不允许残留空白 `Confirm` 标题。
- 基于受控 `dev:mock` 服务 `http://127.0.0.1:4317` 重新验证：focused 删除物流用例 2/2 通过，整份 `tests/e2e/master-detail-dialogs.spec.ts` 6/6 通过，`pnpm lint` 通过。
