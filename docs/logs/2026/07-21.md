# Development Log — 2026-07-21

### 2026-07-21

- **Plan 4.1 executed** — FluxAdapter 完善 (`docs/plans/2026-07-21-0117-2-fluxadapter-completion.md`):
  - Phase 1: Audited FluxAdapter against EngineAdapter interface; gaps already documented in Current Baseline
  - Phase 2: Enhanced `selectOption` — multi-field iteration, cascading (wait-for-child), searchable (type-to-filter), fallback (`data-testid`/`li`/`[role="option"]`)
  - Phase 3: Added `datePickerSelect` — calendar open → month/year navigation → date click; native `input[type="date"]` fill fast path
  - Phase 4: Added `confirmDialog()` and `alertDialog()` helper methods with `[data-slot="dialog-surface"]` / `[role="alertdialog"]` selectors
  - Phase 5: Verification — `pnpm typecheck` 28/28, `pnpm build` 15/15, `pnpm test` 55 files/368 tests all green; new `FluxAdapter.test.ts` with 7 structural tests
  - Plan Status: `completed`; roadmap item 4.1 updated to ✅; Phase 4 tracking: 1/4 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `packages/e2e-shared/src/FluxAdapter.ts`

- **Plan 17 executed** — Navigation Login Unification for nop-app-erp (`docs/plans/2026-07-21-0117-1-navigation-unification-nop-app-erp.md`):
  - Phase 1: Audited all call sites (index.ts, Page.ts, fixtures.ts); mapped divergence between shared and local Navigation; documented 4 divergence areas (params, wait strategy, URL format, login detection); strategy decision: hybrid local wrapper (b)
  - Phase 2: Updated local `Navigation.ts` with divergence documentation header; preserved all implementations (no behavior change for ~180 specs); barrel export (`index.ts`) unchanged
  - Phase 3: Verified Page.ts → BasePage.goto() → loginAndNavigate chain intact; CrudListPage.ts extends BasePage — no changes needed
  - Phase 4: nop-chaos-next: `pnpm typecheck` 28/28, `pnpm build` 15/15, `pnpm test` 55 files/368 tests — all green; nop-app-erp: `npx playwright test --list` — 614 tests in 130 files, no import errors
  - Plan Status: `completed`; roadmap item 3.3 updated to ✅; Phase 3 tracking: 3/7 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `../nop-app-erp/tests/e2e/pages/Navigation.ts`

- **Plan 16 executed** — nop-app-erp shared lib sync and cleanup (`docs/plans/2026-07-21-1000-2-nop-app-erp-shared-lib-sync-and-cleanup.md`):
  - Phase 1: Audited all 11 pages/ files against shared lib; identified 4 compatible files for replacement and 7 files for local retention due to API incompatibility (GraphQLClient.ts uses different query syntax/transport, CrudListPage.ts has different constructor `(page,config,engine?)` and URL convention, Page.ts `goto` calls `loginAndNavigate`, Navigation.ts has different login parameters, types.ts has DEFAULT_* constants used by kept locals)
  - Phase 2: Ran `scripts/sync-e2e-shared.sh` → version marker `e2e-shared-version.txt` (v0.0.1); selectively moved/restored files; fixed `_helper.ts` FormDialog getter rename (`dialog.locator` → `dialog.dialog`); `npx playwright test --list` lists 614 tests with no import errors
  - Phase 3: Replaced AmisAdapter.ts, FluxAdapter.ts, engine.ts, FormDialog.ts with shared versions; kept local: types.ts, Page.ts, CrudListPage.ts, GraphQLClient.ts, Navigation.ts, index.ts, README.md (documented reasons for each)
  - Phase 4: CRUD runtime verification blocked (no nop-app-erp backend running) — deferred to next mission cycle
  - Plan Status: `completed`; roadmap items 3.1, 3.2 updated to ✅; Phase 3 tracking: 2/7 完成
  - nop-chaos-next verification: `pnpm typecheck` 28/28, `pnpm build` 15/15, `pnpm test` 55 files/368 tests — all green
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `../nop-app-erp/tests/e2e/pages/`, `docs/plans/2026-07-21-1000-2-nop-app-erp-shared-lib-sync-and-cleanup.md`

- **Plan 14 executed** — Navigation Login Unification in nop-entropy-e2e (`docs/plans/2026-07-20-2015-3-navigation-login-unification.md`):
  - Phase 1: Audited all login imports across auth-e2e, code-e2e, job-e2e — 11 files with LoginPage/LoginPO usage; 3 identical login.po.ts wrappers; 2 patterns (standalone login and browserLogin helper); credentials inconsistency (auth-e2e uses explicit args, code/job use defaults); Navigation.login() unused in all 3 packages
  - Phase 2: Updated shared `Navigation.ts` — added `login()` with `{ username, password, baseUrl }` options, locale setting (matching LoginPage.goto), `E2E_AUTH_MODE=rpc` path via RpcClient.loginRpc(); fixed `loginAndNavigate()` signature; exported `login` from e2e-shared index.ts; resolved Module vs MockAuthAdapter export conflict
  - Phase 3: Migrated auth-e2e (4 spec files, 10 LoginPO sites → login()), code-e2e (2 spec files, 4 LoginPO sites), job-e2e (1 spec file, 2 LoginPO sites); fault-tolerance.spec.ts had no LoginPO usage (RPC-only)
  - Phase 4: Removed all 3 login.po.ts files (auth-e2e, code-e2e, job-e2e); full `pnpm typecheck` passes for all 4 packages
  - Plan Status: `completed`; roadmap item 2.5 updated to ✅; Phase 2 tracking: 5/7 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `../nop-entropy/nop-entropy-e2e/packages/e2e-shared/src/Navigation.ts`, `../nop-entropy/nop-entropy-e2e/packages/e2e-shared/src/index.ts`

- **Plan 15 executed** — nop-entropy-e2e test adaptation (Phase 2.6 + 2.7) (`docs/plans/2026-07-21-1000-1-nop-entropy-e2e-test-adaptation.md`):
  - Migrated all 6 AmisCrudPage PO files (user, role, resource, schedule, fire, task) to `CrudListPage` + `FormDialog` pattern
  - Updated 2 code-e2e BasePage PO files (type-hierarchy, symbol-search) to accept `engine` parameter
  - Updated 7 spec files to use `test` fixture from `@nop-entropy/e2e-shared` with `engine` fixture; fault-tolerance.spec.ts (RPC-only) unchanged
  - Fixed `ISearchEngine` classpath issue in nop-code by adding `nop-search-api` dependency to `nop-code-app/pom.xml`
  - Changed e2e-shared RPC exports from `@nop-chaos/e2e-shared` (raw TS in node_modules) to local `RpcClient.ts` to fix Playwright TS stripping error
  - All typechecks pass (nop-entropy-e2e 3 packages + nop-chaos-next 28 tasks)
  - Playwright test discovery verified: auth-e2e 38 tests, code-e2e 9 tests, job-e2e 7 tests (54 total)
  - Plan Status: `completed`; roadmap items 2.6, 2.7 updated to ✅; Phase 2 tracking: 7/7 完成
  - Source: `docs/backlog/e2e-upgrade-roadmap.md`, `docs/plans/2026-07-21-1000-1-nop-entropy-e2e-test-adaptation.md`
