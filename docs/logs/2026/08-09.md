# Development Log — 2026-08-09

### 2026-08-09 (C1a 宿主验收：flux CRUD/Table/Dialog AMIS 视觉对齐宿主联调与程序化验收证据)

flux 侧 C1a plan（`docs/plans/2026-08-09-0001-c1a-crud-table-dialog-visual-amis-parity-plan.md`）Phase 7 宿主人工验收的程序化执行轮（上游 flux 仓会话代执行，改动清单本 log 记录）。

- **flux 基线同步**：`pnpm refresh:flux`（`repack-flux-and-refresh.sh`，FLUX_ROOT → `../nop-chaos-flux` symlink → flux master `300a413a`，含 C1a + closeOnSubmit 契约收口）——重打 `libs/nop-chaos-flux-0.1.0.tgz` + `sync-flux-lib.sh`（ui → `flux-lib/ui`、theme-tokens/tailwind-preset → `packages/`）+ `pnpm add` 重装；flux-sync 日志已自动记录（`docs/logs/flux-sync/2026/08-09.md`，upstream commit `300a413a`）。
- **宿主验收 spec（新增 `tests/e2e/c1a-visual-acceptance.spec.ts`，6/6 PASS，mock 模式 4175）**：
  - AMIS 基线（Amis Preview 页，amis 6.13.1 bridged 主题实测）：thead 14px/44px（padding 11px 10px 11px 12px）、body 12px/47px（padding 11px 10px 11px 16px 边列）、行高 47px；normal modal 宽 500px、top 60px。
  - flux 侧（Flux Demo 页）：thead 14px/40px、body 12px、padding 11px/10px、边列 16px、行 hover 整行 + 单元透传、操作按钮 32px、dialog 500px/top 60px/遮罩 rgba(0,0,0,0.7)/标题 14px —— 全部与 C1a token 面一致。
  - ui 原语直接消费冒烟：master-detail 列表 Table + 详情 ItemsSection Table + AddressDialog（`DialogContent`）、plugins management（`DialogContent`）、flow-editor 列表 Table —— 全部正常渲染。
- **实测偏差记录（token 可调，非结构性缺陷）**：① thead 高 flux 40px vs AMIS 44px（`--table-row-height` 可覆盖）；② body 行高为内容驱动（flux 操作按钮 32px + padding 22px ≈ 55px；AMIS 该页 link 按钮行 47px；AMIS 默认按钮同规格时同样膨胀）；③ 遮罩 flux 0.7 vs 宿主 bridged AMIS 0.4（`amis-fix.css .cxd-Modal-overlay { background: var(--surface-overlay) }` 宿主既有覆盖；C1a 按 AMIS default.css 0.7 落地，宿主可用 `--dialog-overlay-bg` 一键覆盖）。以上已记录 flux 侧 plan Closure Gates 与 daily log。
- **回归**：`E2E_ENGINE=flux` flux-crud-list + flux-crud-form + flux-crud-flow + flux-dashboard + flux-complex-form + flux-report + flux-ids-batch + flux-real-schema-mock = 29/30（唯一失败 `flux-real-resp.spec.ts` 依赖真实后端，本机无后端属环境项）；`c1a-visual-acceptance` 6/6。
- **宿主既有问题观察（与本次同步无关）**：master-detail-buttons 套件当前 26/27 失败（页面正文中文「查看」vs spec 期望英文 'View' 的 locale 错位，表与数据渲染正常）——宿主 e2e-upgrade WIP 范围，非 flux 同步引入（stash 前同款失败模式）。
- 上游 flux 侧配套证据：flux 仓新增 `tests/e2e/component-lab/c1a-visual-amis-parity.spec.ts`（8/8：密度/hover 透传/边缘阴影/操作列/样式表规则/dialog 尺寸矩阵/堆叠/遮罩与 footer），并修复 playground 遗留 `[data-slot='dialog-overlay']` 直写规则改走 `--dialog-overlay-bg` token（C1a 验收发现：portal 外 `--nop-dialog-backdrop` 作用域失效导致遮罩透明）。
