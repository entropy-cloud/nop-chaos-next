# Development Log — 2026-05-13

### 2026-05-13

- Updated `scripts/analyze-main-chunk-graph.mjs` to separate static imports from dynamic lazy boundaries in the final layer checks and to classify Rolldown bootstrap chunks (`index-*`, `preload-helper-*`, `storage-*`) as support/runtime chunks.
- Reclassified `vendor-amis-*`, `vendor-amis-bridge-*`, `vendor-amis-ui-*`, and `vendor-amis-formula-*` into the same `bridge` family as `host-amis-*` so AMIS runtime internals are validated as one boundary instead of being misreported as vendor/host violations.
- Updated `apps/main/src/services/http.ts` to load `@nop-chaos/amis-core` request/response transforms via dynamic import, which removes the last static `host-runtime -> vendor-amis-bridge-*` edge from the main shell.
- Verified `pnpm --filter @nop-chaos/main build` now passes together with `pnpm analyze:imports` and `pnpm analyze:chunks`.
- Added `scripts/sync-flux-lib.sh` in `nop-chaos-next` so the current repo can sync UI-related packages directly from the sibling `../nop-chaos-flux` project instead of requiring manual copy steps.
- The sync flow now copies `ui`, `theme-tokens`, and `tailwind-preset`, strips upstream test sources from `flux-lib/ui`, and refreshes the current workspace install so the synced packages are usable immediately.
- Verified synced packages now run on TypeScript 6 in this repo: `@nop-chaos/ui`, `@nop-chaos/theme-tokens`, and `@nop-chaos/tailwind-preset` all resolve through the current workspace toolchain.
- Workspace-wide `pnpm typecheck` and `pnpm build` now pass.
- Updated `apps/main/src/App.tsx` to stop passing Zustand hook functions as ordinary values into the plugin bridge; the bridge now exposes stable callable store wrappers built from `getState` and `subscribe`.
- Verified the repo no longer has an intentional TypeScript 5 toolchain path; the active workspace toolchain is TypeScript 6 and the synced UI-related packages also execute with TS 6.
- After the `App.tsx` bridge-store fix, workspace-wide `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass again.
- Updated `apps/main/src/plugins/sharedModules.ts` so `recharts` is no longer part of the startup-time base shared module registry; it is now dynamically imported and registered only when plugin shared modules are actually requested.
- Verified `pnpm --filter @nop-chaos/main build` still passes and that `host-runtime-*` no longer imports `vendor-recharts-*`; `vendor-recharts-*` is now only pulled by `page-dashboard-*` and plugin-loading paths.
- Traced the large anonymous `src-*` chunk in `apps/main` to the external `../amis-react19/packages/office-viewer` runtime instead of host app code, then added an explicit path-based mapping in `apps/main/vite.config.ts` so it now emits as `vendor-office-viewer-*`.
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`, and the previous `src-*` hotspot is now a named `vendor-office-viewer-*` chunk.
- Split part of the shared `vendor-misc-*` bucket into named chunks by package family: `vendor-base-ui-*`, `vendor-date-*`, `vendor-embla-*`, `vendor-cmdk-*`, and `vendor-mobx-*`.
- Verified the stable split reduced `vendor-misc-*` from roughly `2.54 MB` to about `1.88 MB` in `apps/main`, while `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`.
- Confirmed `vendor-amis-*` is already separated from `vendor-echarts-*` at the package-mapping level; remaining `echarts` coupling is inside the external AMIS renderer graph, which still emits `INEFFECTIVE_DYNAMIC_IMPORT` warnings from `amis-react19`.
- Added a dedicated `vendor-amis-chart-*` split for `packages/amis/src|esm/renderers/Chart`, which further reduced the main `vendor-amis-*` chunk from about `2.44 MB` to about `2.06 MB` without changing `amis-react19` source code.
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`; `echarts`/`zrender` remain in `vendor-echarts-*`, while chart renderer glue now sits in `vendor-amis-chart-*`.
- Continued the AMIS facade split by moving `AMIS`, `Json`, `OfficeViewer`, and `PdfViewer` renderer entry modules into dedicated chunks (`vendor-amis-renderer-*`, `vendor-amis-json-*`, `vendor-amis-office-viewer-*`, `vendor-amis-pdf-viewer-*`).
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`, and the residual shared `vendor-amis-*` chunk dropped further to about `2.05 MB`.
- Fixed the `RichText` split fully: removed the static `RichTextControlRenderer` import from `../amis-react19/packages/amis/src/compat.ts` by switching that compatibility filter to `renderer.type === 'input-rich-text'`, then corrected `apps/main/vite.config.ts` to match the real renderer path `renderers/Form/InputRichText`.
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`, `InputRichText-*` now emits through `vendor-amis-rich-text-*`, and the shared `vendor-amis-*` chunk dropped again to about `2.04 MB`.
