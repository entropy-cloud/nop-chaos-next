# Development Log — 2026-05-13

### 2026-05-13

- Added a concrete mock-mode `Flux Demo` route in `apps/main/public/data/menu-config.json`, implemented `apps/main/src/flux/providers.ts` plus `apps/main/src/flux/testSchema.ts`, and replaced the old placeholder `FluxRouteRenderer` with a real schema-driven mock page so Flux now has a parity sample next to `Amis Preview`.
- Tightened the lazy-route verification by updating `tests/e2e/lazy-loading.spec.ts` to assert rendered Flux demo content instead of the old placeholder i18n key, and documented in `docs/design/amis-flux-rendering-engine-integration.md` that mock mode must expose a concrete Flux example while keeping the Flux runtime side-effect free until route entry.
- Added `docs/references/build-guide.md` with complete build instructions covering amis-react19 and nop-chaos-flux sibling repos, pack, sync, and nop-chaos-next build pipeline; updated `README.md` Quick Start with prerequisite steps, sibling repo layout, and Flux sync; simplified the AMIS-to-Flux migration roadmap in README; registered the new doc in `docs/index.md`.
- Finished the AMIS `tgz` integration path for `apps/main`: replaced the remaining external-source fallback in `packages/amis-react/package.json`, removed the hard-coded `amis` TypeScript path override in `tsconfig.base.json`, and updated `scripts/main-bundle-utils.mjs` so tarball-based dependencies no longer get rewritten to nonexistent `dist-packages/*.tgz/lib|esm` subpaths.
- Fixed the sibling `amis-react19` packing workflow for the current Windows/Node 25 environment by switching `spawnSync('npm.cmd', ...)` callers to `node .../npm-cli.js`, and updated `amis-react19/scripts/build-amis-for-nop-chaos.mjs` so the custom `amis` build also emits the declaration files needed by the host build.
- Tightened `scripts/analyze-main-chunk-graph.mjs` so only host AMIS bridge chunks are classified as `bridge`, and so same-layer vendor cycles are no longer treated as fatal chunk-gate violations; this restored a green `pnpm --filter @nop-chaos/main build` after the tarball migration changed final chunk topology.
- Verified the final state with `pnpm --filter @nop-chaos/main build`, `pnpm typecheck`, and `pnpm lint`.

- Re-ran focused Playwright coverage against the live `http://127.0.0.1:4175` preview after the runtime override changes: `tests/e2e/amis-demo.spec.ts`, `tests/e2e/amis-react19-transition.spec.ts`, `tests/e2e/lazy-loading.spec.ts`, and `tests/e2e/login.spec.ts` all passed, confirming the `react` / `react-dom` / `echarts` override path still preserves AMIS rendering, transition behavior, lazy loading, and login-shell flow.
- Fixed the new external runtime gate so `echarts` is overridden to a real canonical package root resolved from the live `apps/main` / `amis-react19` dependency graph, instead of aliasing to a nonexistent repo-root install.
- Wired `scripts/check-main-external-runtime-deps.mjs --check` into `apps/main` `build` and `build:analyze`, and updated `docs/design/main-bundle-dependency-strategy.md` to document the new file-dependency runtime singleton gate.
- Added `scripts/check-main-external-runtime-deps.mjs` plus shared policy helpers in `scripts/main-bundle-utils.mjs` to gate duplicated shared runtimes across `apps/main` external `file:` dependencies; the check now explicitly reports `react` / `react-dom` as host-overridden and fails on unresolved duplicates such as `react-router-dom` and `echarts`.
- Fixed the real AMIS login-after-navigation crash by removing the extra bundled `react` dependency from `packages/amis-react/package.json`, moving it to a peer dependency, and forcing root-level `react` / `react-dom` dedupe+alias in `apps/main/vite.config.ts` so the shell and AMIS renderer share one React runtime again.
- Fixed shared menu validation drift for Flux routes by teaching `packages/shared/src/utils/menuConfig.ts` to accept `pageType: 'flux'`, and updated `apps/main/src/services/menuMapper.ts` to map legacy `meta.pageType === 'flux'` plus `meta.schemaPath` into the runtime menu model.
- Added regression coverage for the new menu behavior in `packages/shared/src/utils/menuConfig.test.ts` and `apps/main/src/services/menuMapper.test.ts`.
- Stabilized the E2E baseline by moving login/menu seeding into `tests/e2e/support/auth.ts`; Playwright no longer depends on the preview server actually booting with mock env flags to exercise the shell menu routes.
- Updated `tests/e2e/login.spec.ts`, `tests/e2e/sidebar-user-menu.spec.ts`, and `tests/e2e/lazy-loading.spec.ts` to assert current shell behavior instead of older assumptions about sidebar text, AMIS preload timing, or missing Flux route support.
- Verified the fixes with `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`, plus targeted Playwright runs for `tests/e2e/login.spec.ts`, `tests/e2e/sidebar-user-menu.spec.ts`, and `tests/e2e/lazy-loading.spec.ts`.

- Switched the default Playwright app mode in `playwright.config.ts` back to `mock` so the baseline E2E suite exercises the main app shell instead of the extension-demo shell by default.
- Updated `tests/e2e/lazy-loading.spec.ts` to authenticate before protected-route checks, navigate directly to current AMIS routes, and seed a dedicated Flux menu item only for the Flux lazy-loading assertions.
- Marked `tests/e2e/extension-demo.spec.ts` as extension-mode-only so the Harbor-specific login assertions run only when `PLAYWRIGHT_APP_MODE=extension-demo` is requested.
- Tightened the login regression coverage in `tests/e2e/login.spec.ts` so the post-login dashboard assertion now also fails on uncaught `pageerror` exceptions instead of only checking the URL/sidebar text.
- Added the missing root `happy-dom` devDependency required by `flux-lib/ui/vitest.config.ts`, unblocking workspace-wide `pnpm test` execution again.

- Updated `scripts/analyze-main-chunk-graph.mjs` to separate static imports from dynamic lazy boundaries in the final layer checks and to classify Rolldown bootstrap chunks (`index-*`, `preload-helper-*`, `storage-*`) as support/runtime chunks.
- Reclassified `vendor-amis-*`, `vendor-amis-bridge-*`, `vendor-amis-ui-*`, and `vendor-amis-formula-*` into the same `bridge` family as `host-amis-*` so AMIS runtime internals are validated as one boundary instead of being misreported as vendor/host violations.
- Updated `apps/main/src/services/http.ts` to load `@nop-chaos/amis-core` request/response transforms via dynamic import, which removes the last static `host-runtime -> vendor-amis-bridge-*` edge from the main shell.
- Verified `pnpm --filter @nop-chaos/main build` now passes together with `pnpm analyze:imports` and `pnpm analyze:chunks`.
- Added `scripts/sync-flux-lib.sh` in `nop-chaos-next` so the current repo can sync UI-related packages directly from the sibling `../nop-chaos-flux` project instead of requiring manual copy steps.
- The sync flow now copies `ui`, `theme-tokens`, and `tailwind-preset`, strips upstream test sources from `flux-lib/ui`, and refreshes the current workspace install so the synced packages are usable immediately.
- Verified synced packages now run on TypeScript 6 in this repo: `@nop-chaos/ui`, `@nop-chaos/theme-tokens`, and `@nop-chaos/tailwind-preset` all resolve through the current workspace toolchain.
- Workspace-wide `pnpm typecheck` and `pnpm build` now pass.
- Updated `apps/main/src/App.tsx` to stop passing Zustand hook functions as ordinary values into the plugin bridge; the bridge now exposes stable callable store wrappers built from `getState` and `subscribe`.
- Verified the repo no longer has an intentional TypeScript 5 toolchain path; the active workspace toolchain is TypeScript 6 and the synced UI-related packages also execute with TS 6.
- After the `App.tsx` bridge-store fix, workspace-wide `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass again.
- Updated `apps/main/src/plugins/sharedModules.ts` so `recharts` is no longer part of the startup-time base shared module registry; it is now dynamically imported and registered only when plugin shared modules are actually requested.
- Verified `pnpm --filter @nop-chaos/main build` still passes and that `host-runtime-*` no longer imports `vendor-recharts-*`; `vendor-recharts-*` is now only pulled by `page-dashboard-*` and plugin-loading paths.
- Traced the large anonymous `src-*` chunk in `apps/main` to the external `../amis-react19/packages/office-viewer` runtime instead of host app code, then added an explicit path-based mapping in `apps/main/vite.config.ts` so it now emits as `vendor-office-viewer-*`.
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`, and the previous `src-*` hotspot is now a named `vendor-office-viewer-*` chunk.
- Split part of the shared `vendor-misc-*` bucket into named chunks by package family: `vendor-base-ui-*`, `vendor-date-*`, `vendor-embla-*`, `vendor-cmdk-*`, and `vendor-mobx-*`.
- Verified the stable split reduced `vendor-misc-*` from roughly `2.54 MB` to about `1.88 MB` in `apps/main`, while `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`.
- Confirmed `vendor-amis-*` is already separated from `vendor-echarts-*` at the package-mapping level; remaining `echarts` coupling is inside the external AMIS renderer graph, which still emits `INEFFECTIVE_DYNAMIC_IMPORT` warnings from `amis-react19`.
- Added a dedicated `vendor-amis-chart-*` split for `packages/amis/src|esm/renderers/Chart`, which further reduced the main `vendor-amis-*` chunk from about `2.44 MB` to about `2.06 MB` without changing `amis-react19` source code.
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`; `echarts`/`zrender` remain in `vendor-echarts-*`, while chart renderer glue now sits in `vendor-amis-chart-*`.
- Continued the AMIS facade split by moving `AMIS`, `Json`, `OfficeViewer`, and `PdfViewer` renderer entry modules into dedicated chunks (`vendor-amis-renderer-*`, `vendor-amis-json-*`, `vendor-amis-office-viewer-*`, `vendor-amis-pdf-viewer-*`).
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`, and the residual shared `vendor-amis-*` chunk dropped further to about `2.05 MB`.
- Fixed the `RichText` split fully: removed the static `RichTextControlRenderer` import from `../amis-react19/packages/amis/src/compat.ts` by switching that compatibility filter to `renderer.type === 'input-rich-text'`, then corrected `apps/main/vite.config.ts` to match the real renderer path `renderers/Form/InputRichText`.
- Verified `pnpm --filter @nop-chaos/main build` still passes with `Cycles: none` and `Layer violations: none`, `InputRichText-*` now emits through `vendor-amis-rich-text-*`, and the shared `vendor-amis-*` chunk dropped again to about `2.04 MB`.
