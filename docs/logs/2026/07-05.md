# Development Log — 2026-07-05

### 2026-07-05

- **Fixed pre-existing lint error in `flux-lib/ui/src/components/ui/label.tsx`** — Root cause: root `eslint.config.js` enabled only a subset of `jsx-a11y` rules (omitting `label-has-associated-control`), so the legitimate `eslint-disable-next-line jsx-a11y/label-has-associated-control` directive in the `<Label>` primitive (htmlFor is passed through props by consumers, copied from nop-chaos-flux) was flagged as "unused". Fix: added `'jsx-a11y/label-has-associated-control': 'error'` to `eslint.config.js:98`, making the existing disable directive meaningful and preserving the original accessibility intent. Verified: `pnpm lint` now passes on all 27 tasks with no new errors surfaced anywhere.
- **Implemented `docs/design/shell-profiles.md`** — Multi-client profile mechanism now fully wired end-to-end:
  - New types in `packages/shared/src/types/extension.ts`: `ShellChromeMode`, `ShellProfileOverride`, `ExtensionShellConfig.profiles`, `ShellExtension.profiles`, and `LoadExtensionsOptions.profileName`.
  - New `apps/main/src/config/profile.ts`: `resolveShellProfile()` / `getShellProfile()` (URL `?profile=` > `window.__NOP_SHELL_PROFILE__` > `web`, with `__NOP_SHELL_PROFILE_LOCKED__` opt-out), plus `applyExtensionProfileOverrides()` and `resetShellProfile()` (test-only). Default derivation: `siteId === name`, `chromeMode = name === 'web' ? 'full' : 'chromeless'`.
  - `apps/main/src/main.tsx`: `bootstrap()` now calls `resolveShellProfile()` before `bootstrapExtensions()`.
  - `apps/main/src/services/menuApi.ts`: `siteId: 'main'` replaced with `getShellProfile().siteId`.
  - `apps/main/src/router/AppShell.tsx`: wrapper picks `ChromelessShell` (renders only `<Outlet />`) when `chromeMode === 'chromeless'`, otherwise renders the existing full shell as `FullShell` (avoids React hook-order violations).
  - `packages/extension-host/src/loadExtensions.ts`: filters loaded extensions by `profileName` (skips `setup()` and side effects when extension declares `profiles` not containing the active profile name).
  - `apps/main/src/extensions/bootstrap.ts`: passes `getShellProfile().name` to `loadExtensions` and applies extension overrides after load.
  - `apps/main/index.html`: added `<!--NOP_SHELL_PROFILE_INJECT-->` placeholder.
  - Added `apps/main/src/config/profile.test.ts` (14 tests covering resolution priority, locking, immutability, and override merging).
  - Verification: `pnpm typecheck` ✅, `pnpm build` ✅, `pnpm test` ✅ (329 main tests, 33 extension-host tests, 14 new profile tests). `pnpm lint` clean on touched packages (pre-existing `flux-lib/ui` label.tsx lint error is unrelated — present in working tree before this change).
- Created `docs/design/overview.md` — 系统架构总览，显式描述"通用应用壳（Generic Application Shell）"的顶层设计愿景，包括架构分层、页面模型、数据流和内置页面定位。
- Updated `docs/design/index.md` to reference overview.md as the starting point for new readers
- Updated `docs/index.md` to list overview.md as the first design doc and first reading guide item
- Updated `docs/design/plugin-system.md` and `docs/design/extension-system.md` to add context cross-references to overview.md
- Created `docs/design/shell-profiles.md` — 多客户端形态 profile 设计文档。描述如何通过单一构建产物支持 web/mobile/kiosk 等形态：profile 解析（URL param + `window.__NOP_SHELL_PROFILE__` + 默认 web）、siteId 推导、chrome 模式（full/chromeless）、与 extension 的关系、mobile 全屏 JSON 渲染流程。关键发现：`apps/main/src/services/menuApi.ts:59` 硬编码 `siteId: 'main'`，后端 SiteMap API 已支持 siteId 参数但前端未利用。
- Updated `docs/design/overview.md` and `docs/index.md` to reference shell-profiles.md
