# Development Log — 2026-05-26

### 2026-05-26

- Added a new root workflow command, `pnpm rebuild:amis-flux:build`, to repack `amis-react19`, rebuild and pack `nop-chaos-flux`, sync Flux source packages into `nop-chaos-next`, and then build the current workspace.
- Reworked `scripts/sync-flux-lib.sh` from a raw copy script into a guarded sync pipeline: it now snapshots existing repo-local compatibility edits, replaces packages from `../nop-chaos-flux`, removes accidental synced source artifacts such as `src/**/*.d.ts` / `src/**/*.d.ts.map`, reapplies the downstream compatibility patch, refreshes installs, and rebuilds the synced workspace packages.
- Added script-managed Flux sync history under `docs/logs/flux-sync/`; future successful `pnpm sync:flux` runs will record the sibling repo path, upstream commit SHA, synced packages, timestamps, and the post-sync rebuild/patched result there.
- Kept the Flux integration split explicit: `apps/main` still consumes the `@nop-chaos/flux` tarball from `../nop-chaos-flux/dist-packages/`, while `@nop-chaos/ui`, `@nop-chaos/theme-tokens`, and `@nop-chaos/tailwind-preset` still come from synced source packages.
- Updated the build guide and both READMEs so the new rebuild command and the strengthened Flux sync behavior are visible from the normal setup path.
- Verification results after the script and doc changes:
  - `pnpm lint` passed.
  - `pnpm typecheck` failed on pre-existing implicit-`any` errors in `apps/main/src/flux/adapter.ts:29,48`.
  - `pnpm build` failed on the same pre-existing `apps/main/src/flux/adapter.ts` type errors because the main build runs `tsc --noEmit` before Vite.
- Completed Plan 29 sync-policy closure work against upstream `../nop-chaos-flux` commit `0fadc9a3d92d64b3cd10fb82bcdc551e1b2c7c0d`: `scripts/sync-flux-lib.sh` no longer replays generic downstream source patches, synced packages now copy upstream source directly, and only the runtime-only `package.json:scripts.test = vitest run --passWithNoTests` residual remains for `flux-lib/ui`, `packages/theme-tokens`, and `packages/tailwind-preset`.
- Finalized Scheme B host token extension wiring under `apps/main/src/styles/flux-host-token-extension.css` and `apps/main/src/styles/fluxHostTailwindExtension.ts`, updated root Tailwind wiring and theme contract proof, and declared `@nop-chaos/theme-tokens` in `apps/main/package.json` so the host-owned extension remains package-correct.
- Synced the final upstream cleanup for `../nop-chaos-flux/packages/ui/src/components/ui/label.tsx` so downstream `flux-lib/ui` no longer carries any unadjudicated source drift beyond the documented runtime-only sync residual.
- Updated `docs/references/flux-sync-spec.md`, `docs/references/build-guide.md`, `docs/design/styling-system-specification.md`, and `docs/logs/flux-sync/index.md` to match the live policy: upstream copy + runtime cleanup + explicit `package.json` policy, with no generic downstream patch replay.
- Final verification after the closure work:
  - Upstream package verification had already passed for the landed shared-library changes: `pnpm --filter @nop-chaos/theme-tokens typecheck`, `pnpm --filter @nop-chaos/theme-tokens build`, `pnpm --filter @nop-chaos/tailwind-preset typecheck`, `pnpm --filter @nop-chaos/tailwind-preset build`, `pnpm --filter @nop-chaos/ui typecheck`, `pnpm --filter @nop-chaos/ui build`, `pnpm --filter @nop-chaos/ui test`.
  - Downstream `pnpm sync:flux` passed and recorded the consumed upstream SHA in `docs/logs/flux-sync/2026/05-26.md`.
  - Downstream `pnpm typecheck` passed.
  - Downstream `pnpm lint` passed.
  - Downstream `pnpm build` passed.
  - Downstream `pnpm test` passed.
- Full regression verification also passed after closure:
  - Full unit/integration suite: `pnpm test` passed.
  - Full Playwright e2e suite: `pnpm test:e2e` passed (`60 passed, 1 skipped`).
  - Verification status: unit + e2e full green.
