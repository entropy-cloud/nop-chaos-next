# Development Log — 2026-07-08

### 2026-07-08

- **Wired flux `loadDict`/`loadPage`/`locale` in nop-chaos-next** — Flux framework env now supports dict and page loading. Changes:
  - `apps/main/src/flux/providers.ts`: Added `fetchFluxDict()` calling `@query:DictProvider__getDict/static,options{value,label}` via `ajaxQuery`, with mock dict support for `role`/`status`. Added local `DictBean`/`DictOption` types (not re-exported from `@nop-chaos/flux` bundle yet).
  - `apps/main/src/flux/adapter.ts`: `createMainFluxEnv()` now returns `locale`, `loadPage` (wraps `fetchFluxPage` with `withPageCache`), and `loadDict` (wraps `fetchFluxDict` with `withDictCache`). Locale resolved from `i18n.language` at env creation time; cache closures read `i18n.language` dynamically for locale-keyed caching.
  - `apps/main/src/flux/cache.ts` (created in prior session): LRU page cache (max 50) + TTL dict cache (20s), locale-keyed, clone-on-read, in-flight dedup, error-evict.
  - Verification: `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm test` ✅ (329/329).

## Commits

nop-chaos-next commit included in this log entry.
