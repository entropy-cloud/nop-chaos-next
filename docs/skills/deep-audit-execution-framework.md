# nop-chaos-next 深度审核执行框架

> **定位**: 本文档是一份面向 AI 的多维度深度审核框架。包含通用的执行方法论和项目特定的审计维度。
> 每个维度提供一个可复用的"维度正文"，由主 agent 与共享前缀拼接后派发给专用子 agent 执行。
> **前提**: 执行审核前必须先阅读 `docs/index.md`、`AGENTS.md` 和相关架构文档，以当前代码和文档为准。
> **输出格式**: 每个发现按统一格式输出（见附录 A）。
> **来源**: 从 `nop-chaos-flux/docs/skills/deep-audit-prompts.md` 提取通用执行方法论，并针对 `nop-chaos-next` 项目架构设计专属审计维度。

---

## 审核总览

本框架覆盖以下 **12 个审核维度**，分为 6 大类：

| 类别                  | 维度编号 | 维度名称                 | 审核目标                                                                             |
| --------------------- | -------- | ------------------------ | ------------------------------------------------------------------------------------ |
| **A. 架构与边界**     | 01       | 依赖图与包边界           | 包间依赖是否合规、是否有循环依赖、exports 字段一致性                                |
|                       | 02       | 模块职责与文件边界       | 单一职责、文件大小、入口文件纯度、目录结构                                          |
|                       | 03       | API 表面积与契约一致性   | 导出接口是否收敛、公共类型是否稳定、跨包契约一致                                    |
| **B. 运行时与状态**   | 04       | 状态所有权与单一事实来源 | Zustand store 双状态、持久化策略、React state 与 store 同步链                       |
|                       | 05       | 响应式订阅精度           | selector 稳定性、Context value 创建、不必要重渲染                                   |
|                       | 06       | 异步模式与取消安全       | React Query 缓存、竞态、并发保护、异常吞没                                          |
| **C. 路由与插件**     | 07       | 路由、权限与页面注册     | 菜单过滤、权限守卫双检、pageType 分发正确性、extension 覆盖一致性                    |
|                       | 08       | 插件桥接与共享模块       | PluginBridge 契约完整性、故障隔离、SystemJS import map、shared deps 注册            |
| **D. UI 与样式**      | 09       | 主题与样式系统           | CSS 自定义属性一致性、Tailwind token 映射、theme 切换完整覆盖                       |
|                       | 10       | 组件使用与一致性         | @nop-chaos/ui 优先使用、原生 HTML 替代合理性、import 模式一致                       |
| **E. 工程质量**       | 11       | 类型安全与契约质量       | any 收敛、动态边界合理性、公共接口文档化                                            |
|                       | 12       | 测试覆盖与质量           | 测试边界、E2E 覆盖缺口、setup 膨胀、跨包测试                                       |

---

## 子 Agent 执行模型

### 执行方式

深度审核采用**迭代深挖 + 维度复核 + 风险分层子项复核**模型，分为两个阶段：

#### 阶段一：迭代深挖（每个维度多轮）

每个维度不是单次初审就结束，而是循环执行多轮深挖，直到该维度不再产生新的发现：

1. **第 1 轮（初审）**：派发子 agent 执行该维度的完整审核步骤，产出的发现列表保存到 `docs/analysis/{日期}-deep-audit-{标识}/{维度编号}-{名}.md`。
2. **第 N 轮（追加深挖，N ≥ 2）**：将前 N-1 轮已保存的发现全文作为输入，派发一个新的子 agent，要求它：
   - 读取已保存的全部发现
   - 基于已有发现暴露的文件、模式、关联路径，继续深挖该维度中尚未覆盖的盲区
   - **只输出新发现的条目**（不能重复已有发现）
   - 新发现追加写入同一个维度文件（追加到文件末尾，标注 `## 深挖第 N 轮追加`）
3. **终止条件**：若深挖轮次输出"未发现新的问题"或新发现数量为 0，则该维度的深挖阶段结束。
4. **价值收敛条件**：深挖不是为了凑问题。若已经覆盖该维度的主要路径，继续搜索只能得到低价值、机械重复、缺少明确风险或缺少可行动建议的细节问题，应输出"未发现新的高价值问题。深挖结束。"并进入复核。
5. **上限保护**：每个维度的深挖轮次上限为 **10 轮**（含初审），防止无限循环。

#### 阶段二：复核（每个维度独立复核）

深挖阶段结束后，进入复核阶段：

1. **维度复核**：派发一个**独立子 agent**，读取该维度最终完整的发现文件（含所有深挖追加），重新核对 live code / 当前文档，输出"保留 / 降级 / 驳回"的逐条判断。
2. **子项复核（分层）**：维度复核后，再对高风险或不确定的发现项逐项复核；低风险项允许按文件或按模式批量复核。

#### 核心要求

- 复核 agent 不能复用深挖 agent 的结论当作事实，只能把深挖结果当作"待验证线索"。
- 维度复核 agent 与子项复核 agent 都必须回到 live code / 当前文档重新核对。
- 最终汇总报告只能使用"已通过独立复核"的结论，不能直接汇总深挖结果。
- 如果时间有限，可以减少维度数量，但**不能跳过维度复核阶段**。
- 以下条目必须逐项独立复核：`P0/P1`、跨包边界结论、会驱动实际改代码的结论、以及初审与维度复核意见不一致的条目。
- 以下条目可按文件或同类模式批量复核：纯 `P2/P3`、同一文件中的重复模式、已经有充分代码片段证据且风险较低的机械问题。
- 对于"零发现"的维度（深挖所有轮次均无发现），仍需一个独立复核 agent 复查并明确输出"未发现需报告问题"。
- **发现条目完整性**：所有子 agent（深挖、复核、子项复核）的输出都必须遵守"发现条目完整性守则"。深挖 agent 输出完整发现条目；复核 agent 输出逐条判定并引用发现编号，不重写发现内容。主 agent 保存文件前必须检查发现完整性，不合格的输出必须要求子 agent 重新生成。

### 调度模式

```
主 Agent（协调者）
  │
  │  ── 阶段一：迭代深挖 ──
  │
  ├── Task → 维度 01 第 1 轮（初审） → 保存到文件
  ├── Task → 维度 01 第 2 轮（追加深挖） → 追加到文件
  ├── ...（直到无新发现或达到 10 轮上限）
  │
  ├── Task → 维度 02 第 1 轮（初审） → 保存到文件
  ├── ...
  │
  │  ── 阶段二：复核 ──
  │
  ├── Task → 维度 01 维度复核（读取完整文件，逐条保留/降级/驳回）
  ├── Task → 维度 01 子项 A 复核（高风险逐条）
  ├── Task → 维度 01 子项 B 批量复核（低风险批量）
  │
  ├── Task → 维度 02 维度复核
  ├── ...
  └── 汇总已复核通过的结果 → summary.md
```

### 并行策略

- 同批次的**第 1 轮（初审）**可以并行派发。
- 同一维度的**深挖追加轮次**必须串行（每轮依赖前一轮结果）。
- 同一维度的**维度复核**必须在该维度深挖阶段全部结束后进行。
- 同一维度的**子项复核**必须在该维度复核完成后进行。
- **不同维度之间**的深挖、复核可以并行（互不依赖）。

批次之间的初审依赖关系如下：

| 批次   | 维度          | 可否并行         |
| ------ | ------------- | ---------------- |
| 第一批 | 01, 04, 11    | 互相独立，可并行 |
| 第二批 | 05, 06, 07    | 互相独立，可并行 |
| 第三批 | 02, 08, 12    | 互相独立，可并行 |
| 第四批 | 03, 09, 10    | 互相独立，可并行 |

> **注意**：同批次内各维度的第 1 轮初审可并行，但每个维度自身的深挖追加轮次是串行的。实际调度时，主 agent 可按批次并行派发各维度第 1 轮，然后对有发现的维度继续串行深挖，最后再并行派发各维度的复核。

### 深挖追加提示词模板

从第 2 轮起，主 agent 在维度正文后追加以下内容：

```text
---

## 深挖追加指令（第 N 轮）

以下是本维度前 N-1 轮已保存的全部发现：

[粘贴前 N-1 轮的完整发现文本]

你的任务：

1. 读取上述已有发现，理解本维度已经覆盖了哪些文件、模式和路径。
2. 基于已有发现暴露的文件、模式和关联路径，**继续深挖**本维度中尚未覆盖的盲区：
   - 已有发现涉及的文件是否有同类型问题未被检出？
   - 已有发现的模式是否在其他文件中也存在？
   - 是否有与已有发现相关的、但尚未检查的代码路径？
   - 该维度的执行步骤中是否有尚未充分覆盖的步骤？
3. **只输出新发现的条目**。不要重复已有发现中已经报告的内容。
4. 如果经过仔细检查后确实没有发现新的问题，输出："未发现新的问题。深挖结束。"
5. 如果仍能找到零散细节，但这些细节只是低价值、机械重复、缺少明确风险或缺少可行动建议，不要为了凑数量继续报告；输出："未发现新的高价值问题。深挖结束。"
6. 每个新发现仍需遵守统一的发现格式（文件路径 + 行号 + 证据片段 + 严重程度 + 现状 + 风险 + 建议）。

【重要】发现完整性要求：
- 每个新发现必须按完整格式输出（### 标题 + 文件 + 行号 + 3-10行证据代码片段 + 严重程度 + 现状 + 风险 + 建议 + 误报排除）。
- 禁止将发现压缩为关键词、一句话摘要或表格行。
- 禁止只输出标题列表然后说"详情见上方"。
- 如果发现数量较多导致输出较长，宁可分多个子 agent 输出，也不要压缩每个发现的内容。
```

### 保存与追加规则

主 agent 在保存深挖结果时必须遵守以下规则：

1. **第 1 轮结果**：将子 agent 输出的完整发现正文写入文件。如果子 agent 输出被压缩（发现条目少于 8 行），主 agent 必须要求重新输出。
2. **追加深挖结果**：在文件末尾追加 `## 深挖第 N 轮追加` 标题，然后写入新发现的完整正文。**不修改、不压缩、不覆盖**前 N-1 轮已保存的内容。
3. **复核结论追加**：在深挖全部结束后，追加 `## 维度复核结论` 和 `## 子项复核结论`。复核结论引用已有发现编号（如 `[维度04-01]`），不重写发现内容。

---

## 发现条目完整性守则

> **本节是所有子 agent 的硬性约束，与通用审计口径同级。** 违反本节规则的维度文件视为不合格，主 agent 必须要求子 agent 重新输出。

### 问题：发现被过度压缩

历史审核中，维度文件的发现被层层压缩为关键词或表格行，导致读者无法理解问题是什么、影响是什么、该怎么做。以下是典型的压缩反模式及其正误对照：

**反模式 1：深挖轮次只记关键词**

```markdown
## 深挖轮次

- 第 1 轮: store 持久化 key 不一致、tabStore 初始化竞态。
- 第 2 轮: plugin bridge 缺少错误边界、shared modules 重复注册。
```

**正确做法**：每轮深挖的每个发现必须是完整条目：

```markdown
## 第 1 轮（初审）

### [维度04-01] authStore 持久化 key 与 themeStore 版本号不匹配导致升级后丢失状态

- **文件**: `apps/main/src/store/authStore.ts:15`
- **证据片段**:
  ```ts
  const STORAGE_KEY = 'auth:v2';
  ```
- **严重程度**: P1
- **现状**: authStore 使用 `auth:v2` 作为持久化 key，但 themeStore 使用 `theme:v1`。当应用升级需要清除旧数据时，只能按 key 逐一处理，容易遗漏。
- **风险**: 升级后部分 store 数据可能以旧格式残留，导致运行时错误。
- **建议**: 统一持久化 key 的版本管理策略，或提供迁移工具函数。
- **误报排除**: 不是风格偏好问题，是真实的数据迁移风险。
- **复核状态**: 未复核
```

**反模式 2：复核结论表替代发现正文**

**正确做法**：复核结论表只做索引和判定标注，发现正文保留完整格式不变。维度文件的正确结构为：

```markdown
# 维度 04: 状态所有权与单一事实来源

## 第 1 轮（初审）

### [维度04-01] 完整发现标题

[完整发现条目：文件、行号、证据片段、严重程度、现状、风险、建议、误报排除、复核状态]

### [维度04-02] 完整发现标题

[完整发现条目 ...]

## 深挖第 2 轮追加

### [维度04-03] 完整发现标题

[完整发现条目 ...]

## 维度复核结论

[逐条判断，引用上方发现编号]

- [维度04-01]: 保留 (P1)。[一句话复核理由]
- [维度04-02]: 降级为 P2。[降级理由]
- [维度04-03]: 驳回。[驳回理由]

## 子项复核结论

[逐条或批量复核结果，同样引用发现编号]

## 最终保留项

| 编号  | 严重程度 | 文件                              | 一句话摘要                             |
| ----- | -------- | --------------------------------- | -------------------------------------- |
| 04-01 | P1       | `authStore.ts:15`                 | 持久化 key 版本管理策略不一致           |
| 04-03 | P1       | `tabStore.ts:42`                  | closeTab 返回值竞态                     |
```

### 维度文件质量检查清单

主 agent 在接收子 agent 输出后、保存文件前，必须检查以下各项。任何一项不通过，都必须要求子 agent 补充完整后再保存：

1. **每个发现条目 ≥ 8 行**：文件路径、行号范围、证据片段、严重程度、现状、风险、建议、误报排除缺一不可。
2. **证据片段存在**：每个发现必须有 3-10 行代码片段，或明确标注 `[证据片段待补充]`。
3. **深挖轮次不是关键词列表**：每一轮的每个发现都是完整的 `###` 级条目，不是一行关键词。
4. **复核结论引用了发现编号**：不是独立重写，而是引用上方已有的发现条目编号。
5. **最终保留项有文件路径和严重程度**：不是纯文字列表。
6. **零发现维度也需说明检查范围**：列出读过的关键文件和检查过什么，不能只写"零发现"。

### 最低内容量参考

作为经验参考（非硬性上限/下限），一个有 5 个发现的维度文件，按完整格式输出通常在 120-200 行。如果 5 个发现只写出了 40 行，几乎可以确定发生了压缩。

---

## 通用审计口径

以下口径会嵌入每个子 agent 的提示词中，所有维度的审核员必须遵守：

1. **以当前代码为准**，不以历史日志、已关闭的计划或口头约定为准。
2. **不重复报告已收敛的问题**。如果代码中已按架构规则实现，不再标记为问题。
3. **不把"看起来不优雅"当问题**。必须有结构性原因或可量化风险。
4. **对动态边界保持克制**。`any`、`Record<string, unknown>` 在边界上可能是合理的。
5. **每个发现必须可定位**：文件路径 + 行号范围 + 3-10 行证据代码片段。
6. **区分已自动化 vs 需人工发现**。已被 lint/check 覆盖的问题只在"自动化有漏洞"时才报告。
7. **所有结论都必须经过独立复核**。初审 agent 的输出只是线索，不是最终事实。
8. **文件大小限额**。源代码文件超过 **500 行**需要评估拆分；超过 **700 行**必须拆分。

严重程度判级：
- P0: 当前已构成错误行为、安全违约、核心数据损坏风险、或违反硬性架构红线。
- P1: 高概率回归、核心契约漂移、跨包边界错误、或会误导后续开发的问题。
- P2: 真实维护成本或局部缺陷，但可排期处理。
- P3: 低优先级但真实存在的问题。

---

## 子 Agent 提示词装配结构

下文每个维度给出的内容是**维度正文**，不是可单独复制执行的完整 prompt。主 agent 派发前，必须先拼接共享前缀。

完整 prompt 由两部分组成：

```
1. 共享提示词前缀（固定，所有维度相同）
2. 维度正文（本维度目标 + 必读文档 + 执行步骤 + 额外输出要求）
```

### 共享提示词前缀

主 agent 在派发任一维度前，应先附加以下共享前缀：

```text
你正在审核 nop-chaos-next 项目。这是一个 React 19 + Zustand + TypeScript 的前端应用框架 monorepo（pnpm workspace），支持插件系统和低代码集成。

执行前先阅读：
1. docs/index.md（文档导航基线）
2. AGENTS.md（代码规范、验证命令、agent 工作流）
3. 本维度列出的相关文档

通用审计口径：
1. 以当前代码为准，不以历史日志、已关闭计划或口头约定为准。
2. 不重复报告已收敛的问题。
3. 不把"看起来不优雅"当问题，必须有结构性原因或可量化风险。
4. 对动态边界保持克制。any、Record<string, unknown> 在边界上可能是合理的。
5. 每个发现必须可定位：文件路径 + 行号范围 + 3-10 行证据片段。
6. 区分已自动化 vs 需人工发现。已被 lint/check 覆盖的问题，只有在"自动化有漏洞"时才报告。
7. 初审输出只是线索，不是最终事实；最终结论必须经过独立复核。
8. 文件大小规则：>500 行需评估是否拆分，>700 行默认必须拆分。

严重程度判级：
- P0: 当前已构成错误行为、安全违约、核心数据损坏风险、或违反 CI/硬性架构红线。
- P1: 高概率回归、核心契约漂移、跨包边界错误、或会误导后续开发的公开面问题。
- P2: 真实维护成本或局部缺陷，但可排期处理。
- P3: 低优先级但真实存在的问题。

【发现条目完整性 — 强制规则，不可违反】：
1. 每个发现必须按统一格式（附录 A）完整输出，包含文件路径、行号范围、3-10 行证据代码片段、严重程度、现状、风险、建议。
2. **禁止将发现压缩为一行标题或表格单元格。**
3. **禁止用摘要替代完整条目。**
4. **证据代码片段不可省略。** 如果因上下文限制无法贴出代码，必须明确标注"[证据片段待补充]"并列出需要查看的具体行号范围。
5. 每个发现条目最少 8 行（文件、行号、证据、严重程度、现状、风险、建议、误报排除），少于 8 行的条目视为不合格。
6. 复核结论表是补充索引，不能替代发现正文。

如果本维度需要命令输出（如 pnpm check、依赖图、文件行数基线），优先由主 agent 先生成基线，再把结果连同本 prompt 一起提供给你；不要假设你一定能直接运行命令。
```

---

## A. 架构与模块边界

### 维度 01：依赖图与包边界

**子 Agent 提示词**：

```
以下为"维度 01"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 01：依赖图与包边界

目标：检查包间依赖是否存在真实的边界问题，例如跨包内部路径导入、循环依赖、错误的依赖方向、或未被文档/公开契约支撑的私有耦合。

必读文档：
- AGENTS.md 的 Workspace Layout 和 Package Commands 章节
- tsconfig.base.json（路径别名映射）

执行步骤：

1. 读取所有 packages/*/package.json 和 flux-lib/*/package.json，提取 dependencies 和 peerDependencies 中所有 @nop-chaos/* 引用。
2. 构建完整的内部依赖图，以文本形式绘制。
3. 对照以下规则逐条检查：
   a. @nop-chaos/shared 不能依赖任何其他 @nop-chaos/* 包
   b. @nop-chaos/ui 不能依赖 @nop-chaos/core、@nop-chaos/plugin-bridge、apps/*
   c. @nop-chaos/core 只能依赖 @nop-chaos/shared 和 @nop-chaos/ui
   d. @nop-chaos/plugin-bridge 只能依赖 @nop-chaos/shared
   e. @nop-chaos/amis-core 只能依赖 @nop-chaos/shared
   f. @nop-chaos/amis-react 只能依赖 @nop-chaos/amis-core 和 @nop-chaos/shared
   g. @nop-chaos/theme-tokens 和 @nop-chaos/tailwind-preset 不能依赖任何运行时包
   h. apps/main 可以依赖所有 @nop-chaos/* 包
   i. examples/plugin-demo 只能依赖 peerDependencies 中声明的包
4. 对每个违规或可疑依赖，指出：
   - 哪个包的 package.json
   - 违规的依赖声明
   - 违反了哪条规则
   - 是否存在合理的例外理由
5. 额外检查：是否有 import 语句实际引用了其他包的内部路径（非 index 导出）。
6. 检查是否存在循环依赖的迹象。
7. 检查所有包的 exports 字段是否一致。
8. 检查是否有包缺少 build 脚本或 tsconfig.build.json。

输出格式：

对每个发现：
### [维度01] 简短标题
- **文件**: packages/xxx/package.json
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述
- **风险**: 不修复的后果
- **建议**: 修复方向

最后输出：
1. 完整的依赖图（ASCII art 或文本）
2. 违规清单（按严重程度排序）
3. 合规的包清单
4. 总结评估
```

---

### 维度 02：模块职责与文件边界

**子 Agent 提示词**：

```
以下为"维度 02"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 02：模块职责与文件边界

目标：识别职责混合的超大文件、入口文件泄露实现细节、目录结构混乱等问题。

必读文档：
- AGENTS.md 的 Workspace Layout 和 Naming 章节

文件大小限额（项目强制规则）：
- 源代码文件超过 500 行：需要仔细评估是否应该拆分
- 源代码文件超过 700 行：必须拆分，不允许例外
- 自动化工具：运行 `pnpm lint` 检查 max-lines 规则（700 行）

基线要求：优先使用主 agent 提供的文件行数清单或等价基线。如果没有命令基线，也可以先根据代码搜索做结构审查，但必须把所有涉及">500 行"或">700 行"的判断标记为"待命令基线确认"。

执行步骤：

1. 统计所有 packages/*/src/ 和 apps/*/src/ 下非测试、非 .d.ts 文件的行数。列出超过 300 行的所有文件，按行数降序排列。
2. 对每个超过 500 行的文件：
    a. 读取文件内容，识别其中的职责边界
    b. 用 "职责 A: 行 X-Y"、"职责 B: 行 Y-Z" 格式标注
    c. 判断哪些职责应该提取为独立模块
    d. 如果文件是 orchestrator（组装调用子模块），标注为可接受（但应控制在 500 行以内）
    e. 如果文件已做过拆分但重新吸入了实现细节，标注为"二次膨胀"
    f. 超过 700 行的文件必须在输出中标注为 P0 或 P1 级别问题
3. 检查每个包的 index.ts：
   a. 是否仅做 re-export（理想情况）
   b. 是否包含具体实现逻辑（应提取）
   c. 导出项数量是否超过 50 个
4. 统计每个包 src/ 顶层文件数量，超过 20 个的列出来并建议子目录归组方案。
5. 检查是否存在只有 1-2 个文件的子目录（过度拆分）。

输出格式：

对每个发现：
### [维度02] 简短标题
- **文件**: packages/xxx/src/yyy.ts:行号
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述
- **风险**: 不修复的后果
- **建议**: 修复方向
- **误报排除**: 为什么不是合理的 orchestrator 或动态边界

最后输出：
1. 超大文件清单（带职责分析）
2. 入口文件问题清单
3. 目录结构建议
4. 若缺少命令基线，单列"待基线确认项"
```

---

### 维度 03：API 表面积与契约一致性

**子 Agent 提示词**：

```
以下为"维度 03"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 03：API 表面积与契约一致性

目标：确保每个包的公开 API 表面积收敛、无多余暴露、跨包契约一致。

必读文档：
- packages/shared/src/types/ 目录下的所有类型定义

执行步骤：

1. 读取所有 packages/*/src/index.ts 和 flux-lib/*/src/index.ts，列出每个包的全部导出项（类型导出 vs 值导出分开列）。
2. 对每个包，检查：
   a. 是否有仅内部使用但被公开导出的函数/类型
   b. 是否有"内部实现泄露到公开 API"的痕迹（如导出了 helper、util、internal 前缀的项）
   c. 导出的类型是否有 JSDoc 或在 docs/ 中有文档
3. 检查跨包接口一致性：
   a. MenuItem 类型在 shared 和 core 中的使用是否一致
   b. PluginBridge 接口在 plugin-bridge 包中的定义与实际使用是否匹配
   c. ShellExtension 接口的字段是否与 extensions 加载逻辑一致
4. 检查是否有类型通过 import type 从 A 包导出，又在 B 包 re-export 且添加了不同的约束。
5. 检查 packages/*/src/ 下是否有未被 index.ts 导出但被其他包使用的文件。
6. 检查 exports map：package.json 的 exports 字段与实际 index.ts 导出是否对齐。

输出格式：

对每个发现：
### [维度03] 简短标题
- **文件**: packages/xxx/src/yyy.ts:行号
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述
- **风险**: 不修复的后果
- **建议**: 修复方向

最后输出每个包的 API 表面积报告 + 问题清单。
```

---

## B. 运行时与状态

### 维度 04：状态所有权与单一事实来源

**子 Agent 提示词**：

```
以下为"维度 04"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 04：状态所有权与单一事实来源

目标：发现 Zustand store 双状态、React state 与 store 同步链、持久化策略不一致等问题。

必读文档：
- AGENTS.md 的 React Patterns 和 Routing, Permissions, Plugins 章节

执行步骤：

1. 在 apps/main/src/store/ 下读取所有 Zustand store 文件，检查：
   a. 每个 store 的持久化策略（key、storage 类型、partialize）是否一致
   b. 是否有 store 的状态被其他 store 重复维护
   c. store 的 action 是否有竞态风险（如 login 和 logout 同时触发）
2. 搜索 apps/*/src/ 下所有 useState 声明：
   a. 判断它维护的数据是否在 Zustand store 或 React Query cache 中也存在
   b. 检查是否有 useEffect 将 props 或 store 数据同步到这个 state
   c. 检查这个 state 的值是否影响提交/持久化（如果是，则不应是 local state）
3. 搜索 useEffect + setState/setXxx 模式（props-to-state 同步链）：
   a. useEffect 依赖中包含 props 并设置本地 state
   b. 这种模式应该改为 derived value（useMemo）或直接从 store selector 读取
4. 检查 React Query 的缓存与 Zustand store 的数据关系：
   a. React Query 缓存的菜单数据是否在 Zustand 中也有副本
   b. 用户信息是否同时存在于 authStore 和 React Query cache
   c. staleTime 和 gcTime 配置是否合理
5. 检查 PluginBridge 的 snapshot 数据与 store 数据的关系：
   a. Bridge 提供的 user/theme/plugins 数据是否与对应 store 保持同步
   b. Bridge subscribe/getSnapshot 是否正确反映了 store 变化
6. 检查 extensions 系统：
   a. extension 注册的状态（themes、languages、pages）是否与运行时 store 正确同步
   b. extension 的 setup(context) 是否正确处理了重复调用

输出格式：

对每个发现：
### [维度04] 简短标题
- **文件**: apps/main/src/store/xxx.ts 或 apps/main/src/yyy.tsx:行号
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述
- **风险**: 不修复的后果
- **建议**: 修复方向
- **双状态详情**: 哪两份数据在表达同一件事
- **同步失败症状**: 用户可见的故障表现
```

---

### 维度 05：响应式订阅精度

**子 Agent 提示词**：

```
以下为"维度 05"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 05：响应式订阅精度

目标：发现过宽订阅、selector 引用不稳定、不必要重渲染等性能问题。

必读文档：
- AGENTS.md 的 React Patterns 章节

执行步骤：

1. 搜索所有 Zustand store 的 useXxxStore 调用：
   a. 是否使用 selector 还是对整个 store 解构
   b. selector 函数返回了什么（单个值 vs 对象 vs 数组）
   c. 如果返回对象/数组，是否每次调用都创建新引用
2. 搜索所有 useSyncExternalStore 调用（特别在 plugin-bridge 中）：
   a. subscribe 和 getSnapshot 是否稳定引用
   b. getSnapshot 是否返回原始 store slice 而非衍生计算
3. 检查 useEffect 依赖项：
   a. 依赖中是否包含大型对象
   b. 依赖中是否包含每次 render 都新建的引用
4. 检查 Context provider 的 value prop：
   a. 是否每次 render 都创建新对象
   b. 特别是 PluginBridge 的 context
5. 检查 React.lazy 和 Suspense 的使用：
   a. lazy 导入是否在模块顶层（不是在 render 中）
   b. Suspense fallback 是否合理
6. 搜索 useMemo / useCallback 使用：
   a. 是否有 "void useMemo"（返回值未使用）
   b. 是否有明显性能问题但缺少 memo 的位置

输出格式：

对每个发现：
### [维度05] 简短标题
- **文件**: apps/main/src/xxx.tsx:行号
- **严重程度**: P0/P1/P2/P3
- **订阅位置**: 具体的 hook 调用位置
- **订阅范围**: 当前订阅了什么
- **实际需要**: 组件实际依赖的数据路径
- **重渲染频率**: 估算不必要重渲染的触发频率
- **建议**: 收窄方向
```

---

### 维度 06：异步模式与取消安全

**子 Agent 提示词**：

```
以下为"维度 06"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 06：异步模式与取消安全

目标：确保所有异步操作都有取消机制、并发保护、竞态防护，且异常不会被静默吞掉。

必读文档：
- AGENTS.md 的 Error Handling 章节

执行步骤：

1. 搜索所有 async 函数（非 test 文件）：
   a. 检查 React Query 的 queryFn/mutationFn 是否正确处理错误
   b. 检查 staleTime/gcTime/refetchOnWindowFocus 配置是否合理
2. 检查 React Query 的使用模式：
   a. queryClient 的全局配置（staleTime、retry）是否合理
   b. useQuery 的 enabled 条件是否正确控制了请求时机
   c. mutation 的 onSuccess/onError 回调是否有竞态
3. 搜索所有 fetch / API 调用：
   a. 是否有 AbortController 传递
   b. 快速连续触发时是否有去抖/取消旧请求机制
4. 异常吞掉检查：
   a. 搜索所有 catch 块：检查 catch 体是否至少做了日志输出、rethrow、写入错误状态之一
   b. 搜索所有 `void ...then(...)` 模式：检查是否有对应的 `.catch()`
   c. 搜索 HTTP 客户端的错误处理：401 刷新逻辑是否有死循环风险
5. 检查 token 刷新逻辑：
   a. 多个并发请求同时遇到 401 时是否只刷新一次
   b. 刷新失败后的回退行为是否正确（清除 token、跳转登录）
6. 检查 SystemJS 加载的异步错误处理：
   a. 插件加载失败时是否有正确的错误展示
   b. extension 加载失败是否会导致整个应用崩溃

输出格式：

对每个发现：
### [维度06] 简短标题
- **文件**: apps/main/src/xxx.ts:行号
- **严重程度**: P0/P1/P2/P3
- **问题类别**: 竞态/取消安全/异常吞掉
- **异步操作**: 具体的异步操作描述
- **竞态场景**: 步骤 1 用户做 X → 步骤 2 在操作完成前用户做 Y → 结果 Z
- **用户可见故障**: 用户会看到什么
- **建议**: 防护方案
```

---

## C. 路由与插件

### 维度 07：路由、权限与页面注册

**子 Agent 提示词**：

```
以下为"维度 07"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 07：路由、权限与页面注册

目标：确保菜单过滤、权限守卫、pageType 分发、extension 覆盖的一致性和正确性。

必读文档：
- AGENTS.md 的 Routing, Permissions, Plugins 章节

执行步骤：

1. 检查菜单系统：
   a. filterMenusByRoles 的实现是否正确过滤了所有层级
   b. 菜单 API 返回的数据格式与 MenuItem 类型是否一致
   c. extension overrideMenus 是否正确合并（system pages 是否保持可访问）
2. 检查权限守卫：
   a. usePermissionGuard 的实现是否覆盖了所有需要权限的页面
   b. RouteRenderer 中的权限检查是否与 Sidebar 的菜单过滤使用了相同的逻辑
   c. 是否有绕过权限守卫直接访问路由的路径
3. 检查 pageType 分发：
   a. 每种 pageType（builtin/plugin/amis/flux/iframe/external）的渲染逻辑是否正确
   b. 未知的 pageType 是否有合理的 fallback
   c. 是否有 pageType 判断遗漏
4. 检查页面注册：
   a. pageRegistry 的 registerBuiltinPages 是否允许 extension 正确覆盖
   b. getSystemPage 的回退逻辑是否合理
   c. lazy 导入是否都在模块顶层
5. 检查 tab 管理与路由同步：
   a. openTab 是否正确处理了动态路由参数（如 [id]）
   b. tab 的 activePath 是否与 URL 保持同步
   c. 浏览器前进/后退是否正确更新 tab 状态
6. 检查 AppShell 的布局状态：
   a. sidebar 折叠、fullscreen 模式是否在路由切换时保持
   b. 嵌套路由的布局是否正确

输出格式：

对每个发现：
### [维度07] 简短标题
- **文件**: apps/main/src/router/xxx.tsx:行号
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述
- **风险**: 不修复的后果（特别是安全相关）
- **建议**: 修复方向
```

---

### 维度 08：插件桥接与共享模块

**子 Agent 提示词**：

```
以下为"维度 08"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 08：插件桥接与共享模块

目标：确保 PluginBridge 契约完整、故障隔离正确、SystemJS import map 配置正确、shared deps 不泄漏。

必读文档：
- AGENTS.md 的 Routing, Permissions, Plugins 章节
- packages/plugin-bridge/src/index.ts

执行步骤：

1. 检查 PluginBridge 接口完整性：
   a. 接口定义的所有方法是否在 App.tsx 中都有实现
   b. bridge 注入时机是否在应用渲染之前
   c. bridge 的 subscribe/getSnapshot 是否正确桥接了 Zustand store
2. 检查故障隔离：
   a. 插件渲染异常是否被 ErrorBoundary 捕获
   b. 插件崩溃是否会影响主应用 shell 的稳定性
   c. bridge 通知（toast）是否在插件卸载后仍然有效
3. 检查 SystemJS 配置：
   a. sharedModules.ts 中注册的所有模块是否与 peerDependencies 一致
   b. import map 的名称是否与插件的 import 语句匹配
   c. 是否有遗漏的共享依赖（插件使用但未注册的）
4. 检查共享依赖版本：
   a. 是否有依赖在 host 和 plugin 之间版本不一致
   b. React、React DOM 是否正确作为 singleton 共享
5. 检查 plugin-demo 的构建：
   a. Rollup 配置是否正确将共享依赖标记为 external
   b. 构建产物是否同时支持 SystemJS 和 ESM 加载
   c. 插件的 peerDependencies 是否与 host 版本一致
6. 检查 PluginSlot 的加载流程：
   a. URL 格式判断逻辑是否正确区分 SystemJS 和 ESM
   b. 加载状态（loading/error）的 UI 是否合理
   c. 是否有加载超时机制

输出格式：

对每个发现：
### [维度08] 简短标题
- **文件**: packages/plugin-bridge/src/xxx.ts 或 apps/main/src/plugins/xxx.ts:行号
- **严重程度**: P0/P1/P2/P3
- **类别**: 契约完整性/故障隔离/SystemJS配置/依赖版本
- **现状**: 一句话描述
- **风险**: 不修复的后果
- **建议**: 修复方向
```

---

## D. UI 与样式

### 维度 09：主题与样式系统

**子 Agent 提示词**：

```
以下为"维度 09"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 09：主题与样式系统

目标：确保 CSS 自定义属性一致性、Tailwind token 映射正确、theme 切换完整覆盖所有组件。

必读文档：
- AGENTS.md 的 Styling 章节
- packages/theme-tokens/src/styles.css
- packages/tailwind-preset/src/index.ts

执行步骤：

1. CSS 自定义属性检查：
   a. 列出 theme-tokens 中定义的所有 CSS 变量
   b. 检查每个变量是否在所有 theme/mode 组合中都有定义（classic-light, classic-dark, glass-light, glass-dark）
   c. 是否有遗漏的变量导致某些 theme 下样式缺失
2. Tailwind token 映射检查：
   a. tailwind-preset 中映射的 CSS 变量是否都在 theme-tokens 中有定义
   b. 是否有 Tailwind utility class 引用了不存在的 CSS 变量
   c. apps/main/tailwind.config.ts 的 content scan 是否覆盖了所有需要扫描的文件
3. 主题切换检查：
   a. applyThemeToDocument 是否正确设置了 data-theme 和 data-mode
   b. .dark class 是否在 dark mode 下被正确添加
   c. 主题切换后是否所有 CSS 变量都正确更新
4. Extension 主题检查：
   a. extension 注册的自定义主题 CSS 是否正确加载
   b. 自定义主题是否覆盖了所有必要的 CSS 变量
5. 玻璃主题特殊检查：
   a. glass 主题的 backdrop-filter: blur() 是否有性能影响
   b. glass 相关的 surface 变量是否被正确使用
6. 检查是否有硬编码颜色值：
   a. 搜索 rgba(、hsl(、#hex 模式
   b. 是否有应使用 CSS 变量但直接硬编码了颜色值的地方

输出格式：

对每个发现：
### [维度09] 简短标题
- **文件**: packages/theme-tokens/src/styles.css 或 apps/main/src/xxx.css:行号
- **严重程度**: P0/P1/P2/P3
- **违规类别**: 变量缺失/token映射错误/主题切换/硬编码颜色
- **现状**: 一句话描述
- **建议**: 修复方向
```

---

### 维度 10：组件使用与一致性

**子 Agent 提示词**：

```
以下为"维度 10"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 10：组件使用与一致性

目标：确保 @nop-chaos/ui 组件优先使用，原生 HTML 元素只在有合理理由时使用，import 模式一致。

必读文档：
- AGENTS.md 的 Styling 章节

执行步骤：

1. 读取 packages/ui（或 flux-lib/ui）的 src/index.ts 获取完整的可用组件列表。
2. 在 apps/*/src/ 和 packages/core/src/ 下搜索以下原生 HTML 元素的使用（排除 test 文件）：
   a. <button> → 应使用 <Button>
   b. <input> → 应使用 <Input>
   c. <textarea> → 应使用 <Textarea>
   d. <select>/<option> → 应使用 <Select> 或 <NativeSelect>
   e. <dialog> → 应使用 <Dialog>
   f. <table>/<tr>/<td> → 应使用 <Table> 系列
3. 对每个发现的候选项：
   a. 判断是否在 ui 包本身的内部实现中（合理例外）
   b. 评估替换的可行性和风险
   c. 排除合理例外：浏览器原生能力控件、iframe 嵌入等
4. 检查 @nop-chaos/ui 的导入模式是否一致（所有导入都从统一入口）。
5. 检查 cn() 的使用是否一致（而非 classNames 或模板字符串拼接）。
6. 检查是否有包直接依赖了 radix-ui / @base-ui 而不通过 @nop-chaos/ui（ui 包除外）。

输出格式：

对每个发现：
### [维度10] 简短标题
- **文件**: apps/main/src/xxx.tsx:行号
- **严重程度**: P0/P1/P2/P3
- **原生元素**: 使用的 HTML 元素
- **应替换为**: 对应的 @nop-chaos/ui 组件
- **替换可行性**: 高/中/低
```

---

## E. 工程质量

### 维度 11：类型安全与契约质量

**子 Agent 提示词**：

```
以下为"维度 11"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 11：类型安全与契约质量

目标：识别 any 使用中真正有运行时错误风险的场景，以及公共接口的文档化程度。

特别说明：
- 动态插件加载边界、SystemJS import map、amis schema 等场景的 any/Record<string, unknown> 是合理的。
- 不要把"出现 any"本身当成问题，要关注 any 是否导致了真实的运行时错误风险。
- ShellExtension 的动态配置对象天然是 unknown 语义。

执行步骤：

1. 搜索所有 explicit any 使用（: any, as any, <any>, any[]）：
   a. 按包分组统计 any 使用频率
   b. 对每个 any 判断其合理性：
      - 合理：动态插件配置、SystemJS 返回值、amis schema、第三方库回调
      - 可疑：内部已有更精确类型但未使用、类型断言链过长
      - 危险：有证据表明导致了运行时错误的 any
2. 检查类型断言链：
   a. 是否有 `as unknown as Xxx as Yyy` 这种多重断言
   b. 多重断言是否表明类型设计有缺陷
3. 检查公共接口文档化：
   a. packages/shared/src/types/ 中的类型是否有 JSDoc
   b. PluginBridge 接口的方法是否有文档
   c. ShellExtension 的字段是否有说明
4. 检查 @ts-expect-error 和 @ts-ignore：
   a. 是否有说明注释（eslint 要求）
   b. 注释的原因是否仍然有效
5. 检查跨包类型一致性：
   a. MenuItem 在 shared 和实际使用中是否一致
   b. AuthState 在 shared/types 和 store 中是否匹配
   c. ThemeConfig 在 shared 和 themeStore 中是否匹配

输出格式：

1. any 使用统计（按包分组，每包统计合理/可疑/危险数量）
2. 可疑/危险项详细清单：
### [维度11] 简短标题
- **文件**: packages/xxx/src/yyy.ts:行号
- **严重程度**: P0/P1/P2/P3
- **分类**: 可疑/危险
- **现状**: 当前用法
- **真实风险**: 可能导致什么运行时错误
- **建议**: 收敛方案
- **误报排除**: 为什么这不是合理的动态边界
```

---

### 维度 12：测试覆盖与质量

**子 Agent 提示词**：

```
以下为"维度 12"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 12：测试覆盖与质量

目标：评估测试覆盖的完整性、测试质量、以及测试结构是否有利于维护。

必读文档：
- AGENTS.md 的 Testing Expectations 和 Single-Test Commands 章节

执行步骤：

1. 统计每个包的测试文件数量和测试代码行数。列出测试文件数量为 0 的包。
2. 识别测试覆盖缺口：
   a. 列出所有 src/ 下有 .ts/.tsx 实现文件但没有对应 .test.ts/.test.tsx 的模块
   b. 特别关注：认证流程、权限守卫、菜单过滤、插件加载、store 操作、路由分发
3. 对每个超过 400 行的测试文件：
   a. 检查是否包含跨领域测试（应按领域拆分）
   b. 检查 setup 代码是否超过 50 行且未提取为 helper
   c. 检查 describe 嵌套是否超过 3 层
4. 检查测试隔离性：
   a. 是否有测试依赖全局状态且未在 beforeEach/afterEach 中重置
   b. 是否有 mock 未正确清理
5. 检查测试模式一致性：
   a. 是否所有包都使用 Vitest
   b. mock 模式是否一致（vi.fn() vs jest.fn()）
   c. 测试环境声明是否一致（node vs jsdom）
6. 检查 E2E 测试：
   a. tests/e2e/ 下的测试覆盖了哪些场景
   b. 是否有关键的登录/权限/插件加载/主题切换场景缺少 e2e 覆盖
7. 检查 plugin-bridge 的测试：
   a. 是否测试了 bridge 的 subscribe/getSnapshot 正确性
   b. 是否测试了跨组件 bridge 数据传播

输出格式：

1. 测试覆盖统计（按包）
2. 覆盖缺口清单
3. 测试质量问题清单：

### [维度12] 简短标题
- **文件**: packages/xxx/src/yyy.test.ts:行号
- **严重程度**: P0/P1/P2/P3
- **类别**: 覆盖缺口/跨域/setup膨胀/隔离性/一致性
- **现状**: 一句话描述
- **建议**: 改进方向

4. 优先级排序的测试改进建议
```

---

## 结果输出与归档

每个维度的初审 + 复核完成后，所有已通过独立复核的结果必须保存到 `docs/analysis/` 目录下的专用子目录中。

**目录结构**：

```
docs/analysis/{year}-{month}-{day}-deep-audit-{简短标识}/
├── 01-dependency-graph.md
├── 02-module-responsibility.md
├── 03-api-surface.md
├── 04-state-ownership.md
├── 05-reactive-precision.md
├── 06-async-safety.md
├── 07-routing-permissions.md
├── 08-plugin-bridge.md
├── 09-theme-styling.md
├── 10-component-usage.md
├── 11-type-safety.md
├── 12-test-coverage.md
└── summary.md
```

**命名规则**：

- 子目录名格式：`{YYYY}-{MM}-{DD}-deep-audit-{简短标识}`
- 每个维度一个 md 文件，文件名格式：`{维度编号}-{英文简短名}.md`
- `summary.md` 保存主 agent 的汇总报告（格式见附录 A）

---

## 附录 A：统一输出格式

### 单维度发现格式

每个维度的审核结果应按以下格式输出。**这是强制格式，不得简化、省略或压缩。**

```markdown
### [维度NN-序号] 简短标题

- **文件**: `packages/xxx/src/yyy.ts:行号范围`
- **证据片段**:
  ```ts
  // 贴出 3-10 行与结论直接相关的原文
  ```
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述当前问题
- **风险**: 不修复的后果
- **建议**: 一句话描述修复方向
- **误报排除**: 说明为什么这不是合理的动态边界或 orchestrator
- **复核状态**: `未复核` / `维度复核通过` / `子项复核通过` / `已降级` / `已驳回`
```

**反压缩规则（违反即为不合格输出）**：

| 反模式           | 示例                                        | 正确做法                                       |
| ---------------- | ------------------------------------------- | ---------------------------------------------- |
| 发现变成关键词   | `第 1 轮: store 持久化 key 不一致`          | 完整的 `###` 级发现条目                        |
| 发现变成表格行   | `store key \| 保留 \| P1 \| file.ts:15`    | 表格只做复核索引，正文保留完整格式              |
| 省略证据片段     | 只写文件路径不贴代码                        | 必须贴 3-10 行代码或标注 `[待补充]`            |
| 省略风险/建议    | 只写"存在问题"                              | 必须说明后果和修复方向                          |
| 合并多个发现     | "8 项 P1 问题"                              | 每个发现必须是独立的 `###` 条目                |
| 最终保留项无结构 | `- P1: store persistence`                   | 表格形式：编号、严重程度、文件、摘要            |

补充要求：

- 如果某维度"未发现需报告问题"，也要明确输出"零发现结论"，并说明读过哪些关键代码/文档后得出该判断。
- 每个发现条目至少 8 行。5 个发现的维度文件通常在 120-200 行。40 行写完 5 个发现说明发生了严重压缩。

### 汇总报告格式

主 agent 收到所有初审与复核子 agent 的结果后，输出汇总报告：

```markdown
# 深度审核汇总报告

## 审核范围
- 执行的维度：[列表]
- 覆盖的包：[列表]
- 审核日期：YYYY-MM-DD
- 执行方式：每个维度多轮迭代深挖 + 维度复核 + 子项复核，共 N 个子 agent

## 深挖统计
- 维度总数：[N]
- 各维度深挖轮次：维度01=3轮, 维度02=1轮(零发现), ...
- 深挖总轮次：[N]
- 深挖总发现数：[N]

## 复核统计
- 深挖发现总数：[N]
- 已独立复核条目数：[N]
- 保留：[N]
- 降级：[N]
- 驳回：[N]

## P0 清单（按文件分组）
[table]

## P1 清单（按文件分组）
[table]

## 高频问题文件（出现在多个维度中的文件）
[table]

## 跨维度模式（多个维度报告的同类问题）
[描述]

## 已自动化的检查项（lint/check 已覆盖，不需人工跟进）
[list]

## 建议新增的自动化检查
[list]

## 可暂缓项（有问题但 ROI 暂时不高）
[list]
```

---

## 附录 B：执行优先级与并行策略

如果时间有限，建议按以下批次执行维度。同批次的维度互相独立，可以并行派发。

| 批次       | 维度              | 原因                       | 并行度 |
| ---------- | ----------------- | -------------------------- | ------ |
| **第一批** | 01 包边界         | 架构基础，影响所有其他维度 | 3 并行 |
|            | 04 状态所有权     | 应用核心，历史高频 bug     |        |
|            | 11 类型安全       | 工程质量基线               |        |
| **第二批** | 05 订阅精度       | 性能相关                   | 3 并行 |
|            | 06 异步模式       | 运行时安全                 |        |
|            | 07 路由权限       | 安全关键                   |        |
| **第三批** | 02 模块职责       | 可渐进改进                 | 3 并行 |
|            | 08 插件桥接       | 系统集成关键               |        |
|            | 12 测试覆盖       | 质量保障基础               |        |
| **第四批** | 03 API 表面积     | 长期维护性                 | 3 并行 |
|            | 09 主题样式       | 视觉一致性                 |        |
|            | 10 组件使用       | 代码一致性                 |        |

---

## 附录 C：执行注意事项

1. **子 agent 类型**: 所有维度使用 `subagent_type="explore"`，因为审核以代码搜索和阅读为主。
2. **派发时必须拼接共享前缀**: 下文维度章节默认只提供"维度正文"；主 agent 派发时必须把"共享提示词前缀 + 维度正文"拼接成完整 prompt。
3. **上下文隔离**: 每个子 agent 有独立的上下文窗口，不需要担心维度之间互相干扰。
4. **独立复核是强制步骤**: 每个维度必须经过"迭代深挖 → 维度复核"；高风险或不确定发现还必须继续做子项复核。
5. **复核 agent 必须独立**: 不能让深挖 agent 自己给自己复核；必须换新的子 agent。
6. **结果汇总**: 主 agent 负责收集所有子 agent 的输出，去重（同一问题可能被多个维度发现），仅汇总已通过独立复核的结果。
7. **增量审核**: 可以只选择部分维度执行，不必每次全量审核。用维度编号指定要执行的维度即可，但选中的维度仍需完整深挖+复核链路。
8. **与现有检查的关系**: 审核发现的机械问题应优先转化为 lint 规则或 check 脚本，而非持续依赖审核。
9. **结果归档**: 审核结果必须保存到 `docs/analysis/{YYYY}-{MM}-{DD}-deep-audit-{简短标识}/` 子目录中。
10. **需要命令基线的维度**: 若某维度依赖 `pnpm check:*`、依赖图、文件行数等命令输出，优先由主 agent 先跑命令并把结果传给子 agent。
11. **深挖轮次上限**: 每个维度最多 10 轮深挖（含初审），防止无限循环。达到上限后强制进入复核阶段。

---

## 附录 D：快速调度模板

### 单维度调度（第 1 轮初审）

```
Task(
  subagent_type="explore",
  description="维度01: 依赖图与包边界 (第1轮初审)",
  prompt=<共享提示词前缀 + 维度 01 正文>
)
```

### 深挖追加轮次调度

```
Task(
  subagent_type="explore",
  description="维度01: 依赖图与包边界 (第N轮深挖)",
  prompt=<共享提示词前缀 + 维度 01 正文 + 深挖追加指令（第 N 轮）+ 已有发现全文>
)
```

### 复核调度模板

```text
维度复核：
- 输入：该维度完整文件（含所有深挖轮次发现）
- 要求：重新读取 live code / 当前文档，不把深挖结论当事实；输出保留 / 降级 / 驳回清单

子项逐条复核（用于 P0/P1、高风险、或存在争议的条目）：
- 输入：单条发现全文 + 文件路径 + 行号范围 + 证据片段
- 要求：只复核这一条，输出成立 / 降级 / 驳回与理由

批量复核（用于低风险重复项）：
- 输入：同文件或同模式的一组发现
- 要求：逐条给出保留 / 降级 / 驳回结果，不得只输出总评
```

### 全量调度

```
阶段一（迭代深挖）—— 按 4 个批次顺序派发各维度第 1 轮初审，每批次内并行：
  批次 1: Task(01-R1) + Task(04-R1) + Task(11-R1)
  批次 2: Task(05-R1) + Task(06-R1) + Task(07-R1)
  批次 3: Task(02-R1) + Task(08-R1) + Task(12-R1)
  批次 4: Task(03-R1) + Task(09-R1) + Task(10-R1)

  对有发现的维度，串行追加深挖轮次直到无新发现。
  零发现维度跳过深挖，直接进入阶段二。

阶段二（复核）—— 各维度深挖结束后并行派发复核：
  各维度: Task(NN-维度复核) → Task(NN-子项复核)* → 写入最终复核结论
```

---

## 附录 E：主 Agent 执行说明

### 执行流程总览

```
1. 准备阶段
   └─ 读取本文档 + 创建输出目录

2. 阶段一：迭代深挖（按批次）
   ├─ 批次 1：并行派发 01/04/11 第 1 轮
   │   ├─ 保存结果
   │   ├─ 对有发现的维度：串行追加第 2,3,... 轮深挖
   │   └─ 对零发现维度：标记为"待复核确认"
   ├─ 批次 2-4：同上
   └─ 全部维度深挖结束

3. 阶段二：复核
   ├─ 并行派发所有维度的维度复核子 agent
   ├─ 收到维度复核结果后：
   │   ├─ 高风险项 → 派发逐条复核子 agent
   │   └─ 低风险项 → 按文件/模式批量复核
   └─ 全部复核完成

4. 汇总阶段
   ├─ 仅汇总"已通过独立复核"的条目
   ├─ 写入 summary.md
   └─ 更新 docs/logs/ 开发日志
```

### 每个维度的详细执行步骤

**步骤 1：派发第 1 轮（初审）**

```
Task(
  subagent_type="explore",
  description="维度NN: 名称 (第1轮初审)",
  prompt=<共享提示词前缀 + 维度 NN 正文>
)
```

**步骤 2：保存第 1 轮结果**

将子 agent 返回的发现写入 `docs/analysis/{日期}-deep-audit-{标识}/{NN}-{名}.md`。

保存前必须做完整性检查（参见"发现条目完整性守则"）。

**步骤 3：判断是否需要深挖**

- 如果第 1 轮有 ≥1 个发现：进入步骤 4（追加深挖）。
- 如果第 1 轮零发现：跳到步骤 7（维度复核确认零发现）。

**步骤 4-5：追加深挖**（使用"深挖追加提示词模板"）

**步骤 6：维度复核**

```
Task(
  subagent_type="explore",
  description="维度NN: 维度复核",
  prompt=<读取该维度完整文件，要求重新核对 live code，逐条输出保留/降级/驳回>
)
```

**步骤 7：子项复核（分层）**

- P0/P1 及高风险条目：逐条派发独立子 agent 复核。
- P2/P3 低风险同类条目：按文件或模式批量复核。
- 零发现维度：仍需一个复核 agent 确认。

**步骤 8：写入复核结论** → 追加到维度文件。

### 汇总报告生成

所有维度完成后：

1. 扫描每个维度文件的复核结论，仅保留"保留"和"子项复核通过"的条目。
2. 去重：同一问题被多个维度报告时合并，保留最详细的那个。
3. 按附录 A 的汇总报告格式输出，写入 `summary.md`。
4. 汇总报告必须包含"深挖统计"（每维度深挖轮次）和"复核统计"。
