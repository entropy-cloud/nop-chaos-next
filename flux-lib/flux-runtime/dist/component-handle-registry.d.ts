import type { ComponentHandleRegistry } from '@nop-chaos/flux-core';
export declare function createComponentHandleRegistry(input: {
    id: string;
    parent?: ComponentHandleRegistry;
}): ComponentHandleRegistry;
