import { createRoot } from 'react-dom/client';

createRoot(document.getElementById('root')!).render(
  <div>
    <h1>External Demo — Standalone Preview</h1>
    <p>Edit <code>src/index.ts</code> to define your extension.</p>
  </div>,
);
